---
phase: 02-git-workflow-and-loop-control
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - plugins/application-dev/agents/generator.md
  - plugins/application-dev/agents/evaluator.md
  - plugins/application-dev/agents/planner.md
autonomous: true
requirements: [GIT-02, GIT-03, GIT-04, LOOP-06, LOOP-07, LOOP-08]

must_haves:
  truths:
    - "Generator agent definition instructs feature-by-feature git commits with conventional commit messages"
    - "Generator agent definition instructs .gitignore creation/extension with node_modules/, build output, and .playwright-cli/"
    - "Generator agent definition enforces fix-only mode in rounds 2+ (no new features, no refactoring working code)"
    - "Generator agent definition instructs reading evaluation/round-{N-1}/EVALUATION.md before SPEC.md in rounds 2+"
    - "Evaluator agent definition instructs committing report and artifacts to evaluation/round-N/"
    - "All three agent definitions use GAN ubiquitous language (evaluation, not QA; EVALUATION.md, not QA-REPORT.md)"
    - "No agent definition contains the word 'QA' or references 'QA-REPORT.md' or 'qa/' folder"
  artifacts:
    - path: "plugins/application-dev/agents/generator.md"
      provides: "Generator with git commits, fix-only mode, evaluation path, GAN language"
      contains: "evaluation/round-"
    - path: "plugins/application-dev/agents/evaluator.md"
      provides: "Evaluator with git commits to evaluation/round-N/, GAN language"
      contains: "evaluation/round-"
    - path: "plugins/application-dev/agents/planner.md"
      provides: "Planner with GAN language (no QA references)"
  key_links:
    - from: "plugins/application-dev/agents/generator.md"
      to: "evaluation/round-{N-1}/EVALUATION.md"
      via: "Read instruction in rounds 2+ section"
      pattern: "evaluation/round-.*EVALUATION\\.md"
    - from: "plugins/application-dev/agents/evaluator.md"
      to: "evaluation/round-N/"
      via: "Write + git commit instruction"
      pattern: "evaluation/round-"
---

<objective>
Update all three agent definitions with GAN ubiquitous language (QA -> evaluation rename), git commit instructions for Generator and Evaluator, Generator fix-only mode for rounds 2+, and Generator context loading order (EVALUATION.md before SPEC.md).

Purpose: Agents must commit their own work to git for the version-controlled workflow. The GAN language rename ensures consistent terminology across the entire plugin. Fix-only mode and evaluation-first reading order are damping controls that prevent the Generator from oscillating or adding scope in later rounds.

Output: Updated planner.md, generator.md, evaluator.md
</objective>

<execution_context>
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-git-workflow-and-loop-control/02-CONTEXT.md
@.planning/phases/02-git-workflow-and-loop-control/02-RESEARCH.md
@.planning/phases/01-orchestrator-integrity/01-01-SUMMARY.md

<interfaces>
<!-- Current agent tool allowlists (from frontmatter) -->
Planner tools: ["Read", "Write"]
Generator tools: ["Read", "Write", "Edit", "Glob", "Bash"]
Evaluator tools: ["Read", "Write", "Glob", "Bash"]

<!-- GAN ubiquitous language rename mapping (from CONTEXT.md) -->
| Old | New |
|-----|-----|
| QA-REPORT.md | EVALUATION.md |
| qa/ | evaluation/ |
| qa/round-N/ | evaluation/round-N/ |
| "QA round" | "evaluation round" |
| "QA feedback" | "Evaluator's feedback" |
| "build round" | "generation round" |
| "Building" | "Generating" |
| "QA engineer" | (remove from evaluator.md identity) |

<!-- Per-round evaluation folder structure (from CONTEXT.md) -->
evaluation/round-N/
  EVALUATION.md       (main report)
  screenshots/        (visual evidence)
  console.log         (JS console output)
  network.log         (network requests)

<!-- Generator reads evaluation/round-{N-1}/EVALUATION.md in rounds 2+ -->
<!-- Evaluator writes to evaluation/round-N/ and derives N from prompt -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Generator and Evaluator with git commits, evaluation paths, and loop behavior</name>
  <files>plugins/application-dev/agents/generator.md, plugins/application-dev/agents/evaluator.md</files>
  <action>
**Generator (generator.md) -- 7 changes:**

1. **GAN language rename throughout the file:**
   - All occurrences of "QA-REPORT.md" -> "EVALUATION.md"
   - All occurrences of "qa/" -> "evaluation/"
   - "QA feedback" -> "Evaluator's feedback"
   - "build round" -> "generation round"
   - "Build Round" -> "Generation Round" (in headers)
   - "QA round" -> "evaluation round"
   - In the frontmatter description examples: update round references to use "generation round" language and "EVALUATION.md" instead of "QA-REPORT.md"

2. **Update prompt guard (Rule 1):** Change from "Do not write to the `qa/` folder or `QA-REPORT.md`" to "Do not write to the `evaluation/` folder or `EVALUATION.md`. These are Evaluator-only artifacts. Your output domain is the application source code, configuration, and `README.md`."

3. **Add git commit instructions to Round 1 Build Process:** After step 1 (Set up the project), add a new instruction: "After setting up the project structure and installing dependencies, make your first git commit: `git add <project files> && git commit -m 'feat(<project>): initial project setup'`. Then commit after each major feature is implemented -- not all at once at the end. Use conventional commits with the feature as scope, e.g., `feat(editor): implement level editor`, `feat(design): establish visual design system`. Keep the application runnable between commits."

4. **Add .gitignore instruction (GIT-03):** In the Round 1 Build Process, after setting up the project, add: "Create or extend `.gitignore` with entries appropriate for your tech stack. At minimum include: `node_modules/`, the build output folder (e.g., `dist/`, `build/`, `.next/`), and `.env` if applicable. The orchestrator has already seeded `.gitignore` with `.appdev-state.json`, `.playwright-cli/`, and `node_modules/` -- extend it, do not overwrite."

5. **Rewrite Rounds 2+ section for fix-only mode (LOOP-07):** Replace the current "Rounds 2+ (With QA Feedback)" opening with:
   "Read `evaluation/round-{N-1}/EVALUATION.md` first, THEN re-read the relevant sections of `SPEC.md`. Reading the Evaluator's feedback before the spec primes you for fixing, not building."

   Add a prominent rule at the top of the Rounds 2+ section:
   "**Fix-only mode (rounds 2+):** In rounds 2 and later, you are a surgeon, not an architect. Fix ONLY what the Evaluator flagged -- do not add new features, do not refactor working code, do not 'improve' things the Evaluator did not mention. Every change must trace back to a specific item in the Evaluator's report. This is the cybernetics damping principle: unconstrained changes cause oscillation instead of convergence."

6. **Update the Rounds 2+ evaluation path (LOOP-08):** Change "Read `QA-REPORT.md` carefully" to specify the exact path: "Read `evaluation/round-{N-1}/EVALUATION.md` carefully, where N is the current generation round number. For example, generation round 2 reads `evaluation/round-1/EVALUATION.md`, generation round 3 reads `evaluation/round-2/EVALUATION.md`."

7. **Add git commit instructions to Rounds 2+:** After completing fixes, add: "Commit your fixes with conventional commit messages scoped to the feature or area fixed, e.g., `fix(editor): resolve canvas click handler`, `fix(design): restore typography hierarchy`. Commit after each logical fix, not all at once."

**Evaluator (evaluator.md) -- 6 changes:**

1. **GAN language rename throughout the file:**
   - Remove "QA engineer" from the opening identity line. Change "You are a rigorous, skeptical QA engineer and product critic" to "You are a rigorous, skeptical product critic and adversarial evaluator."
   - All occurrences of "QA-REPORT.md" -> "EVALUATION.md"
   - All occurrences of "qa/" -> "evaluation/"
   - "QA round" -> "evaluation round"
   - "build/QA loop" -> "generation/evaluation loop"
   - "build/QA round" -> "generation/evaluation round"
   - In frontmatter description: update "QA" references to "evaluation"

2. **Update output domain prompt guard (Rule 2):** Change from "Your output domain is strictly `QA-REPORT.md` and files within the `qa/` folder" to "Your output domain is strictly `EVALUATION.md` and files within the `evaluation/` folder. Specifically, write to `evaluation/round-N/` where N is the current evaluation round number."

3. **Update file write instructions (section 8):** Change "Write your report to `QA-REPORT.md` in the working directory" to "Write your report to `evaluation/round-N/EVALUATION.md` where N is the current evaluation round number (derive from the prompt, e.g., 'This is evaluation round 2' means write to `evaluation/round-2/EVALUATION.md`). Also save screenshots to `evaluation/round-N/screenshots/`."

4. **Add git commit instructions:** After section 8 (Write EVALUATION.md), add a new step before Clean Up:
   "### 8.5. Commit Evaluation Artifacts
   Commit your evaluation report and artifacts to git:
   ```bash
   git add evaluation/round-N/
   git commit -m 'docs(evaluation): round N report'
   ```
   Replace N with the current round number."

5. **Update regression detection (section 2):** Change "Read the existing `QA-REPORT.md`" to "Read the previous round's report at `evaluation/round-{N-1}/EVALUATION.md`" with the same derivation logic.

6. **Update self-verification:** Change "re-read `QA-REPORT.md`" to "re-read `evaluation/round-N/EVALUATION.md`".

7. **Update the evaluator's playwright-cli usage:** Change `playwright-cli` to `npx playwright-cli` throughout (since it is now a project devDependency via @playwright/cli, not a system PATH tool). This affects: the "Essential commands" section where `playwright-cli open`, `playwright-cli snapshot`, `playwright-cli screenshot`, etc. are shown. Prefix each with `npx`.
  </action>
  <verify>
    <automated>! git grep -q "QA-REPORT\|qa/" -- plugins/application-dev/agents/generator.md plugins/application-dev/agents/evaluator.md</automated>
  </verify>
  <done>Zero matches for "QA-REPORT" or "qa/" in generator.md and evaluator.md. Generator has fix-only mode, evaluation-first reading, feature-by-feature git commits, and .gitignore instructions. Evaluator writes to evaluation/round-N/ and commits after each round. Both use npx playwright-cli.</done>
</task>

<task type="auto">
  <name>Task 2: Update Planner with GAN ubiquitous language</name>
  <files>plugins/application-dev/agents/planner.md</files>
  <action>
**Planner (planner.md) -- 2 changes:**

1. **GAN language rename:** The Planner has minimal QA references but they must be cleaned:
   - In Critical Rules item 6: change "Do not read or reference QA-REPORT.md or any QA artifacts" to "Do not read or reference EVALUATION.md or any evaluation artifacts."
   - Search the entire file for any remaining "QA" references and replace with appropriate evaluation terminology.

2. **No git commit instructions for Planner.** Per RESEARCH.md recommendation and CONTEXT.md principle: the Planner has only Read and Write tools (no Bash). The orchestrator will commit SPEC.md after confirming it exists. Do NOT add Bash to the Planner's tools array. Do NOT add git instructions to the Planner.

Verify: The Planner's `tools: ["Read", "Write"]` frontmatter remains unchanged. No "QA" text remains anywhere in the file.
  </action>
  <verify>
    <automated>! git grep -q "QA" -- plugins/application-dev/agents/planner.md</automated>
  </verify>
  <done>Zero matches for "QA" in planner.md. Planner tools unchanged at ["Read", "Write"]. GAN language consistent across all three agent definitions.</done>
</task>

</tasks>

<verification>
1. `git grep -c "QA-REPORT\|QA_REPORT\|qa/" -- plugins/application-dev/agents/` returns zero matches
2. `git grep "evaluation/round-" -- plugins/application-dev/agents/generator.md` shows evaluation path references
3. `git grep "evaluation/round-" -- plugins/application-dev/agents/evaluator.md` shows evaluation path references
4. `git grep "fix-only\|Fix-only\|Fix ONLY" -- plugins/application-dev/agents/generator.md` confirms LOOP-07
5. `git grep "npx playwright-cli" -- plugins/application-dev/agents/evaluator.md` confirms npx usage
6. Generator.md contains ".gitignore" instructions (GIT-03)
7. Planner tools remain `["Read", "Write"]` -- no Bash added
</verification>

<success_criteria>
- All three agent definitions use "evaluation" terminology exclusively (zero "QA" references)
- Generator commits feature-by-feature with conventional commits (GIT-02)
- Generator creates/extends .gitignore (GIT-03)
- Generator reads evaluation/round-{N-1}/EVALUATION.md before SPEC.md in rounds 2+ (LOOP-08)
- Generator enforces fix-only mode in rounds 2+ (LOOP-07)
- Evaluator writes to evaluation/round-N/ and commits (GIT-04)
- Evaluator uses npx playwright-cli (project devDependency)
- LOOP-06 acknowledged as deferred to Phase 3 (no feature watchdog in any agent)
- Planner unchanged except GAN language cleanup
</success_criteria>

<output>
After completion, create `.planning/phases/02-git-workflow-and-loop-control/02-02-SUMMARY.md`
</output>
