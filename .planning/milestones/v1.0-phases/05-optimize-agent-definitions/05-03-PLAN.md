---
phase: 05-optimize-agent-definitions
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/application-dev/agents/generator.md
  - plugins/application-dev/agents/planner.md
autonomous: true
requirements: [OPT-01, OPT-05]
must_haves:
  truths:
    - "Generator skills note reframed as a design choice (selective loading), not a bug workaround"
    - "Generator rules use WHY-based rationale instead of bare constraints"
    - "Planner emphasis cleaned up with WHY-based rationale where beneficial"
    - "Generator round-conditional headings use consistent convention"
    - "No regression in generator build process, tech stack selection, or testing framework"
    - "No regression in planner output format, SPEC template reference, or self-verification"
  artifacts:
    - path: "plugins/application-dev/agents/generator.md"
      provides: "Generator agent with refined emphasis and reframed skills note"
      contains: "selective loading"
    - path: "plugins/application-dev/agents/planner.md"
      provides: "Planner agent with minor emphasis cleanup"
      contains: "Self-Verification"
  key_links:
    - from: "plugins/application-dev/agents/generator.md"
      to: "skills/vite-plus/SKILL.md"
      via: "Read instruction in Tech Stack Selection"
      pattern: "vite-plus/SKILL.md"
    - from: "plugins/application-dev/agents/generator.md"
      to: "skills/playwright-testing/SKILL.md"
      via: "Read instruction in Phase 3"
      pattern: "playwright-testing/SKILL.md"
    - from: "plugins/application-dev/agents/planner.md"
      to: "references/SPEC-TEMPLATE.md"
      via: "Read instruction in Output Format"
      pattern: "SPEC-TEMPLATE.md"
---

<objective>
Apply minor instruction engineering refinements to generator.md and planner.md -- WHY-based rationale, skills note reframing, and emphasis cleanup.

Purpose: Generator and planner are already well-structured (253 and 98 lines respectively) and do not need extraction or restructuring. The refinements align them with the same instruction engineering principles applied to evaluator.md and SKILL.md: WHY-based rationale instead of bare directives, consistent emphasis patterns, and accurate framing of the skills loading mechanism.

Output: Refined generator.md and planner.md with consistent emphasis patterns across all agent definitions
</objective>

<execution_context>
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-optimize-agent-definitions/05-CONTEXT.md
@.planning/phases/05-optimize-agent-definitions/05-RESEARCH.md

@plugins/application-dev/agents/generator.md
@plugins/application-dev/agents/planner.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refine generator.md emphasis, skills note, and round-conditional labels</name>
  <files>plugins/application-dev/agents/generator.md</files>
  <action>
Apply targeted refinements to generator.md. This is surgical editing, not a rewrite -- the file structure, section ordering, and content are already correct from the Phase 4 rewrite.

**1. Reframe the skills loading note (line 215):**

Current (bug-workaround framing):
```
**Skill loading note:** The `skills` frontmatter above lists all 6 skills for auto-injection. Due to bug #25834 (plugin agent skills frontmatter may not inject content), always use the explicit Read instructions provided in each step above as a fallback.
```

New (design-choice framing, per CONTEXT.md decision):
```
**Skill loading:** The `skills` frontmatter lists all 6 skills for documentation and future auto-injection. The Read instructions in each step above are the primary mechanism -- they enable selective loading (~2-3k tokens on demand vs ~15k tokens if all skills were injected upfront).
```

This reframes Read as superior by design (selective loading efficiency), not as a bug workaround. One line, no multi-paragraph justification.

**2. Add WHY rationale to the Rules section (line 219):**

Current:
```
1. **Do not write to the `evaluation/` folder or `EVALUATION.md`.** These are Evaluator-only artifacts. Your output domain is the application source code, configuration, and `README.md`.
```

The rule already has a brief rationale ("These are Evaluator-only artifacts"), but strengthen it:
```
1. **Do not write to the `evaluation/` folder or `EVALUATION.md`.** These belong to the Evaluator agent -- writing there would contaminate the adversarial feedback loop by mixing generator output with evaluator analysis.
```

**3. Add WHY rationale to Quality Standards where bare directives exist:**

Scan the Quality Standards section (lines 222-231). Add brief WHY clauses to any items that are bare directives:

- "No stubs" -> "No stubs. Placeholder features waste a generation round because the Evaluator will flag them and the Generator must implement them anyway."
- "No fabricated URLs" -> "No fabricated URLs. External URLs that return 404 break the app for every user and trigger Evaluator's asset validation as Critical bugs."
- Keep items that already have implicit rationale (e.g., "Error handling. The app should not crash on common user actions" -- the WHY is implicit).

**4. Clean up any remaining ALL-CAPS emphasis:**

Scan for MUST/NEVER/CRITICAL/ALWAYS in ALL-CAPS. The generator.md should have very few after the Phase 4 rewrite, but check:
- Line 244: "Your application MUST be startable" -> "The application must be startable" (lowercase, imperative)
- Any other instances: convert to normal phrasing.

**5. Ensure round-conditional heading consistency:**

Verify the Generator uses this heading pattern consistently:
- "### Round 1 (No Prior Evaluation Feedback)" -- correct, keep
- "### Rounds 2+ (With Evaluation Feedback)" -- correct, keep

These match the evaluator's "### Step 2: Check for Regressions (Rounds 2+ Only)" convention of putting the condition in the heading. No change needed if already consistent.

**Preserve all integration contracts:**
- All `${CLAUDE_PLUGIN_ROOT}` Read instructions for skills (vite-plus, playwright-testing, vitest-browser, browser-prompt-api, browser-webllm, browser-webnn)
- The `skills:` frontmatter field (keep all 6 skills listed)
- The `tools: ["Read", "Write", "Edit", "Glob", "Bash"]` frontmatter
- The description field with examples
- The entire Build Process structure (Phase 1-4, Steps 1-10)
- The fix-only mode content in Rounds 2+
- The testing decision framework table
- The Install and Start Convention section

Target: ~253 lines (same or very close -- this is refinement, not reduction).
  </action>
  <verify>
    <automated>wc -l plugins/application-dev/agents/generator.md && git grep -c "CLAUDE_PLUGIN_ROOT" plugins/application-dev/agents/generator.md && git grep "selective loading" plugins/application-dev/agents/generator.md</automated>
  </verify>
  <done>generator.md is approximately 253 lines (+-5). All skill Read instructions preserved (CLAUDE_PLUGIN_ROOT count matches or exceeds current ~10). Skills note reframed with "selective loading" wording. WHY rationale added to Rules and Quality Standards. No ALL-CAPS MUST/NEVER/CRITICAL emphasis remaining.</done>
</task>

<task type="auto">
  <name>Task 2: Refine planner.md emphasis with WHY-based rationale</name>
  <files>plugins/application-dev/agents/planner.md</files>
  <action>
Apply minimal refinements to planner.md. The file is compact (98 lines) and well-structured. Only touch what needs emphasis cleanup.

**1. Scan for ALL-CAPS emphasis and convert:**

Check for any MUST/NEVER/CRITICAL/ALWAYS in ALL-CAPS. Known instances:
- Line 42, Rule 1: "Do NOT specify a tech stack or AI services" -- convert "NOT" to lowercase "not" since the rest of the sentence explains the constraint clearly.
- Any other instances: convert to normal phrasing with WHY rationale if missing.

**2. Add WHY rationale to the anti-AI-slop instruction in Guidelines for Visual Design:**

Current (line 72):
```
- Actively penalize AI-slop markers in your thinking: purple gradients over white cards, generic hero sections, stock illustration styles, excessive rounded corners with shadow cards
```

This is already effective guidance but could benefit from a brief WHY:
```
- Actively avoid AI-slop patterns (purple gradients over white cards, generic hero sections, stock illustration styles, excessive rounded corners with shadow cards) -- these are the most common markers of LLM-generated applications and signal a lack of intentional design.
```

**3. Verify self-verification section is clean:**

The planner's Self-Verification (lines 89-98) should already be in good shape. Verify no MUST/NEVER emphasis. Ensure the "fix the file before completing" instruction uses the same phrasing pattern as the evaluator's Step 14.

**Preserve all integration contracts:**
- The `tools: ["Read", "Write"]` frontmatter
- The description field with examples
- The `${CLAUDE_PLUGIN_ROOT}` Read instructions for SPEC-TEMPLATE.md and frontend-design-principles.md
- The Output Format section referencing SPEC-TEMPLATE.md
- The File Write Requirements section
- The entire Self-Verification checklist (6 items)
- Rule 6: "You may only write SPEC.md" (output domain constraint -- keep as "Do not" if converting emphasis)

Target: ~98 lines (same -- minimal changes).
  </action>
  <verify>
    <automated>wc -l plugins/application-dev/agents/planner.md && git grep -c "CLAUDE_PLUGIN_ROOT" plugins/application-dev/agents/planner.md && git grep -c "MUST\|NEVER\|CRITICAL\|ALWAYS" plugins/application-dev/agents/planner.md</automated>
  </verify>
  <done>planner.md is approximately 98 lines (+-3). Both CLAUDE_PLUGIN_ROOT Read instructions preserved (count = 2). ALL-CAPS emphasis count is 0 (or near-0, only in frontmatter description if needed). Self-Verification checklist intact with 6 items.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `wc -l plugins/application-dev/agents/generator.md plugins/application-dev/agents/planner.md` shows both at approximately their current sizes
2. Generator: skills note contains "selective loading" (not "bug #25834" as primary framing)
3. Generator: all 6 skill Read instructions present
4. Generator: Rules section has WHY rationale
5. Planner: both CLAUDE_PLUGIN_ROOT Read instructions present
6. Planner: Self-Verification has 6 items
7. Neither file has ALL-CAPS MUST/NEVER/CRITICAL/ALWAYS emphasis (grep count 0 or near-0)
8. Overall: `wc -l plugins/application-dev/agents/*.md plugins/application-dev/skills/application-dev/SKILL.md` shows total line count reduced from 1,277 to under 1,200
</verification>

<success_criteria>
- Generator skills note reframed around selective loading efficiency
- Generator and planner have WHY-based rationale on key rules/constraints
- Zero or near-zero ALL-CAPS emphasis in both files
- All skill Read instructions preserved in generator
- All template Read instructions preserved in planner
- Both agents' frontmatter (tools, skills, description) unchanged
- Line counts approximately unchanged (refinement, not reduction)
</success_criteria>

<output>
After completion, create `.planning/phases/05-optimize-agent-definitions/05-03-SUMMARY.md`
</output>
