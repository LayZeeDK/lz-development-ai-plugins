---
phase: 03-evaluator-hardening
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - plugins/application-dev/agents/evaluator.md
autonomous: false
requirements: [EVAL-01, EVAL-02, EVAL-03, EVAL-04, EVAL-05]

must_haves:
  truths:
    - "Evaluator workflow has 15 steps in the correct order from CONTEXT.md"
    - "Evaluator installs sharp, imghash, and leven as devDependencies at step 3"
    - "Evaluator scrolls every page in viewport-height chunks at 4 responsive breakpoints at step 5"
    - "Evaluator loads AI-PROBING-REFERENCE.md at step 8 and generates domain-appropriate probes from SPEC.md"
    - "Evaluator lists ALL findings before scoring at step 11 (bug-first workflow)"
    - "Evaluator loads SCORING-CALIBRATION.md at step 12 before assigning any scores"
    - "ELIZA effect warning is present in Critical Mindset section naming the phenomenon explicitly"
    - "AI slop checklist is present with specific observable patterns"
    - "Self-verification has 10 checks including 4 feature watchdog rules"
    - "Off-spec features guidance uses correct terminology and scoring impact"
  artifacts:
    - path: "plugins/application-dev/agents/evaluator.md"
      provides: "Hardened adversarial evaluator with 15-step workflow, asset validation, AI probing, calibrated scoring"
      contains: "ELIZA effect"
  key_links:
    - from: "evaluator.md step 3"
      to: "npm install --save-dev sharp imghash leven"
      via: "Install Analysis Toolchain step"
      pattern: "sharp.*imghash.*leven"
    - from: "evaluator.md step 8"
      to: "AI-PROBING-REFERENCE.md"
      via: "${CLAUDE_PLUGIN_ROOT} reference path"
      pattern: "AI-PROBING-REFERENCE"
    - from: "evaluator.md step 12"
      to: "SCORING-CALIBRATION.md"
      via: "${CLAUDE_PLUGIN_ROOT} reference path"
      pattern: "SCORING-CALIBRATION"
    - from: "evaluator.md step 13"
      to: "EVALUATION-TEMPLATE.md"
      via: "${CLAUDE_PLUGIN_ROOT} reference path"
      pattern: "EVALUATION-TEMPLATE"
---

<objective>
Rewrite the Evaluator agent definition with the hardened 15-step workflow, ELIZA effect warning, AI slop checklist, off-spec features guidance, and extended 10-check self-verification.

Purpose: This transforms the Evaluator from a general-purpose critic (9 steps, 3 self-checks) into an adversarial quality gate (15 steps, 10 self-checks) with structural enforcement mechanisms. The reference files from Plan 01 are loaded on demand at appropriate workflow steps.

Output:
- MODIFIED: agents/evaluator.md (complete rewrite of Workflow section, extensions to Critical Mindset and Self-Verification, new AI Slop Checklist section)
</objective>

<execution_context>
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-evaluator-hardening/03-CONTEXT.md
@.planning/phases/03-evaluator-hardening/03-RESEARCH.md
@.planning/phases/03-evaluator-hardening/03-01-SUMMARY.md
@plugins/application-dev/agents/evaluator.md
@plugins/application-dev/skills/application-dev/references/EVALUATION-TEMPLATE.md
@plugins/application-dev/skills/application-dev/references/SCORING-CALIBRATION.md
@plugins/application-dev/skills/application-dev/references/AI-PROBING-REFERENCE.md
@plugins/application-dev/skills/application-dev/references/frontend-design-principles.md

<interfaces>
<!-- Current evaluator.md structure (289 lines) that will be rewritten -->
<!-- From plugins/application-dev/agents/evaluator.md -->

Current structure:
- Frontmatter (name, description, model, color, tools)
- ## Critical Mindset (5 paragraphs: try to break it, skeptical, don't rationalize, don't inflate, default to strict, actionable feedback)
- ## Workflow (9 steps: Understand Spec, Check Regressions, Start App, Test with playwright-cli, Test API, Review Code, Score and Report, Write EVALUATION.md, Clean Up)
- ## Rules (6 rules: never modify source, output domain, test like real user, specific bugs, score honestly, useful critique)
- ## Self-Verification (3 checks: verdict line, scores table, priority fixes)

Tools: ["Read", "Write", "Glob", "Bash"]
Model: inherit
Color: yellow

<!-- Reference file paths (created by Plan 01) -->
${CLAUDE_PLUGIN_ROOT}/skills/application-dev/references/SCORING-CALIBRATION.md
${CLAUDE_PLUGIN_ROOT}/skills/application-dev/references/AI-PROBING-REFERENCE.md
${CLAUDE_PLUGIN_ROOT}/skills/application-dev/references/EVALUATION-TEMPLATE.md
${CLAUDE_PLUGIN_ROOT}/skills/application-dev/references/frontend-design-principles.md
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite evaluator.md with hardened workflow</name>
  <files>plugins/application-dev/agents/evaluator.md</files>
  <action>
Read the existing evaluator.md completely. Rewrite it preserving the frontmatter (name, description, model, color, tools) and overall section structure, but with significant changes to Critical Mindset, Workflow, and Self-Verification sections. Add a new AI Slop Checklist section.

**PRESERVE UNCHANGED:**
- The YAML frontmatter block (name, description with examples, model, color, tools)
- The opening paragraph ("You are a rigorous, skeptical product critic...")
- The ## Rules section (6 rules -- unchanged, but add rule 7)

**MODIFY: ## Critical Mindset**

Keep all 6 existing paragraphs (Try to break it, Be skeptical, Do not rationalize, Do not inflate, Default to strict, Make feedback actionable). Add one new paragraph:

**ELIZA effect warning** (add after "Be skeptical of surface impressions" paragraph):
"First impressions of intelligence are unreliable -- the Generator's AI features may trigger the ELIZA effect (Weizenbaum, 1966), where polished UI and keyword-relevant responses create an illusion of understanding. A chatbot that echoes your keywords in grammatically correct sentences is not evidence of real AI. Extra skepticism is required for emotionally engaging AI features (therapy chatbot, wellness coach, companion) where the ELIZA effect is strongest and canned responses most harmful to users. Do not assess AI features from the happy path alone -- the probe battery in step 8 exists because surface impressions deceive."

**ADD: ## AI Slop Checklist** (new section, after Critical Mindset, before Workflow)

This is a concrete checklist of observable AI-slop design patterns. The Evaluator checks these during Visual Design Assessment (step 5 scroll-and-inspect, step 10 code review). Items that match lower the Visual Design score per the rubric (4-5: "framework defaults, template look, AI-slop patterns").

Source the checklist items from RESEARCH.md's "AI Slop Checklist" section AND from frontend-design-principles.md's "Anti-Patterns (AI-Slop Markers)" section. Combine and deduplicate. Organize by category:

- Typography Slop (Inter/Roboto/Arial as sole font, no display/heading font, default browser font stack)
- Color Slop (purple-to-blue gradient heroes, purple gradients on white cards, timid evenly-distributed palettes, cliched tech startup palettes)
- Layout Slop (predictable 3-column card grid with uniform rounded corners/shadows, generic hero + centered text + stock illustration, cookie-cutter component library defaults, identical patterns across industries)
- Content Slop (empty headlines: "Build the future"/"Your all-in-one platform"/"Scale without limits", lorem ipsum/coming soon, stock photo aesthetic)
- Motion Slop (same fade-in on every element, no purposeful motion, excessive decoration animations)
- Design Identity Slop (no aesthetic direction matching product domain, mismatched aesthetic: game maker looks like SaaS, creative tool looks corporate, design communicates nothing about brand/product/audience)

Frame as: "Check for these patterns during visual assessment. Each match is evidence supporting the 4-5 range for Visual Design. Multiple matches strongly suggest AI-slop. The absence of these patterns is necessary but not sufficient for scores above 5."

**REWRITE: ## Workflow**

Replace the current 9-step workflow with the 15-step workflow from CONTEXT.md. Preserve the existing content for steps that map to old steps (regression checking, starting the app, etc.) and add new steps. The step numbering and structure:

**Step 1: Understand the Spec** -- Keep existing content.

**Step 2: Check for Regressions (Rounds 2+ Only)** -- Keep existing content. Add: "Also compare current feature count to previous round. Feature count decrease = Critical regression (Generator removing features to game scores)."

**Step 3: Install Analysis Toolchain** (NEW) --
```bash
npm install --save-dev sharp imghash leven
```
The Evaluator installs these as devDependencies at the start of each evaluation. Tech-stack-agnostic: even if the app is Python/Rust, the Evaluator uses npm packages (Phase 2 workspace setup already does `npm init -y`). Allowed to install additional tools on-demand if evaluation surfaces a need.

**Step 4: Start the Application** -- Keep existing content (was step 3).

**Step 5: Scroll-and-Inspect All Pages** (NEW) --
Scroll every page in viewport-height chunks to trigger all lazy-loaded content (IntersectionObserver, loading="lazy", infinite scroll). Screenshots at each scroll position. 4 responsive breakpoints per page: 320px, 768px, 1280px, 1920px.

For each page:
1. Navigate to the page
2. Set viewport to each breakpoint width
3. Scroll from top to bottom in viewport-height increments
4. At each scroll position: take screenshot, capture network log, capture console output
5. Save screenshots to evaluation/round-N/screenshots/

This builds the evidence base for all subsequent assessment steps: visual evidence, network log for asset validation, console output for error monitoring. Must happen BEFORE feature testing (step 6) because lazy-loaded content must be triggered first.

Collect ALL network requests (especially failures: 4xx, 5xx, CORS blocks, timeouts). Collect ALL console output (console.error, uncaught exceptions, unhandled promise rejections). Save failures to evaluation/round-N/network.log.

**Step 6: Test Features with playwright-cli** -- Keep existing content from old step 4 (Test with playwright-cli). This is feature testing with user stories, interactions, edge cases, negative tests. The scroll-and-inspect step already triggered lazy content.

**Step 7: Asset Validation** (NEW) --
Comprehensive asset validation using the hybrid approach:

a. **URL Collection**: Union network log URLs (from step 5) with DOM-extracted URLs (data-src, hidden elements, srcset). Deduplicate.

b. **Per-image inspection** (every unique image, no cap):
   - curl -sI: Content-Type, Content-Length, status code. Detect soft-404s (Content-Type: text/html for image URLs).
   - sharp: dimensions, format, metadata, intrinsic vs display size.
   - sharp stats(): solid-fill detection (all channel stdev < 1.0), gradient-only detection (low entropy + moderate stdev), placeholder pattern detection (entropy < 2.0).
   - Claude visual inspection: navigate to image URL, screenshot, assess for watermarks, placeholders, quality, relevance to app context, AI generation artifacts, visual-context match (does image match its alt text? fit the page context? match spec's theme?).
   - Save downloaded images to evaluation/round-N/assets/. Save sharp metadata as analysis.json.

c. **Perceptual hashing**: Hash all images with imghash, compare distances with leven (threshold <= 12 for visually similar). Report clusters. Apply context judgment: same entity in different views (gallery grid + detail page) = legitimate reuse. Different entities sharing one image = flag as lazy duplication.

d. **Link checking**: Internal links via playwright-cli navigation (404 = Major). External links via curl (4xx/5xx = Minor). Multiple dead `#` links = Major (pattern of stub navigation). Anchor links checked for matching element ID.

e. **Font checking**: Network log for failed .woff2/.woff requests. Visual comparison for typography mismatch with spec's design language.

f. **Meta assets**: Check favicon, og:image, apple-touch-icon, manifest icons. Missing = Minor. Declared-but-broken = Major.

g. **Alt text**: Missing alt = Minor (Major if >50% missing). Generic alt ('image', 'photo', 'placeholder') = Minor.

**Severity rules:**
- CORS-blocked resource: always Major or Critical, never Minor (structural -- resource fundamentally inaccessible)
- External image URLs: lower the score (Generator should prefer self-hosted). External CDN URLs for fonts/CSS are fine.
- Severity escalation by app type: visual-heavy app (gallery, portfolio): ALL placeholders = Critical, >50% = Major, 1-2 = Minor. Utility app (dashboard, CLI tool): ALL = Major, >50% = Minor, 1-2 = Cosmetic.
- Dynamic assets (inline SVG, canvas, CSS art): legitimate if intentional. Flag only if broken, placeholder-patterned, or lazy substitute for spec-required imagery.

**Step 8: AI Feature Probing** (NEW) --
Read `${CLAUDE_PLUGIN_ROOT}/skills/application-dev/references/AI-PROBING-REFERENCE.md`.

For each AI feature claimed in SPEC.md:
1. Identify the feature's modality (text->text, text->image, etc.)
2. Apply the probe battery for that modality from AI-PROBING-REFERENCE.md
3. Generate all probe inputs on the fly from SPEC.md domain context (Goodhart's Law protection)
4. Record each probe result
5. Check Tier 2 technical signals (latency, network requests, browser API presence)
6. Render verdict: Real AI / Canned / Hybrid
7. If canned: apply Product Depth max 5 ceiling, flag as Major bug in Functionality
8. If real AI: assess quality (accuracy, relevance, coherence, helpfulness)
9. Check graceful degradation: does the app handle missing browser AI APIs?
10. Check latency UX: loading indicators, progress bars, streaming, frozen UI

The Evaluator is a Subject Matter Expert judge at this point -- it read SPEC.md in step 1 and knows the app's domain.

**Step 9: Test API Endpoints** -- Keep existing content (was step 5).

**Step 10: Review Code (Read-Only)** -- Keep existing content (was step 6). Add: check for AI slop patterns in the code (excessive framework defaults, copy-paste patterns, no customization of component libraries).

**Step 11: List ALL Findings** (NEW) --
Before scoring, list every finding from steps 2-10 in a structured list. Group by type:
- Bugs (with severity: Critical/Major/Minor)
- Asset issues (broken, placeholder, duplicate, CORS)
- AI probe results (per feature: Real/Canned/Hybrid)
- Console errors and exceptions
- Off-spec features
- Regressions (rounds 2+)
- Design observations (positive and negative)
- Code quality observations

Do NOT assign scores yet. The purpose is to separate the finding phase (less biased) from the scoring phase (more biased). This prevents the "I've already given it a 7 so I'll downplay this next bug" trap.

**Step 12: Read Calibration + Score** (NEW) --
Read `${CLAUDE_PLUGIN_ROOT}/skills/application-dev/references/SCORING-CALIBRATION.md`.

For each criterion:
1. Review the ceiling rules. Determine if any ceiling applies based on the findings from step 11.
2. If a ceiling applies, the score cannot exceed that ceiling regardless of other qualities.
3. Read the calibration scenarios for this criterion. Find the scenario closest to the current application state.
4. Assign the score, anchored to the calibration scenario and the rubric descriptors.
5. Write a mandatory justification citing specific findings: "Functionality: 5/10 -- 2 Critical bugs (#3, #7), 3 Major bugs, core search broken."

Keep the existing scoring rubric (Product Depth 1-10, Functionality 1-10, Visual Design 1-10, Code Quality 1-10) with existing threshold values and grade descriptors. The calibration and ceilings are ADDITIONAL constraints on top of the existing rubric. The cross-criterion propagation rule from the existing evaluator.md stays.

**Step 13: Write EVALUATION.md** -- Keep existing content (was step 8). The template now has new sections from Plan 01.

**Step 14: Self-Verification** (expanded from old step implicit in "Write EVALUATION.md") --
Extended 10-check checklist. See below.

**Step 15: Commit + Clean Up** -- Merge old steps 8.5 and 9. Commit evaluation/round-N/ to git, close playwright-cli, kill dev server.

**REWRITE: ## Self-Verification**

Replace the current 3-check self-verification with the 10-check extended checklist from CONTEXT.md:

1. **Verdict line present** -- `## Verdict: PASS` or `## Verdict: FAIL` as defined in template (existing)
2. **Scores table complete** -- all four criteria with scores, thresholds, and status (existing)
3. **Priority Fixes section present** -- with content if FAIL, with "No priority fixes" if PASS (existing)
4. **Each score respects ceiling rules** -- re-read SCORING-CALIBRATION.md ceiling rules, verify no score exceeds applicable ceiling (new)
5. **Score justifications reference actual findings** -- each score in Score Justifications table cites specific findings from this report (new)
6. **No score > 8 without explicit evidence of excellence** -- if any score is 9 or 10, verify the justification describes genuine surprise-level quality (new)
7. **Every SPEC.md feature in the feature status table** -- cross-reference SPEC.md features list against Product Depth Assessment table. Missing entries = incomplete report (watchdog, new)
8. **Verdict is FAIL if any Core feature Missing/Broken** -- check the feature status table for Core features with status Missing or Broken. If any exist, verdict must be FAIL (watchdog, new)
9. **Verdict is FAIL if >50% features Missing/Broken/Partial** -- count feature statuses. If more than half are Missing, Broken, or Partial, verdict must be FAIL (watchdog, new)
10. **Feature count >= previous round** (rounds 2+ only) -- count features in current feature status table. Must be >= previous round's count. Decrease = Critical regression from Generator removing features to game scores (watchdog, new)

"If any check fails, fix the report before completing. This is your inner quality gate -- do not hand off a report with gaps."

**ADD to ## Rules:**

Add rule 7:
7. **Off-spec features are penalized, not praised.** Features found in the app but NOT in SPEC.md are off-spec. They receive penalties in Product Depth (misallocated effort, GAN precision principle) and Code Quality (YAGNI violation). Bugs from off-spec features count normally in Functionality. A canned off-spec AI feature is Major severity (deceptive). Use the term "off-spec features" -- not "bonus features" or "scope creep." Feature count decrease between rounds from removing off-spec features is NOT a regression.

**IMPORTANT CONSTRAINTS:**
- The file is distributed to users (lives in plugins/application-dev/agents/). Keep it clean -- no research notes, no plan references, no scratch content.
- Follow the established pattern: behavioral guidance in evaluator.md, structural/mechanical guidance in reference files. Do NOT copy ceiling rules or calibration scenarios into evaluator.md -- reference the SCORING-CALIBRATION.md file instead.
- Workflow steps reference files using `${CLAUDE_PLUGIN_ROOT}/skills/application-dev/references/<file>.md` path pattern.
- Do NOT use emojis or non-ASCII characters anywhere in the file.
- Use the existing code block style (bash blocks for commands) consistent with the current file.
  </action>
  <verify>
    <automated>git grep -c "Step 1\|Step 2\|Step 3\|Step 4\|Step 5\|Step 6\|Step 7\|Step 8\|Step 9\|Step 10\|Step 11\|Step 12\|Step 13\|Step 14\|Step 15" plugins/application-dev/agents/evaluator.md</automated>
  </verify>
  <done>evaluator.md has: ELIZA effect warning in Critical Mindset, AI Slop Checklist section with 6 categories, 15-step workflow in correct order (Understand Spec, Check Regressions, Install Toolchain, Start App, Scroll-and-Inspect, Test Features, Asset Validation, AI Probing, Test API, Review Code, List Findings, Calibrate+Score, Write Report, Self-Verify, Commit+Clean), 10-check self-verification including 4 watchdog rules, off-spec features rule, all reference file paths use ${CLAUDE_PLUGIN_ROOT} pattern. Frontmatter and Rules section preserved. No structural/mechanical content duplicated from reference files.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete Evaluator hardening</name>
  <files>plugins/application-dev/agents/evaluator.md, plugins/application-dev/skills/application-dev/references/SCORING-CALIBRATION.md, plugins/application-dev/skills/application-dev/references/AI-PROBING-REFERENCE.md, plugins/application-dev/skills/application-dev/references/EVALUATION-TEMPLATE.md</files>
  <action>Human verifies the complete Evaluator hardening pipeline across all 4 modified/created files. This checkpoint covers both Plan 01 (reference files) and Plan 02 (evaluator.md rewrite) to verify they work together as a coherent system.</action>
  <verify>
    Human reviews:
    1. Read evaluator.md -- verify 15-step workflow makes sense end-to-end, ELIZA effect warning is present, AI slop checklist covers the key patterns, self-verification has 10 checks
    2. Read SCORING-CALIBRATION.md -- verify ceiling rules match CONTEXT.md exactly, calibration scenarios are concrete and useful anchors
    3. Read AI-PROBING-REFERENCE.md -- verify modality-based organization, all 10 Turing test concepts present, no fixed probe scripts (strategies only)
    4. Read EVALUATION-TEMPLATE.md -- verify 5 new sections present in correct order, existing regex-parsed sections unchanged
    5. Run: git diff plugins/application-dev/scripts/appdev-cli.mjs -- should show NO changes
    6. Verify evaluator.md references all three reference files via ${CLAUDE_PLUGIN_ROOT} path at the correct workflow steps (step 8, step 12, step 13)
    Type "approved" or describe issues.
  </verify>
  <done>Human has reviewed and approved the complete Evaluator hardening: evaluator.md 15-step workflow, SCORING-CALIBRATION.md, AI-PROBING-REFERENCE.md, and EVALUATION-TEMPLATE.md all work together correctly.</done>
</task>

</tasks>

<verification>
1. evaluator.md has 15-step workflow in correct order from CONTEXT.md
2. evaluator.md references SCORING-CALIBRATION.md at step 12, AI-PROBING-REFERENCE.md at step 8, EVALUATION-TEMPLATE.md at step 13
3. ELIZA effect warning names the phenomenon explicitly and references Weizenbaum
4. AI slop checklist has 6 categories matching RESEARCH.md + frontend-design-principles.md
5. Self-verification has exactly 10 checks including 4 watchdog rules
6. Off-spec features rule uses correct terminology ("off-spec features")
7. No ceiling rules or calibration scenarios duplicated in evaluator.md (they live in SCORING-CALIBRATION.md)
8. No fixed probe scripts in evaluator.md (they live as strategies in AI-PROBING-REFERENCE.md)
9. appdev-cli.mjs is NOT modified: git diff plugins/application-dev/scripts/appdev-cli.mjs shows nothing
10. All files are in plugins/application-dev/ (distributed to users, clean -- no research/plan artifacts)
</verification>

<success_criteria>
- Evaluator agent definition transforms from 9-step general critic to 15-step adversarial quality gate
- All 5 EVAL requirements are structurally addressed through workflow steps, reference file loading, and self-verification checks
- Behavioral guidance (mindset, skepticism, ELIZA warning) stays in evaluator.md
- Structural/mechanical guidance (ceilings, calibration, probe strategies) stays in reference files
- Feature watchdog prevents Generator from gaming scores by removing features
- Bug-first workflow (find then score) structurally decouples finding from scoring bias
- Human verification confirms the complete hardened evaluation pipeline
</success_criteria>

<output>
After completion, create `.planning/phases/03-evaluator-hardening/03-02-SUMMARY.md`
</output>
