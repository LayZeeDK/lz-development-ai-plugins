---
phase: 11-scoring-foundation-perturbation-critic
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/application-dev/skills/application-dev/references/evaluator/SCORING-CALIBRATION.md
  - plugins/application-dev/agents/perturbation-critic.md
autonomous: true
requirements: [CRITIC-01, CRITIC-03, CRITIC-04]

must_haves:
  truths:
    - "SCORING-CALIBRATION.md contains Robustness ceiling rules with 5 conditions (crash=max4, unrecoverable=max5, no-error-handling=max5, 3+-exceptions=max6, console-warnings=max7)"
    - "SCORING-CALIBRATION.md contains Robustness calibration scenarios at 5/10 (below), 7/10 (at), 9/10 (above) with boundary explanations"
    - "perturbation-critic agent exists with YAML frontmatter, Information Barrier, Write Restriction, Step 0, and methodology sections"
    - "perturbation-critic methodology covers input perturbation, console monitoring under stress, rapid navigation, viewport extremes, and error recovery"
    - "perturbation-critic has explicit boundary rule distinguishing it from perceptual-critic and projection-critic"
    - "perturbation-critic writes summary.json with dimension 'Robustness' and findings prefixed RB-"
    - "No Averaging or Trading section in SCORING-CALIBRATION.md lists Robustness threshold 6"
  artifacts:
    - path: "plugins/application-dev/skills/application-dev/references/evaluator/SCORING-CALIBRATION.md"
      provides: "Robustness ceiling rules and calibration scenarios"
      contains: "Robustness"
    - path: "plugins/application-dev/agents/perturbation-critic.md"
      provides: "Adversarial testing agent definition"
      contains: "perturbation-critic"
  key_links:
    - from: "perturbation-critic.md SCORE section"
      to: "SCORING-CALIBRATION.md Robustness section"
      via: "Read reference instruction in SCORE step"
      pattern: "SCORING-CALIBRATION"
    - from: "perturbation-critic.md REPORT section"
      to: "summary.json universal schema"
      via: "dimension field must match DIMENSIONS constant name"
      pattern: "\"dimension\": \"Robustness\""
    - from: "perturbation-critic.md boundary rule"
      to: "perceptual-critic and projection-critic methodology"
      via: "within-spec vs beyond-spec condition boundary"
      pattern: "beyond.*spec"
---

<objective>
Create the Robustness scoring calibration and the perturbation-critic agent definition. The calibration anchors scoring before the agent is used. The agent definition establishes adversarial testing methodology with clear boundaries that prevent overlap with existing critics.

Purpose: Calibration-first prevents threshold anchoring (scores clustering at 7). The perturbation-critic is the R-FID analog of the ensemble -- it evaluates quality STABILITY under adversarial conditions, not quality itself. Clear methodology boundaries (within-spec = perceptual/projection, beyond-spec = perturbation) prevent duplicate findings.

Output: SCORING-CALIBRATION.md with Robustness section, perturbation-critic.md agent definition.
</objective>

<execution_context>
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-scoring-foundation-perturbation-critic/11-CONTEXT.md
@.planning/phases/11-scoring-foundation-perturbation-critic/11-RESEARCH.md

<interfaces>
<!-- Existing agent and calibration patterns the executor must follow. -->

From plugins/application-dev/agents/projection-critic.md (YAML frontmatter pattern):
```yaml
---
name: projection-critic
description: |
  Use this agent to evaluate a running application's functional coverage via acceptance tests.
  Spawned by the application-dev orchestrator during evaluation phase.
  Should not be triggered directly by users.
  <example>...</example>
model: inherit
color: yellow
tools: ["Read", "Write", "Bash(npx playwright-cli *)", "Bash(node *appdev-cli* install-dep *)", "Bash(npx playwright test *)", "Bash(node *appdev-cli* static-serve*)"]
---
```

From plugins/application-dev/agents/projection-critic.md (section structure):
```
- Hard Boundary (Information Barrier)
- Write Restriction
- Step 0: Start Evaluation Server
- Methodology: UNDERSTAND, TEST, PROBE, SCORE, REPORT
- Finding Format
- Token Efficiency
```

From plugins/application-dev/skills/application-dev/references/evaluator/SCORING-CALIBRATION.md (calibration pattern):
```markdown
### [Dimension Name]

| Condition | Ceiling |
|-----------|---------|
| ... | max N |

## Calibration Scenarios
### [Dimension Name]
**Below Threshold: N/10**
[Scenario text]
Score: N/10 -- [rationale]
Not N+1 because: [boundary explanation]

**At Threshold: N/10**
...

**Above Threshold: N/10**
...
```

Summary.json universal schema (all critics must follow):
```json
{
  "critic": "<critic-name>",
  "dimension": "<exact DIMENSIONS name>",
  "score": 7,
  "threshold": 6,
  "pass": true,
  "findings": [{ "id": "RB-1", "severity": "Major", "title": "...", "description": "...", "affects_dimensions": ["Robustness"] }],
  "ceiling_applied": null,
  "justification": "Score justification citing findings by ID",
  "off_spec_features": []
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Robustness ceiling rules and calibration scenarios to SCORING-CALIBRATION.md</name>
  <files>plugins/application-dev/skills/application-dev/references/evaluator/SCORING-CALIBRATION.md</files>
  <action>
    Update SCORING-CALIBRATION.md with the Robustness scoring section. Read the file first, then apply these additions:

    **1. Update "Loaded by" header comment (line 3):**
    Change to: `**Loaded by:** perceptual-critic (Visual Design ceilings), projection-critic (Functionality and Product Depth ceilings), perturbation-critic (Robustness ceilings), CLI compile-evaluation (all ceilings for cross-validation).`

    **2. Add Robustness ceiling rules section after the Visual Design section (after line 38, before Browser AI Degradation):**

    ```markdown
    ### Robustness

    | Condition | Ceiling |
    |-----------|---------|
    | App crash or freeze under perturbation | max 4 |
    | Unrecoverable state (requires reload) | max 5 |
    | No error handling for invalid inputs | max 5 |
    | 3+ uncaught exceptions under stress | max 6 |
    | Console warnings that don't affect behavior | max 7 |
    ```

    Theoretical basis for executor context (do NOT include in file -- this is locked decision rationale): Wiener's damping ratio (crash = undamped divergence zeta=0, unrecoverable = no return to equilibrium, exceptions = underdamped oscillation visible in spectral domain). CISQ SRI sigma levels (ISO/IEC 5055:2021) for severity mapping.

    **3. Update "No Averaging or Trading" section (line 73-76):**
    Add Robustness threshold to the list:
    ```
    - Product Depth: 7
    - Functionality: 7
    - Visual Design: 6
    - Robustness: 6
    ```

    **4. Add Robustness calibration scenarios at the end of the Calibration Scenarios section (after the Visual Design Above Threshold scenario, before the end of file):**

    Use the EXACT calibration scenario text from 11-CONTEXT.md locked decisions. These are user-locked and must not be paraphrased:

    **Below Threshold: 5/10** -- The recipe app scenario with crash on 500-char input, duplicate favorites, blank screen on back-during-load, 3+ uncaught exceptions. "Undamped + insufficient variety." Boundary: "Not 6 because: A 6 requires the app to at least survive perturbation without crashing. Here the system diverges -- it cannot return to equilibrium without a full reload, and multiple crash paths exist."

    **At Threshold: 7/10** -- The recipe app handles long inputs with truncation, rapid clicking debounced, loading indicator instead of blank screen, clean console under normal use (1-2 warnings under rapid stress), error messages for invalid forms, graceful degradation for missing browser APIs. "Critically damped + adequate variety." Boundary: "Not 8 because: An 8 requires the console to stay fully clean under stress and extreme viewports to be handled gracefully. At 7, the app handles common disturbances but has gaps at the extremes."

    **Above Threshold: 9/10** -- All inputs validated with informative errors, rapid interactions debounced/throttled, extreme viewports usable, console clean under stress, offline state handled, missing APIs transparent, zero uncaught exceptions. "Overdamped + full variety." Boundary: "Not 10 because: A 10 means zero degradation found during exhaustive adversarial testing, including novel disturbance types not explicitly tested. The 9 has complete coverage of known disturbance classes but has not been tested against every conceivable perturbation."

    Copy the full scenario descriptions from 11-CONTEXT.md `### Robustness calibration anchors` section. Do NOT summarize or paraphrase -- these are locked decisions.
  </action>
  <verify>
    <automated>git grep -c "Robustness" -- "plugins/application-dev/skills/application-dev/references/evaluator/SCORING-CALIBRATION.md"</automated>
  </verify>
  <done>SCORING-CALIBRATION.md contains: Robustness ceiling rules table (5 conditions), perturbation-critic in Loaded-by header, Robustness: 6 in No Averaging section, and 3 calibration scenarios (5/10, 7/10, 9/10) with boundary explanations copied verbatim from CONTEXT.md.</done>
</task>

<task type="auto">
  <name>Task 2: Create perturbation-critic agent definition with methodology boundaries</name>
  <files>plugins/application-dev/agents/perturbation-critic.md</files>
  <action>
    Create the perturbation-critic agent definition following the established agent definition structure from projection-critic.md and perceptual-critic.md. This is a NEW file.

    **YAML Frontmatter:**
    ```yaml
    ---
    name: perturbation-critic
    description: |
      Use this agent to evaluate a running application's resilience through adversarial testing.
      Spawned by the application-dev orchestrator during evaluation phase.
      Should not be triggered directly by users.

      <example>
      Context: The application-dev orchestrator needs resilience evaluation after a generation round
      user: "This is evaluation round 1."
      assistant: "I'll evaluate the application's resilience by running adversarial tests against the product surface."
      <commentary>
      Orchestrator spawns perturbation-critic to score Robustness dimension.
      </commentary>
      </example>
    model: inherit
    color: yellow
    tools: ["Read", "Write", "Bash(npx playwright-cli *)", "Bash(node *appdev-cli* install-dep *)", "Bash(npx playwright test *)", "Bash(node *appdev-cli* static-serve*)"]
    ---
    ```

    Tool allowlist rationale: Same as projection-critic. Needs `Bash(npx playwright test *)` for write-and-run adversarial tests. Does NOT need `Bash(node *appdev-cli* check-assets *)` (that is perceptual-critic only).

    **Role Statement:**
    You are a perturbation discriminator evaluating application resilience through adversarial testing. You score the **Robustness** dimension. You are the R-FID analog of the ensemble -- you evaluate quality STABILITY under adversarial conditions, not quality itself.

    **Information Barrier:**
    Same pattern as existing critics. MUST NOT read source code files. Product-surface only. Include WHY rationale.

    **Write Restriction:**
    Write ONLY to `evaluation/round-N/perturbation/`. Same pattern as other critics.

    **Step 0: Start Evaluation Server:**
    Same pattern as projection-critic. Read `.appdev-state.json` for build_dir, run static-serve, get port, handle SPA flag.

    **Methodology Boundary Rule (CRITIC-04 -- locked decision):**
    Include the explicit boundary rule from CONTEXT.md:

    "If the condition is within the spec's stated parameters (breakpoints, valid inputs, documented workflows), it belongs to perceptual or projection. If it pushes beyond what the spec anticipates, it is perturbation. The perturbation-critic is the chaos engineering layer of the ensemble."

    Include the condition-to-owner table:

    | Condition | Owner | Rationale |
    |-----------|-------|-----------|
    | Standard breakpoints (360px-1920px) | Perceptual | Within spec -- responsive DESIGN |
    | Below 320px, above 2560px, rapid resize | Perturbation | Beyond spec -- chaos/fault injection |
    | Form submission with valid data | Projection | Within spec -- feature CORRECTNESS |
    | Empty/XSS/extreme-length inputs | Perturbation | Beyond spec -- input fault injection |
    | Page navigation | Projection | Within spec -- workflow correctness |
    | Rapid back-forward-reload sequences | Perturbation | Beyond spec -- timing fault injection |

    **Methodology sections (UNDERSTAND, PERTURB, MONITOR, SCORE, REPORT):**

    UNDERSTAND: Read SPEC.md to identify perturbation targets (forms, navigation, AI features, data-dependent pages). Build a target map of inputs, endpoints, and state transitions that can be stressed. This is attack surface discovery, not feature inventory.

    PERTURB: Read the Playwright evaluation reference for write-and-run and console filtering patterns:
    `Read ${CLAUDE_PLUGIN_ROOT}/skills/application-dev/references/evaluator/PLAYWRIGHT-EVALUATION.md`

    Use the write-and-run pattern for token efficiency. Write adversarial test files to `evaluation/round-N/perturbation/adversarial-tests.spec.ts`, run with `npx playwright test`, read structured JSON results.

    Adversarial test categories in priority order (per CONTEXT.md locked decisions):

    **Must-have (core ~60K token budget):**
    1. Input perturbation -- boundary/extreme values on every form field (empty, whitespace-only, 1000+ characters, XSS payloads like `<script>alert(1)</script>`, SQL injection strings, special characters, negative numbers, zero, very large numbers). Test every input the SPEC mentions.
    2. Console monitoring under stress -- run `npx playwright-cli console error` DURING all other perturbation categories (concurrent monitoring, not a separate step). This is the spectral/R-FID analog: catches invisible fragility that surface-level testing misses.
    3. Rapid navigation -- back/forward/reload sequences during page transitions, rapid clicking of navigation links, navigating away during async operations (form submission, data loading).

    **Important (remaining budget):**
    4. Viewport extremes -- resize to 320px width, 4K width (2560px+), rapid resize during page load or animation.
    5. Error recovery -- disable/mock browser APIs (e.g., LanguageModel API unavailable), test offline indicators, missing assets, 404 responses.

    **Stretch (only if token budget remains):**
    6. JavaScript disabled -- check for meaningful progressive enhancement (noscript content, server-rendered fallbacks).

    Round 2+ Test Reuse: Same pattern as projection-critic. Reuse adversarial tests from prior round, heal selector timeouts, regenerate if >50% timeout.

    MONITOR: Console error monitoring runs DURING all PERTURB activities. Record: (1) uncaught exceptions under each perturbation category, (2) unhandled promise rejections, (3) network errors, (4) warning count under stress vs normal usage. The console is the "spectral domain" -- surface behavior may look stable while internal oscillation (exceptions) reveals fragility.

    SCORE: Load the scoring calibration reference:
    `Read ${CLAUDE_PLUGIN_ROOT}/skills/application-dev/references/evaluator/SCORING-CALIBRATION.md`
    Read the **Robustness** ceiling rules and calibration scenarios. Apply ceilings mechanically. Score Robustness as quality CHANGE under adversarial conditions -- a form that works with valid data tells you nothing about Robustness; what matters is what happens when inputs are invalid, extreme, or adversarial.

    REPORT: Write `evaluation/round-N/perturbation/summary.json` with the universal schema:
    ```json
    {
      "critic": "perturbation",
      "dimension": "Robustness",
      "score": 7,
      "threshold": 6,
      "pass": true,
      "findings": [
        {
          "id": "RB-1",
          "severity": "Major",
          "title": "Short description",
          "description": "Behavioral symptom observed under adversarial condition",
          "affects_dimensions": ["Robustness"]
        }
      ],
      "ceiling_applied": null,
      "justification": "Score justification citing findings by ID",
      "off_spec_features": []
    }
    ```
    Note: `"dimension": "Robustness"` must EXACTLY match the DIMENSIONS constant entry name. This is the join key for compile-evaluation's justification lookup.

    **Finding Format:**
    Same principle as other critics: describe behavioral symptoms from the product surface. Include the adversarial condition that triggered the finding. Do not diagnose code-level causes.
    - Good: "Submitting the search form with a 1000-character input freezes the UI for 8 seconds and produces an uncaught TypeError in the console"
    - Bad: "The search handler does not debounce input and runs an O(n^2) filter on each keystroke"

    **Token Efficiency:**
    Context budget is approximately 60K tokens. The write-and-run pattern keeps adversarial tests outside context. Set `CLAUDE_AUTOCOMPACT_PCT_OVERRIDE=50` if available.
  </action>
  <verify>
    <automated>git grep -c "perturbation-critic\|Robustness\|adversarial" -- "plugins/application-dev/agents/perturbation-critic.md"</automated>
  </verify>
  <done>perturbation-critic.md exists with: YAML frontmatter (name, description, model, color, tools), Information Barrier, Write Restriction, Step 0, methodology boundary rule with condition-to-owner table, 5 methodology sections (UNDERSTAND, PERTURB, MONITOR, SCORE, REPORT), adversarial test priorities (must-have/important/stretch), Finding Format, Token Efficiency. The dimension field in summary.json is "Robustness" matching DIMENSIONS constant.</done>
</task>

</tasks>

<verification>
1. `git grep "perturbation-critic" -- "plugins/application-dev/agents/perturbation-critic.md"` -- file exists and contains agent name
2. `git grep "Robustness" -- "plugins/application-dev/skills/application-dev/references/evaluator/SCORING-CALIBRATION.md"` -- ceiling rules and calibration scenarios present
3. `git grep "beyond.*spec\|within.*spec" -- "plugins/application-dev/agents/perturbation-critic.md"` -- methodology boundary rule present
4. `git grep "dimension.*Robustness" -- "plugins/application-dev/agents/perturbation-critic.md"` -- correct dimension name in summary.json
5. Visual inspection: perturbation-critic.md follows same structure as projection-critic.md (YAML frontmatter, Barrier, Restriction, Step 0, Methodology, Format, Efficiency)
</verification>

<success_criteria>
- SCORING-CALIBRATION.md has Robustness ceiling rules (5 conditions) and 3 calibration scenarios with boundary explanations
- perturbation-critic.md follows established agent definition pattern with correct YAML frontmatter and tool allowlist
- Methodology boundaries clearly distinguish perturbation (beyond-spec) from perceptual/projection (within-spec)
- Adversarial test priorities match CONTEXT.md locked decisions (input perturbation, console monitoring, rapid navigation as must-have)
- Console monitoring runs DURING other perturbation activities (concurrent, not sequential)
- Summary.json dimension field is "Robustness" (exact match with DIMENSIONS constant)
</success_criteria>

<output>
After completion, create `.planning/phases/11-scoring-foundation-perturbation-critic/11-02-SUMMARY.md`
</output>
