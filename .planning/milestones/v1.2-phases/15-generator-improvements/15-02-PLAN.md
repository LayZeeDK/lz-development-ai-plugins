---
phase: 15-generator-improvements
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - plugins/application-dev/skills/vite-plus/SKILL.md
  - plugins/application-dev/agents/generator.md
autonomous: true
requirements: [GEN-02, GEN-03, GEN-04]
user_setup: []

must_haves:
  truths:
    - "Generator uses Vite+ as the DEFAULT toolchain for compatible frameworks, not just a preference"
    - "Generator must explicitly justify choosing plain Vite over Vite+ for a compatible framework"
    - "Generator includes a dependency freshness step in Round 1 Project Setup that upgrades all deps to latest compatible versions"
    - "Generator Step 4 references the browser-built-in-ai decision tree, not three-equal-options framing"
    - "Generator Step 8 diagnostic battery leads with vp commands, plain Vite equivalents in parentheses"
    - "Vite+ skill reflects v0.1.15 with breaking changes (VP_* env vars, vp run flag order, new install URLs)"
  artifacts:
    - path: "plugins/application-dev/skills/vite-plus/SKILL.md"
      provides: "Refreshed Vite+ skill for v0.1.15 with alpha caveat, breaking changes, full CLI inventory"
      contains: "v0.1.15"
    - path: "plugins/application-dev/agents/generator.md"
      provides: "Updated generator with browser-built-in-ai skill, dependency freshness, Vite+ default, vp-first diagnostics"
      contains: "browser-built-in-ai"
  key_links:
    - from: "plugins/application-dev/agents/generator.md"
      to: "plugins/application-dev/skills/browser-built-in-ai/SKILL.md"
      via: "frontmatter skills list and Step 4 Read instruction"
      pattern: "browser-built-in-ai"
    - from: "plugins/application-dev/agents/generator.md"
      to: "plugins/application-dev/skills/vite-plus/SKILL.md"
      via: "Vite+ preference paragraph and Step 8 diagnostic commands"
      pattern: "vite-plus/SKILL.md"
---

<objective>
Refresh the Vite+ skill to v0.1.15 and update generator.md with dependency freshness, Vite+ default status, browser-built-in-ai references, and vp-first diagnostics.

Purpose: GEN-02 requires the Vite+ skill to reflect the current v0.1.15 CLI (breaking changes in env vars, argument order, install URLs). GEN-03 requires a dependency freshness instruction in generator.md Step 1. GEN-04 requires Vite+ to be the default toolchain (not a preference) with an escape hatch. generator.md Step 4 must reference the new browser-built-in-ai meta-skill (created in Plan 01) instead of browser-prompt-api.

Output: Updated vite-plus/SKILL.md and generator.md with all GEN-02/03/04 requirements addressed.
</objective>

<execution_context>
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-generator-improvements/15-CONTEXT.md
@.planning/phases/15-generator-improvements/15-RESEARCH.md
@.planning/phases/15-generator-improvements/15-01-SUMMARY.md

<interfaces>
<!-- Current generator.md structure that will be modified. Key sections: -->

From plugins/application-dev/agents/generator.md:
```
frontmatter: skills: [browser-prompt-api, browser-webllm, browser-webnn, playwright-testing, vitest-browser, vite-plus]
line 57-59: "Vite+ preference" paragraph (currently "prefer Vite+ over plain Vite when compatible")
line 69-100: Phase 1 (Project Setup) -- no dependency freshness instruction currently
line 114-128: Step 4 (AI features) -- currently lists Prompt API, WebLLM, WebNN as three equal choices
line 156-163: Step 8 diagnostic battery -- currently lists npm/npx commands first, vp in comments
```

From plugins/application-dev/skills/vite-plus/SKILL.md:
```
line 1-12: frontmatter with name and description
line 14-20: header and links (old URLs: vite.plus)
line 22: "Oxlint 1.52" (outdated, now 1.58.0)
line 51-56: installation (old URLs: curl -fsSL https://vite.plus | bash)
line 70-131: CLI commands (missing: vpr, vp staged, vp config, vp env, vp add/remove/update, etc.)
line 220-229: bundled tool versions (outdated versions)
```

From Plan 01 output (browser-built-in-ai skill):
```
New skill path: plugins/application-dev/skills/browser-built-in-ai/SKILL.md
Reference files in: plugins/application-dev/skills/browser-built-in-ai/references/
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refresh vite-plus SKILL.md to v0.1.15</name>
  <files>plugins/application-dev/skills/vite-plus/SKILL.md</files>
  <action>
Rewrite vite-plus/SKILL.md to reflect v0.1.15. This is a full rewrite of the existing skill, not a patch. Use the detailed findings from 15-RESEARCH.md.

**Prominent alpha caveat at top** (after frontmatter, before section 1):
> Vite+ is alpha software (tested: v0.1.15). When vp commands fail unexpectedly, research the latest vite-plus docs or fall back to known working vp commands from a previous version. Do NOT fall back to plain Vite -- Vite+ is the default toolchain and the Generator should exhaust vp troubleshooting before considering alternatives.

**Breaking changes section** (NEW -- add before or after installation):
1. Environment variable prefix renamed: `VITE_PLUS_*` -> `VP_*` (v0.1.15)
2. `vp run` argument order changed: flags before task name. `vp run -r build` (correct) vs `vp run build -r` (broken) (v0.1.15)
3. Installation URL changed to viteplus.dev (old vite.plus still redirects)
4. TypeScript peer range: `^6.0.0`
5. Bun package manager support: first-class in `vp create` and install

**Updated installation URLs:**
- macOS/Linux: `curl -fsSL https://viteplus.dev/install.sh | bash`
- Windows: `irm https://viteplus.dev/install.ps1 | iex`
- Env vars: `VP_VERSION` (version to install), `VP_HOME` (install dir, default: `~/.vite-plus`)

**Full CLI command inventory** (from RESEARCH.md):
- Start phase: vp create, vp migrate, vp config, vp staged, vp install, vp env
- Development: vp dev, vp check, vp check --fix, vp lint, vp fmt, vp test
- Execution: vp run, vpr (shorthand), vp run --parallel, vp run --concurrency-limit, vp cache, vpx, vp exec, vp dlx
- Build: vp build, vp pack, vp preview
- Dependency management: vp add, vp remove, vp update, vp dedupe, vp outdated, vp why, vp info, vp pm
- Maintenance: vp upgrade, vp implode

**Updated bundled tool versions:**
| Tool | Version | Replaces |
| Vite | 8.0.3 | Vite (standalone) |
| Vitest | 4.1.2 | Vitest (standalone) |
| Oxlint | 1.58.0 | ESLint |
| Oxfmt | 0.43.0 | Prettier |
| tsgo | experimental | tsc |
| Rolldown | 1.0.0-rc.12 | Rollup / esbuild |
| tsdown | 0.21.7 | tsup |

**Updated framework compatibility table** (from RESEARCH.md):
- React, Vue, Svelte, Solid, react-router: fully supported
- Angular: not supported (tsgo incompatible)
- Nuxt, TanStack Start: experimental

**Unified vite.config.ts** section: include lint, test, and run.tasks configuration example

**Keep existing sections that are still accurate:** When to use, Angular incompatibility, Vue/Svelte template linting limitation, CI integration, Decision table (Vite+ vs plain Vite)

**Update the "When to use" section** to reflect Vite+ as DEFAULT (per GEN-04):
- Change "Prefer Vite+ for greenfield web projects" to "Vite+ is the default toolchain for greenfield web projects"
- Generator must explicitly justify choosing plain Vite over Vite+ for a compatible framework
- Escape hatch preserved: Angular (tsgo incompatible), Nuxt (incomplete), TanStack Start (incomplete), user prompt explicitly requests another bundler

**Known limitations** update: add "breaking changes between alpha releases" caveat, update version numbers

**Docs link update:** `https://viteplus.dev/guide/` (primary), keep announcement link
  </action>
  <verify>
    <automated>bash -c "git grep -c 'v0.1.15' plugins/application-dev/skills/vite-plus/SKILL.md && git grep -c 'viteplus.dev' plugins/application-dev/skills/vite-plus/SKILL.md && git grep -c 'VP_' plugins/application-dev/skills/vite-plus/SKILL.md && echo 'Key v0.1.15 markers present'"</automated>
  </verify>
  <done>
    - vite-plus/SKILL.md reflects v0.1.15 with all breaking changes documented
    - Installation URLs use viteplus.dev (not old vite.plus)
    - Environment variable prefix is VP_* (not VITE_PLUS_*)
    - Bundled tool versions are current (Oxlint 1.58.0, Vitest 4.1.2, etc.)
    - Alpha caveat is prominent at top of skill
    - Full CLI command inventory includes new commands (vpr, vp staged, vp config, vp env, vp add/remove/update, etc.)
    - Vite+ framed as default (not preference) with escape hatch
  </done>
</task>

<task type="auto">
  <name>Task 2: Update generator.md (frontmatter, Step 1, Step 4, Step 8, Vite+ paragraph)</name>
  <files>plugins/application-dev/agents/generator.md</files>
  <action>
Make targeted edits to generator.md for GEN-01 (skill reference), GEN-03 (dependency freshness), and GEN-04 (Vite+ default + diagnostics). Each edit below is a specific section change.

**Edit 1 -- Frontmatter skills list:**
Change `browser-prompt-api` to `browser-built-in-ai` in the `skills:` array:
```
skills: [browser-built-in-ai, browser-webllm, browser-webnn, playwright-testing, vitest-browser, vite-plus]
```

**Edit 2 -- Vite+ preference paragraph (around line 57-59):**
Strengthen from preference to default. Replace the current "Vite+ preference" paragraph with:

"**Vite+ default:** For greenfield web projects, Vite+ is the default toolchain when compatible with the chosen framework. Vite+ bundles Vite 8, Vitest 4.1, Oxlint, Oxfmt, and tsgo into a single `vp` CLI -- replacing separate ESLint + Prettier + tsc + Vite + Vitest setups. Read `${CLAUDE_PLUGIN_ROOT}/skills/vite-plus/SKILL.md` for vp CLI commands, framework compatibility, and known limitations. If choosing plain Vite over Vite+ for a compatible framework (React, Vue, Svelte, Solid, react-router), explicitly justify the choice."

Update the next paragraph to match:
"Vite+ is compatible with React, Vue, Svelte, Solid, and react-router. For incompatible frameworks (Angular -- tsgo does not support Angular compiler plugins; Nuxt or TanStack Start -- incomplete integration) or when the user prompt explicitly requests another bundler, fall back to plain Vite with separate lint/format/typecheck tooling."

**Edit 3 -- Step 1 (Project Setup), after "Install dependencies" and before "Configure dev server":**
Add a dependency freshness instruction block:

"**Dependency freshness (Round 1 only):** After scaffolding (vp create or framework CLI), upgrade all dependencies to their latest compatible versions. This is a greenfield project -- there is no legacy code to break. If an upgrade causes breakage, fix forward: adapt code to new APIs using pre-trained knowledge. Only research latest docs if fix-forward based on existing knowledge fails. Non-SemVer exceptions to be aware of:
- **Playwright** -- calendar versioning; minors contain new browser versions and potential breaking changes
- **TypeScript** -- minors add new type checks that may break existing code
- **0.x packages** -- SemVer allows breaking changes in minor AND patch versions

Do NOT upgrade dependencies in Round 2+ (fix-only mode, cybernetics damping principle). Exception: the evaluation report explicitly flags a dependency bug."

**Edit 4 -- Step 4 (AI features), rewrite the decision tree:**
Replace the current three-equal-options framing (Prompt API / WebLLM / WebNN bullets) with the Built-in AI hierarchy. Keep the opening detection line and general principles. Replace the three skill bullets with:

"When AI features are present, use the browser's Built-in AI APIs as the primary approach. Read `${CLAUDE_PLUGIN_ROOT}/skills/browser-built-in-ai/SKILL.md` for the decision tree that routes to the correct API:

1. **Task-specific need** (summarize, write, rewrite, translate, detect language)? Use the matching Built-in AI API -- Summarizer, Writer, Rewriter, Translator, or LanguageDetector. These are purpose-built and more efficient than general prompting.
2. **General-purpose / agentic / tool-calling?** Use LanguageModel (Prompt API) -- supports native tool calling, structured output, streaming, multimodal input.
3. **Specific model selection** (Llama, Mistral, etc.)? Use WebLLM. Read `${CLAUDE_PLUGIN_ROOT}/skills/browser-webllm/SKILL.md`.
4. **Non-LLM inference** (vision, audio, embeddings)? Use WebNN. Read `${CLAUDE_PLUGIN_ROOT}/skills/browser-webnn/SKILL.md`.

All Built-in AI APIs (items 1-2) share an identical pattern: feature-detect the global, check availability, create a session. Code works in both Chrome (Gemini Nano) and Edge (Phi-4-mini) without changes. When Built-in AI APIs are unavailable, degrade gracefully -- hide or disable AI features. Do NOT fall back to WebLLM as a substitute (different API surface, different model, not drop-in)."

Keep the "General AI feature principles" list that follows the bullets.

**Edit 5 -- Step 8 (diagnostic battery), reorder to lead with vp commands:**
Replace the current Step 8 command list with vp-first ordering (plain Vite equivalents in parentheses):

"Run the full CI suite in sequence:
1. Build: `vp build` (or `npm run build` if using plain Vite)
2. Production build state recording (see below)
3. Static analysis: `vp check` (or `npx tsc --noEmit && npx eslint . && npx prettier --check .` if using plain Vite) -- `vp check` combines lint + format + typecheck in one pass
4. Unit tests: `vp test` (or `npx vitest run --project unit` if using plain Vite)
5. Browser tests: `npx vitest run --project browser` (same regardless of Vite+) -- if applicable
6. E2e tests: `npx playwright test` (same regardless of Vite+) -- if applicable"

Also update the Phase 1 quality tooling section (around lines 79-84) to list vp commands first in each bullet, matching this pattern.
  </action>
  <verify>
    <automated>bash -c "git grep -c 'browser-built-in-ai' plugins/application-dev/agents/generator.md && git grep 'Dependency freshness' plugins/application-dev/agents/generator.md | head -1 && git grep 'Vite+ default' plugins/application-dev/agents/generator.md | head -1 && git grep 'vp build' plugins/application-dev/agents/generator.md | head -1 && echo 'All 4 generator changes present'"</automated>
  </verify>
  <done>
    - generator.md frontmatter skills list references browser-built-in-ai (not browser-prompt-api)
    - Step 1 contains dependency freshness instruction with non-SemVer exceptions and Round 2+ prohibition
    - Step 4 uses Built-in AI hierarchy (not three-equal-options framing)
    - Step 4 includes "Do NOT fall back to WebLLM" warning
    - Step 8 diagnostic commands lead with vp commands, plain Vite in parentheses
    - Vite+ paragraph says "default" (not "preference") with explicit justification requirement
    - No references to browser-prompt-api remain in generator.md
  </done>
</task>

</tasks>

<verification>
1. vite-plus/SKILL.md contains v0.1.15 version, viteplus.dev URLs, VP_* env vars, updated tool versions
2. generator.md frontmatter lists browser-built-in-ai (not browser-prompt-api)
3. generator.md Step 1 has dependency freshness instruction with non-SemVer exceptions
4. generator.md Step 4 has Built-in AI hierarchy decision tree (not three-equal-options)
5. generator.md Step 8 has vp-first diagnostic commands
6. generator.md Vite+ paragraph says "default" with justification requirement
7. No references to browser-prompt-api remain in generator.md
8. No references to old Vite+ URLs (vite.plus) or old env vars (VITE_PLUS_*) remain
</verification>

<success_criteria>
- vite-plus/SKILL.md fully reflects v0.1.15 with breaking changes, new CLI commands, updated versions
- generator.md has all 5 targeted edits applied (frontmatter, Vite+ default, dependency freshness, AI hierarchy, vp-first diagnostics)
- All GEN-02, GEN-03, GEN-04 requirements are addressed
- generator.md Step 4 references browser-built-in-ai skill path (wired to Plan 01 output)
</success_criteria>

<output>
After completion, create `.planning/phases/15-generator-improvements/15-02-SUMMARY.md`
</output>
