---
phase: 15-generator-improvements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/application-dev/skills/browser-built-in-ai/SKILL.md
  - plugins/application-dev/skills/browser-built-in-ai/references/prompt-api.md
  - plugins/application-dev/skills/browser-built-in-ai/references/summarizer-api.md
  - plugins/application-dev/skills/browser-built-in-ai/references/writer-rewriter-api.md
  - plugins/application-dev/skills/browser-built-in-ai/references/translator-api.md
  - plugins/application-dev/skills/browser-built-in-ai/references/graceful-degradation.md
autonomous: true
requirements: [GEN-01]
user_setup: []

must_haves:
  truths:
    - "Generator can determine which Built-in AI API to use for any given task via the SKILL.md decision tree"
    - "Generator knows which APIs are available in Chrome vs Edge via the comparison table"
    - "Generator can load the correct reference file for any specific API on demand"
    - "Graceful degradation pattern is generalized to work with all 7 Built-in AI APIs, not just LanguageModel"
  artifacts:
    - path: "plugins/application-dev/skills/browser-built-in-ai/SKILL.md"
      provides: "Routing SKILL.md with decision tree, Chrome vs Edge table, shared patterns, Read instructions"
      contains: "decision tree"
    - path: "plugins/application-dev/skills/browser-built-in-ai/references/prompt-api.md"
      provides: "LanguageModel reference: general-purpose, agentic, tool calling, structured output, multimodal"
    - path: "plugins/application-dev/skills/browser-built-in-ai/references/summarizer-api.md"
      provides: "Summarizer API reference: configuration options, usage patterns"
    - path: "plugins/application-dev/skills/browser-built-in-ai/references/writer-rewriter-api.md"
      provides: "Writer and Rewriter API reference: combined since they share W3C spec"
    - path: "plugins/application-dev/skills/browser-built-in-ai/references/translator-api.md"
      provides: "Translator and LanguageDetector API reference: combined per MDN pairing"
    - path: "plugins/application-dev/skills/browser-built-in-ai/references/graceful-degradation.md"
      provides: "Generalized degradation pattern for any Built-in AI API"
  key_links:
    - from: "plugins/application-dev/skills/browser-built-in-ai/SKILL.md"
      to: "references/prompt-api.md"
      via: "Read instruction in decision tree"
      pattern: "references/prompt-api.md"
    - from: "plugins/application-dev/skills/browser-built-in-ai/SKILL.md"
      to: "references/graceful-degradation.md"
      via: "Read instruction for degradation pattern"
      pattern: "references/graceful-degradation.md"
---

<objective>
Create the browser-built-in-ai meta-skill that replaces the browser-prompt-api skill with a full 7-API hierarchy.

Purpose: GEN-01 requires browser-agnostic AI guidance referencing both Chrome (Gemini Nano) and Edge (Phi-4-mini) with graceful degradation. The current browser-prompt-api skill only covers LanguageModel and frames three AI options (Prompt API, WebLLM, WebNN) as equal choices. The new meta-skill implements a clear hierarchy: task-specific Built-in AI APIs first, then LanguageModel, then WebLLM, then WebNN.

Output: New browser-built-in-ai skill directory with routing SKILL.md (~120 lines) and 5 reference files, old browser-prompt-api directory removed.
</objective>

<execution_context>
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-generator-improvements/15-CONTEXT.md
@.planning/phases/15-generator-improvements/15-RESEARCH.md

<interfaces>
<!-- Existing skill patterns to follow. Extracted from codebase. -->

From plugins/application-dev/skills/playwright-testing/SKILL.md (meta-skill pattern):
- Lean routing SKILL.md with frontmatter (name, description)
- references/ directory with on-demand reference files
- Read instructions pointing to specific reference files per use case
- SKILL.md ~50-80 lines of routing logic, references ~100-200 lines each

From plugins/application-dev/skills/browser-prompt-api/SKILL.md (content to migrate):
- 350 lines covering LanguageModel only
- Sections: Purpose, Browser support, Feature detection, Session creation, Prompting, Structured output, Tool use, Context management, Session lifecycle, Multimodal, Permissions, Graceful degradation, Best practices
- references/graceful-degradation.md (53 lines): LanguageModel-specific degradation pattern
- examples/tool-use.js: tool calling example code

Key constraint: SKILL.md frontmatter `description` field triggers skill loading.
The new SKILL.md replaces browser-prompt-api -- its description must be broad enough
to trigger on any Built-in AI API reference (Summarizer, Writer, Translator, etc.),
not just LanguageModel/Prompt API.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create browser-built-in-ai routing SKILL.md and reference files</name>
  <files>
    plugins/application-dev/skills/browser-built-in-ai/SKILL.md
    plugins/application-dev/skills/browser-built-in-ai/references/prompt-api.md
    plugins/application-dev/skills/browser-built-in-ai/references/summarizer-api.md
    plugins/application-dev/skills/browser-built-in-ai/references/writer-rewriter-api.md
    plugins/application-dev/skills/browser-built-in-ai/references/translator-api.md
    plugins/application-dev/skills/browser-built-in-ai/references/graceful-degradation.md
  </files>
  <action>
Create the browser-built-in-ai meta-skill following the playwright-testing pattern (lean routing SKILL.md + on-demand reference files).

**SKILL.md (~120 lines)** must contain:
1. Frontmatter with `name: browser-built-in-ai` and a `description` that triggers on: Built-in AI, Summarizer, Writer, Rewriter, Translator, LanguageDetector, Proofreader, LanguageModel, Prompt API, on-device AI, Gemini Nano, Phi-4-mini, browser-local AI. Explicitly exclude WebLLM (use browser-webllm) and WebNN (use browser-webnn).
2. Decision tree for which API to use:
   - Task-specific need (summarize, write, rewrite, translate, detect language)? Use matching API
   - General-purpose / agentic / tool-calling? Use LanguageModel (Prompt API)
   - Specific model selection (Llama, Mistral, etc.)? Use WebLLM (separate skill)
   - Non-LLM inference (vision, audio, embeddings)? Use WebNN (separate skill)
3. Chrome vs Edge comparison table: all 7 APIs with availability status per browser (Chrome stable/OT, Edge dev preview/planned/not available). Note Proofreader as informational-only (OT 141-145, Chrome only, least mature).
4. Shared availability/create pattern (identical across all 7 APIs): feature detect global -> availability check -> create session with monitor
5. Graceful degradation summary with Read instruction to references/graceful-degradation.md
6. Per-use-case Read instructions pointing to the correct reference file

**references/prompt-api.md** -- LanguageModel (Prompt API):
- Migrate content from existing browser-prompt-api/SKILL.md sections 1-11 and 13 (Purpose, Browser support, Feature detection, Availability, Session creation, Prompting, Structured output, Tool use, Context management, Session lifecycle, Multimodal, Best practices)
- Update namespaces: global `LanguageModel` object (no `window.ai`)
- Note `topK`/`temperature` deprecated (silently ignored in web contexts, only honored in extensions)
- Include Chrome 140+ multimodal input (images, canvas)
- Include Chrome vs Edge notes: Phi-4-mini (3.8B params) on Edge, identical API surface
- Include permissions policy: `language-model` directive
- Types package: `@types/dom-chromium-ai`
- Keep under ~300 lines. Focus on patterns and gotchas, link to official docs for exhaustive reference.
- Source: existing browser-prompt-api/SKILL.md (350 lines -- trim to essentials since SKILL.md routing handles shared patterns)

**references/summarizer-api.md** -- Summarizer API:
- Configuration options: type (key-points, tl;dr, teaser, headline), format (markdown, plain-text), length (short, medium, long), sharedContext, expectedInputLanguages, outputLanguage
- Usage pattern: create -> summarize/summarizeStreaming -> destroy
- Chrome stable 138+, Edge dev preview 138+
- Permissions policy: `summarizer` directive
- Keep under ~150 lines

**references/writer-rewriter-api.md** -- Writer and Rewriter APIs (combined per user decision -- they share W3C Writing Assistance APIs spec):
- Writer configuration: tone (formal, neutral, casual), format, length, sharedContext, language options
- Rewriter configuration: tone (as-is, more-formal, more-casual), format (as-is, markdown, plain-text), length (as-is, shorter, longer), sharedContext, language options
- Usage patterns: create -> write/writeStreaming, create -> rewrite/rewriteStreaming, destroy
- Both OT 137-148 in Chrome, dev preview 138+ in Edge
- Permissions policy: `writer` and `rewriter` directives
- Keep under ~200 lines

**references/translator-api.md** -- Translator and LanguageDetector APIs (combined per MDN pairing):
- Translator: sourceLanguage/targetLanguage (BCP 47 codes), 45+ languages, uses expert translation model (not Gemini Nano)
- LanguageDetector: returns ranked array of {detectedLanguage, confidence}, warn about short text accuracy
- Chrome stable 138+, Edge planned (not yet available) for both
- Permissions policy: `translator` and `language-detector` directives
- Keep under ~150 lines

**references/graceful-degradation.md** -- Generalized pattern:
- Migrate from existing browser-prompt-api/references/graceful-degradation.md (53 lines)
- Generalize from LanguageModel-only to any Built-in AI API global
- Use the `tryCreateSession(ApiGlobal, options)` pattern from RESEARCH.md: pass global object itself (handles feature detection via typeof), check availability, create with monitor
- Per-API usage examples showing Summarizer, Writer, Translator alongside LanguageModel
- Keep under ~80 lines

**Anti-patterns to enforce (from CONTEXT.md locked decisions):**
- Do NOT frame WebLLM as a fallback for Built-in AI. Different API, different model, not drop-in.
- Graceful degradation means hiding AI features when unavailable, not switching to WebLLM.
- Do NOT list Prompt API, WebLLM, WebNN as equal options (old framing). Use the hierarchy.

**Sizing guidance:** Follow existing skill conventions. Reference files focus on patterns and gotchas, not exhaustive API docs. Link to official docs (Chrome developer docs, MDN) for full reference.
  </action>
  <verify>
    <automated>bash -c "test -f 'plugins/application-dev/skills/browser-built-in-ai/SKILL.md' && test -f 'plugins/application-dev/skills/browser-built-in-ai/references/prompt-api.md' && test -f 'plugins/application-dev/skills/browser-built-in-ai/references/summarizer-api.md' && test -f 'plugins/application-dev/skills/browser-built-in-ai/references/writer-rewriter-api.md' && test -f 'plugins/application-dev/skills/browser-built-in-ai/references/translator-api.md' && test -f 'plugins/application-dev/skills/browser-built-in-ai/references/graceful-degradation.md' && echo 'All 6 files exist' || echo 'MISSING files'"</automated>
  </verify>
  <done>
    - browser-built-in-ai/SKILL.md exists with decision tree, Chrome vs Edge table, shared pattern, Read instructions (~120 lines)
    - All 5 reference files exist with API-specific content
    - No reference file exceeds 300 lines
    - Decision tree routes task-specific needs to matching API, general-purpose to LanguageModel, model selection to WebLLM, non-LLM to WebNN
    - Graceful degradation generalized to any Built-in AI API (not LanguageModel-only)
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove browser-prompt-api skill directory</name>
  <files>
    plugins/application-dev/skills/browser-prompt-api/SKILL.md
    plugins/application-dev/skills/browser-prompt-api/references/graceful-degradation.md
    plugins/application-dev/skills/browser-prompt-api/examples/tool-use.js
  </files>
  <action>
Remove the entire browser-prompt-api skill directory. Its content has been migrated to browser-built-in-ai.

Run: `git rm -r plugins/application-dev/skills/browser-prompt-api/`

This removes:
- SKILL.md (350 lines -- content migrated to browser-built-in-ai/references/prompt-api.md)
- references/graceful-degradation.md (53 lines -- content migrated and generalized in browser-built-in-ai/references/graceful-degradation.md)
- examples/tool-use.js (tool calling example -- content incorporated into prompt-api.md reference)

After removal, verify no other files in the plugin reference browser-prompt-api by path. Search for `browser-prompt-api` in:
- plugins/application-dev/agents/generator.md (will be updated in Plan 02)
- plugins/application-dev/skills/ (should have no remaining references)

Do NOT update generator.md in this plan -- that is Plan 02's responsibility. The old skill name in generator.md frontmatter is harmless until Plan 02 runs.
  </action>
  <verify>
    <automated>bash -c "test ! -d 'plugins/application-dev/skills/browser-prompt-api' && echo 'Directory removed' || echo 'STILL EXISTS'"</automated>
  </verify>
  <done>
    - browser-prompt-api directory completely removed from the working tree and git index
    - No orphaned files from the old skill remain
  </done>
</task>

</tasks>

<verification>
1. browser-built-in-ai/SKILL.md contains a decision tree routing to 5 reference files
2. All 5 reference files exist and contain API-specific content
3. SKILL.md comparison table covers all 7 APIs with Chrome and Edge status
4. Graceful degradation pattern is generalized (tryCreateSession with ApiGlobal parameter)
5. browser-prompt-api directory is fully removed
6. No file in browser-built-in-ai exceeds 300 lines
7. SKILL.md is approximately 120 lines (routing only, not exhaustive)
</verification>

<success_criteria>
- New browser-built-in-ai meta-skill exists with 1 routing SKILL.md + 5 reference files
- Old browser-prompt-api skill is completely removed
- Decision tree implements the locked hierarchy: task-specific API -> LanguageModel -> WebLLM -> WebNN
- Chrome vs Edge comparison table documents all 7 APIs
- Graceful degradation is generalized to work with any Built-in AI API global
- Content follows existing skill conventions (meta-skill pattern, progressive disclosure)
</success_criteria>

<output>
After completion, create `.planning/phases/15-generator-improvements/15-01-SUMMARY.md`
</output>
