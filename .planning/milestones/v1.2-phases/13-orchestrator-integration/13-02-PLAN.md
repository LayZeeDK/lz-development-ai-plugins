---
phase: 13-orchestrator-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - plugins/application-dev/skills/application-dev/SKILL.md
autonomous: true
requirements:
  - ORCH-01
  - ORCH-04
  - ORCH-05

must_haves:
  truths:
    - "SKILL.md evaluation phase spawns 3 critics in parallel and checks all 3 summary.json files"
    - "SKILL.md SAFETY_CAP wrap-up round spawns all 3 critics"
    - "SKILL.md architecture section describes 5 agents in two-tier grouping"
    - "SKILL.md prompt protocol includes perturbation-critic"
    - "No references to 'both critics' or 'two critics' remain in SKILL.md"
  artifacts:
    - path: "plugins/application-dev/skills/application-dev/SKILL.md"
      provides: "3-critic evaluation phase, SAFETY_CAP, prompt protocol, architecture section"
      contains: "perturbation-critic"
  key_links:
    - from: "plugins/application-dev/skills/application-dev/SKILL.md"
      to: "plugins/application-dev/agents/perturbation-critic.md"
      via: "Agent spawn with subagent_type application-dev:perturbation-critic"
      pattern: "application-dev:perturbation-critic"
    - from: "plugins/application-dev/skills/application-dev/SKILL.md"
      to: "plugins/application-dev/scripts/appdev-cli.mjs"
      via: "--critics flag includes perturbation"
      pattern: "--critics perceptual,projection,perturbation"
---

<objective>
Update all SKILL.md sections to integrate the perturbation-critic into the orchestrator's evaluation flow, SAFETY_CAP wrap-up, prompt protocol, and architecture documentation.

Purpose: Plan 01 updated the CLI and dispatch table. This plan completes the integration by updating the SKILL.md orchestration instructions -- evaluation phase (3-critic spawn + checks), SAFETY_CAP wrap-up (3-critic spawn), prompt protocol (perturbation entry), and architecture section (5-agent two-tier structure). All "both critics" / "two critics" prose is generalized.

Output: SKILL.md fully updated for 3-critic orchestration with no remaining 2-critic references.
</objective>

<execution_context>
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-orchestrator-integration/13-CONTEXT.md
@.planning/phases/13-orchestrator-integration/13-RESEARCH.md
@.planning/phases/13-orchestrator-integration/13-01-SUMMARY.md

@plugins/application-dev/skills/application-dev/SKILL.md

<interfaces>
<!-- Key sections in SKILL.md that need updating. Extracted from codebase. -->

From SKILL.md (lines 259-293) -- Evaluation Phase:
```markdown
#### Evaluation Phase (each round N)

Output: `[2/3] Evaluating (round N)...`

Update state and set expected critics:
```
Bash(node ${CLAUDE_PLUGIN_ROOT}/scripts/appdev-cli.mjs update --step evaluate --critics perceptual,projection --round N)
```

Spawn both critics in parallel:
```
Agent(subagent_type: "application-dev:perceptual-critic", prompt: "This is evaluation round N.")
Agent(subagent_type: "application-dev:projection-critic", prompt: "This is evaluation round N.")
```

Apply the error recovery pattern per critic (see below).

**Binary checks:** Verify both critics produced their summary artifacts:
```
Bash(ls evaluation/round-N/perceptual/summary.json 2>/dev/null)
Bash(ls evaluation/round-N/projection/summary.json 2>/dev/null)
```

If either summary.json is missing, retry the SPECIFIC critic that failed (not
both) with the same prompt. Counts toward the 2-retry limit per critic.

**Compile evaluation:** After both summary.json files exist, compile the
ensemble evaluation report:
```

From SKILL.md (lines 375-399) -- SAFETY_CAP wrap-up:
```markdown
  - Spawn both critics in parallel:
    ```
    Agent(subagent_type: "application-dev:perceptual-critic", prompt: "This is evaluation round {N+1}.")
    Agent(subagent_type: "application-dev:projection-critic", prompt: "This is evaluation round {N+1}.")
    ```
  - Binary checks (both summary.json files exist)
```

From SKILL.md (lines 489-504) -- Agent Prompt Protocol:
```markdown
## Agent Prompt Protocol
...
**Perceptual Critic (all rounds):** `This is evaluation round N.` -- the
orchestrator fills in only the round number.

**Projection Critic (all rounds):** `This is evaluation round N.` -- the
orchestrator fills in only the round number.
```

From SKILL.md (lines 532-555) -- Architecture:
```markdown
## Architecture

Four agents with distinct roles:
- **Planner**: Expands the user's prompt into an ambitious product specification
- **Generator**: Builds the full application from the spec
- **Perceptual Critic**: Evaluates visual design quality from the product
  surface (scores Visual Design)
- **Projection Critic**: Evaluates functional coverage via write-and-run
  acceptance tests (scores Functionality, provides Product Depth test data)

The Generator and critics form an adversarial pair -- the critics' honest
assessment drives the Generator to improve. Separating generation from
evaluation prevents self-praise bias. The two critics run in parallel with
non-overlapping scoring dimensions, and the CLI compiles their outputs into a
unified EVALUATION.md.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update evaluation phase, SAFETY_CAP, and prompt protocol for 3 critics</name>
  <files>plugins/application-dev/skills/application-dev/SKILL.md</files>
  <action>
Update four sections of SKILL.md. Each change extends established 2-critic patterns to 3 critics.

**1. Evaluation Phase (lines ~259-293):**

a. Line ~262: Change --critics flag value:
   ```
   Bash(node ${CLAUDE_PLUGIN_ROOT}/scripts/appdev-cli.mjs update --step evaluate --critics perceptual,projection,perturbation --round N)
   ```

b. Line ~265: Change prose from "Spawn both critics in parallel:" to "Spawn all three critics in parallel:"

c. Lines ~268-269: Add perturbation-critic Agent call:
   ```
   Agent(subagent_type: "application-dev:perceptual-critic", prompt: "This is evaluation round N.")
   Agent(subagent_type: "application-dev:projection-critic", prompt: "This is evaluation round N.")
   Agent(subagent_type: "application-dev:perturbation-critic", prompt: "This is evaluation round N.")
   ```

d. Line ~274: Change "Verify both critics" to "Verify all three critics produced their summary artifacts:"

e. Lines ~277-279: Add perturbation binary check:
   ```
   Bash(ls evaluation/round-N/perceptual/summary.json 2>/dev/null)
   Bash(ls evaluation/round-N/projection/summary.json 2>/dev/null)
   Bash(ls evaluation/round-N/perturbation/summary.json 2>/dev/null)
   ```

f. Line ~282: Change "retry the SPECIFIC critic that failed (not both)" to "retry the SPECIFIC critic that failed (not all)"

g. Line ~284: Change "After both summary.json files exist" to "After all three summary.json files exist"

**2. SAFETY_CAP wrap-up (lines ~375-399):**

a. Line ~382: Change "Spawn both critics in parallel:" to "Spawn all three critics in parallel:"

b. Lines ~384-385: Add perturbation-critic Agent call:
   ```
   Agent(subagent_type: "application-dev:perceptual-critic", prompt: "This is evaluation round {N+1}.")
   Agent(subagent_type: "application-dev:projection-critic", prompt: "This is evaluation round {N+1}.")
   Agent(subagent_type: "application-dev:perturbation-critic", prompt: "This is evaluation round {N+1}.")
   ```

c. Line ~387: Change "Binary checks (both summary.json files exist)" to "Binary checks (all three summary.json files exist)"

**3. Agent Prompt Protocol (lines ~489-504):**

Add perturbation-critic entry after the Projection Critic entry:

```markdown
**Perturbation Critic (all rounds):** `This is evaluation round N.` -- the
orchestrator fills in only the round number.
```

Note: Per locked decision, the perturbation-critic prompt follows the same pattern as other critics -- no adversarial hint, no orchestrator additions.

**4. File-Based Communication (lines ~506-530):**

No content changes needed to the file-based communication section -- perturbation-critic uses the same summary.json pattern that is already generically described. However, verify no stale "both" references exist in this section.

Run a search after all edits: `git grep "both critic\|two critic\|both summary" -- plugins/application-dev/skills/application-dev/SKILL.md` should return zero matches.
  </action>
  <verify>
    <automated>git grep -c "both critic\|two critic\|both summary\|spawn-both" -- plugins/application-dev/skills/application-dev/SKILL.md</automated>
  </verify>
  <done>Evaluation phase spawns 3 critics with 3 binary checks. SAFETY_CAP spawns 3 critics. Prompt protocol includes perturbation-critic. No stale "both critics" / "two critics" / "both summary" / "spawn-both" references remain in SKILL.md.</done>
</task>

<task type="auto">
  <name>Task 2: Restructure architecture section for 5 agents</name>
  <files>plugins/application-dev/skills/application-dev/SKILL.md</files>
  <action>
Replace the Architecture section (lines ~532-555) with a restructured version that:

1. Changes "Four agents" to "Five agents"
2. Groups agents into two conceptual tiers: Planning and Generation vs Critic Ensemble
3. Each critic bullet includes its scoring dimension in parentheses (matches WGAN Critic Roadmap)
4. Adds perturbation-critic with Robustness dimension
5. Updates "two critics" to "three critics" in the adversarial prose
6. Preserves existing prose about adversarial separation, information barrier, non-overlapping dimensions
7. Leaves the convergence detection paragraph unchanged (already refers to escalation levels generically)

Target structure (per locked decision -- exact wording at Claude's discretion):

```markdown
## Architecture

Five agents with distinct roles:

**Planning and Generation:**
- **Planner**: Expands the user's prompt into an ambitious product specification
- **Generator**: Builds the full application from the spec

**Critic Ensemble:**
- **Perceptual Critic** (Visual Design): Evaluates visual design quality from
  the product surface
- **Projection Critic** (Functionality, Product Depth): Evaluates functional
  coverage via write-and-run acceptance tests
- **Perturbation Critic** (Robustness): Evaluates application resilience through
  adversarial testing of extreme conditions

The Generator and critics form an adversarial pair -- the critics' honest
assessment drives the Generator to improve. Separating generation from
evaluation prevents self-praise bias. The three critics run in parallel with
non-overlapping scoring dimensions, and the CLI compiles their outputs into a
unified EVALUATION.md.

Role boundaries enforced by tool allowlists (agent frontmatter) and prompt
guards (agent instructions). Critics cannot read source code (information
barrier). The orchestrator's output domain is .appdev-state.json and .gitignore
only.

Convergence detection uses escalation levels (E-0 through E-IV) computed by
appdev-cli from score trajectory data. The orchestrator acts on the structured
JSON response without interpreting scores directly.
```

Verify the final section reads correctly and the two-tier grouping is visually clear.
  </action>
  <verify>
    <automated>git grep -c "Five agents\|Critic Ensemble\|Perturbation Critic.*Robustness\|three critics run in parallel" -- plugins/application-dev/skills/application-dev/SKILL.md</automated>
  </verify>
  <done>Architecture section describes 5 agents in two tiers (Planning and Generation / Critic Ensemble). Each critic includes its scoring dimension. Prose says "three critics" not "two critics". Convergence detection paragraph unchanged. Information barrier and tool allowlist prose preserved.</done>
</task>

</tasks>

<verification>
1. `git grep "both critic\|two critic\|spawn-both\|Four agents" -- plugins/application-dev/skills/application-dev/SKILL.md` -- returns zero matches (all 2-critic references replaced)
2. `git grep "perturbation-critic" -- plugins/application-dev/skills/application-dev/SKILL.md` -- returns matches in dispatch table, evaluation phase, SAFETY_CAP, prompt protocol, and architecture section
3. `git grep "spawn-all-critics" -- plugins/application-dev/skills/application-dev/SKILL.md` -- returns dispatch table entry
4. `git grep "Five agents" -- plugins/application-dev/skills/application-dev/SKILL.md` -- returns 1 match
5. `git grep "Critic Ensemble" -- plugins/application-dev/skills/application-dev/SKILL.md` -- returns 1 match
6. `node --test plugins/application-dev/scripts/test-appdev-cli.mjs` -- full test suite still passes (SKILL.md changes are prose-only, no test regression)
</verification>

<success_criteria>
- SKILL.md evaluation phase: --critics perceptual,projection,perturbation, 3 Agent() calls, 3 binary checks
- SKILL.md SAFETY_CAP: 3 Agent() calls for wrap-up round, 3 binary checks
- SKILL.md prompt protocol: perturbation-critic entry present with same pattern as other critics
- SKILL.md architecture: 5 agents, two-tier grouping (Planning and Generation / Critic Ensemble), each critic with dimension annotation, "three critics run in parallel"
- Zero occurrences of "both critics", "two critics", "spawn-both-critics", or "Four agents" in SKILL.md
- All existing tests still pass (no regression from prose changes)
</success_criteria>

<output>
After completion, create `.planning/phases/13-orchestrator-integration/13-02-SUMMARY.md`
</output>
