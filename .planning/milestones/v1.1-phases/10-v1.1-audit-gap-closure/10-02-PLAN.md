---
phase: 10-v1.1-audit-gap-closure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/evaluator-hardening-structure.test.mjs
  - plugins/application-dev/skills/application-dev/references/evaluator/ASSET-VALIDATION-PROTOCOL.md
  - plugins/application-dev/agents/generator.md
  - plugins/application-dev/skills/application-dev/references/evaluator/AI-PROBING-REFERENCE.md
  - plugins/application-dev/README.md
autonomous: true
requirements:
  - ENSEMBLE-01
  - ENSEMBLE-02
  - ENSEMBLE-03
  - ENSEMBLE-05
  - ENSEMBLE-06
  - ENSEMBLE-07
  - ENSEMBLE-08
  - ENSEMBLE-09
  - ENSEMBLE-10
  - BARRIER-01
  - BARRIER-02
  - BARRIER-03
  - BARRIER-04

must_haves:
  truths:
    - "No test file references deleted evaluator.md"
    - "No orphaned reference files ship to users without a consumer"
    - "generator.md has zero stale Evaluator or Code Quality references"
    - "AI-PROBING-REFERENCE.md uses v1.1 terminology (critic, not Evaluator)"
    - "README.md describes 4 agents, 3 dimensions, EVALUATION.md protocol, ensemble architecture"
  artifacts:
    - path: "plugins/application-dev/agents/generator.md"
      provides: "Updated v1.1 terminology"
      contains: "evaluation report"
    - path: "plugins/application-dev/skills/application-dev/references/evaluator/AI-PROBING-REFERENCE.md"
      provides: "Updated v1.1 terminology"
      contains: "projection-critic"
    - path: "plugins/application-dev/README.md"
      provides: "Accurate v1.1 user documentation"
      contains: "Perceptual Critic"
  key_links:
    - from: "generator.md fix-only mode"
      to: "evaluation/round-N/EVALUATION.md"
      via: "Read instruction in rounds 2+"
      pattern: "evaluation report"
    - from: "README.md Architecture section"
      to: "perceptual-critic + projection-critic"
      via: "Documentation of ensemble pattern"
      pattern: "ensemble"
---

<objective>
Clean up stale artifacts from the evaluator-to-ensemble migration: delete the broken test file and orphaned reference, update generator.md and AI-PROBING-REFERENCE.md terminology, and rewrite README.md for the v1.1 architecture. This closes all 14 orphaned Phase 7 requirements (ENSEMBLE-01..10, BARRIER-01..04) which were implemented correctly but never formally verified.

Purpose: No dead code ships to users, no stale terminology confuses agents, and user-facing documentation is accurate.
Output: Clean codebase with consistent v1.1 terminology and accurate README.md.
</objective>

<execution_context>
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-v1.1-audit-gap-closure/10-RESEARCH.md

<interfaces>
<!-- Key stale references to fix. Extracted from codebase. -->

From generator.md (12 stale references):
- Line 18: "Evaluator's feedback" -> "evaluation report"
- Line 29: "iterate based on Evaluator's feedback" -> "iterate based on critic ensemble feedback"
- Line 101: "the Evaluator catches what tests miss" -> "the critic ensemble catches what tests miss"
- Line 203: "The Evaluator takes over from here" -> "The critic ensemble evaluates from here"
- Line 207: "what the Evaluator flagged" -> "what the evaluation report flagged"
- Line 209: "Read the Evaluator's feedback first" -> "Read the evaluation report first"
- Line 214: "from the evaluation report" (already correct phrasing in some spots)
- Line 223: "the Evaluator did not flag" -> "the evaluation report did not flag"
- Line 225: "If Code Quality is below 6, refactor." -> DELETE this sentence entirely (Code Quality removed)
- Line 245: "belong to the Evaluator agent" -> "belong to the critic ensemble"
- Line 249: "the Evaluator will flag them" -> "the critic ensemble will flag them"
- Line 251: "trigger the Evaluator's asset validation" -> "trigger the critic ensemble's asset validation"

From AI-PROBING-REFERENCE.md (17 stale references):
- Replace "Evaluator" with "projection-critic" or "critic" as context requires
- Replace "Code Quality" with appropriate v1.1 dimension names or remove

From README.md (85 lines, multiple stale sections):
- Line 7: "three specialized agents" -> four
- Line 11: Evaluator description -> Perceptual Critic + Projection Critic
- Line 37-38: "four criteria" -> three dimensions
- Line 39: "3 build/QA rounds" -> "10 generation/evaluation rounds"
- Line 43-44: playwright-cli PATH prereq -> npm devDependency
- Lines 53-57: Architecture section -> ensemble
- Lines 62-68: Evaluation Criteria table -> 3 dimensions (no Code Quality)
- Lines 72-77: File Protocol -> EVALUATION.md (not QA-REPORT.md)

From tests/evaluator-hardening-structure.test.mjs:
- 21 tests total, 15 fail (reference deleted evaluator.md)
- Decision: DELETE entirely (research recommendation)

From ASSET-VALIDATION-PROTOCOL.md:
- 48 lines, zero consumers in codebase
- Decision: DELETE (research recommendation)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete stale artifacts and update generator.md terminology</name>
  <files>
    tests/evaluator-hardening-structure.test.mjs
    plugins/application-dev/skills/application-dev/references/evaluator/ASSET-VALIDATION-PROTOCOL.md
    plugins/application-dev/agents/generator.md
  </files>
  <action>
**Delete 1: evaluator-hardening-structure.test.mjs**

Delete `tests/evaluator-hardening-structure.test.mjs` entirely. This file has 21 tests, 15 of which fail because they reference the deleted `evaluator.md`. The 6 passing tests verify AI-PROBING-REFERENCE.md and SCORING-CALIBRATION.md content which is stable and validated by Phase 7 manual review. The v1.1 architecture has its own test coverage (57 automated tests in test-appdev-cli.mjs).

Use `git rm tests/evaluator-hardening-structure.test.mjs` to remove from tracking.

**Delete 2: ASSET-VALIDATION-PROTOCOL.md**

Delete `plugins/application-dev/skills/application-dev/references/evaluator/ASSET-VALIDATION-PROTOCOL.md`. This 48-line file was extracted for the monolithic evaluator's Step 7 (asset validation with sharp/imghash/leven). In v1.1, perceptual-critic references AI-SLOP-CHECKLIST.md and calls install-dep directly. The file has zero consumers (confirmed by `git grep`). It ships dead code to users.

Use `git rm plugins/application-dev/skills/application-dev/references/evaluator/ASSET-VALIDATION-PROTOCOL.md` to remove.

**Update 3: generator.md v1.1 terminology**

Read `plugins/application-dev/agents/generator.md` in full. Make these specific changes:

1. Line 18 (example assistant message): Change "Evaluator's feedback" to "the evaluation report" -- e.g., "I'll spawn the generator agent to address the evaluation report's feedback."
2. Line 29: Change "iterate based on Evaluator's feedback" to "iterate based on the evaluation report"
3. Line 101: Change "the Evaluator catches what tests miss" to "the critic ensemble catches what tests miss"
4. Line 203: Change "The Evaluator takes over from here" to "The critic ensemble evaluates from here"
5. Line 207: Change "what the Evaluator flagged" to "what the evaluation report flagged" (also update the full sentence for natural reading)
6. Line 209: Change "Read the Evaluator's feedback first" to "Read the evaluation report first"
7. Line 214: Change "the Evaluator groups them" to "the evaluation report groups them"
8. Line 223: Change "the Evaluator did not flag" to "the evaluation report did not flag"
9. Line 225: DELETE the entire sentence "If Code Quality is below 6, refactor." -- Code Quality was removed as a dimension in Phase 7. The remaining prioritization guidance (Product Depth/Functionality below 7, Visual Design below 6) is correct.
10. Line 245: Change "belong to the Evaluator agent" to "belong to the critic ensemble"
11. Line 249: Change "the Evaluator will flag them" to "the critic ensemble will flag them"
12. Line 251: Change "trigger the Evaluator's asset validation" to "trigger the critic ensemble's asset validation"

Preserve all other content. The behavioral instructions are correct -- only the terminology is stale. Renumber any affected list items after the Code Quality deletion on line 225.
  </action>
  <verify>
    <automated>node plugins/application-dev/scripts/test-appdev-cli.mjs</automated>
  </verify>
  <done>
- tests/evaluator-hardening-structure.test.mjs deleted (git rm)
- ASSET-VALIDATION-PROTOCOL.md deleted (git rm)
- generator.md has zero "Evaluator" references (all replaced with "evaluation report" or "critic ensemble")
- generator.md has zero "Code Quality" references (line 225 deleted)
- `git grep "Evaluator" -- plugins/application-dev/agents/generator.md` returns empty
- `git grep "Code Quality" -- plugins/application-dev/agents/generator.md` returns empty
- All 57 CLI tests still pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Update AI-PROBING-REFERENCE.md terminology and rewrite README.md</name>
  <files>
    plugins/application-dev/skills/application-dev/references/evaluator/AI-PROBING-REFERENCE.md
    plugins/application-dev/README.md
  </files>
  <action>
**Update 1: AI-PROBING-REFERENCE.md v1.1 terminology**

Read `plugins/application-dev/skills/application-dev/references/evaluator/AI-PROBING-REFERENCE.md` in full. It has 17 stale "Evaluator" and "Code Quality" references. Make these replacements:

- "Evaluator" -> "projection-critic" when referring to the agent that uses this file
- "Evaluator" -> "critic" when referring generically to the evaluation role
- "Code Quality" -> remove or replace with the appropriate v1.1 dimension name:
  - If the reference is about scoring: replace with "Functionality" (since projection-critic scores Functionality)
  - If the reference is about a scoring dimension list: update to the v1.1 list (Product Depth, Functionality, Visual Design)
  - If the reference gives Code Quality-specific guidance (e.g., "deduct for poor code structure"): DELETE that guidance entirely since Code Quality is no longer a scored dimension

Read each reference in context to determine the correct replacement. The file teaches AI feature probing -- most "Evaluator" references mean "the agent reading this file" which is now projection-critic.

**Update 2: README.md for v1.1 architecture**

Read `plugins/application-dev/README.md` (85 lines) in full and rewrite it to reflect the v1.1 architecture. Keep the same concise style -- do NOT over-engineer (Pitfall 4 from research). Specific changes:

1. **Opening line** (line 1-2): Change "three-agent architecture" to "GAN-inspired four-agent ensemble architecture" or similar.

2. **Agent list** (lines 9-12): Replace the 3-agent list with 4 agents:
   - **Planner**: (keep as-is)
   - **Generator**: (keep as-is)
   - **Perceptual Critic**: Scores Visual Design by detecting AI slop and assessing whether the product passes as hand-built. Evaluates via browser screenshots and interaction.
   - **Projection Critic**: Scores Functionality by writing and running acceptance tests against SPEC.md criteria. Tests execute outside agent context for token efficiency.

3. **How It Works** (lines 32-39):
   - Step 3: Change "Evaluate" description from single Evaluator to ensemble: "Both critics evaluate the running app in parallel -- the Perceptual Critic observes visual quality, the Projection Critic runs acceptance tests. The CLI compiles their findings into a single evaluation report."
   - Step 4: Change "3 build/QA rounds" to "Up to 10 generation/evaluation rounds" and update language from "Evaluator's detailed feedback" to "the evaluation report"

4. **Prerequisites** (lines 42-44): Remove the `playwright-cli` PATH requirement. Replace with:
   - Claude Code with Opus model access (recommended for best results)
   - Node.js (npm used for devDependencies during workspace setup)

5. **Architecture** (lines 51-58): Update the GAN description:
   - Replace "Generator and Evaluator form an adversarial pair" with "Generator and critic ensemble form an adversarial pair"
   - Replace "The Evaluator is calibrated to be skeptical" with "The critics are calibrated to be skeptical"
   - Add mention of the ensemble pattern: "Two specialized critics (Perceptual + Projection) evaluate in parallel, each in its own isolated context. The CLI aggregates their findings deterministically."

6. **Evaluation Criteria** table (lines 62-68): Update to 3 dimensions:
   | Dimension | Threshold | What It Measures |
   |-----------|-----------|------------------|
   | Product Depth | 7/10 | Feature completeness vs. spec (CLI-computed from acceptance tests) |
   | Functionality | 7/10 | Does it actually work when used? (Projection Critic) |
   | Visual Design | 6/10 | Coherent identity, not AI-slop (Perceptual Critic) |

   Remove Code Quality row entirely.

7. **File Protocol** table (lines 72-77): Update:
   | File | Writer | Reader | Purpose |
   |------|--------|--------|---------|
   | `SPEC.md` | Planner | Generator, Critics | Product specification |
   | `evaluation/round-N/EVALUATION.md` | CLI (compile-evaluation) | Generator (next round) | Compiled evaluation report |
   | `evaluation/round-N/*/summary.json` | Each critic | CLI (compile-evaluation) | Per-critic scoring data |

   Remove QA-REPORT.md reference.

8. **Model recommendation** (lines 83-85): Keep as-is (still accurate).

Preserve the existing sections: Usage examples, Design Quality, License. These are still correct.
  </action>
  <verify>
    <automated>! git grep -l "Evaluator\|Code Quality\|QA-REPORT" -- "plugins/application-dev/README.md" "plugins/application-dev/agents/generator.md" "plugins/application-dev/skills/application-dev/references/evaluator/AI-PROBING-REFERENCE.md"</automated>
  </verify>
  <done>
- AI-PROBING-REFERENCE.md has zero stale "Evaluator" references (replaced with projection-critic/critic)
- AI-PROBING-REFERENCE.md has zero stale "Code Quality" references (removed or replaced)
- README.md describes 4 agents (Planner, Generator, Perceptual Critic, Projection Critic)
- README.md has 3 evaluation dimensions (no Code Quality)
- README.md file protocol shows EVALUATION.md (not QA-REPORT.md)
- README.md architecture section describes the ensemble pattern
- README.md prerequisites mention npm (not playwright-cli on PATH)
- `git grep "Evaluator" -- plugins/application-dev/README.md` returns 0 matches
- `git grep "QA-REPORT" -- plugins/application-dev/README.md` returns 0 matches
  </done>
</task>

</tasks>

<verification>
1. `node plugins/application-dev/scripts/test-appdev-cli.mjs` -- all 57 tests pass
2. `ls tests/evaluator-hardening-structure.test.mjs` -- file does not exist
3. `ls plugins/application-dev/skills/application-dev/references/evaluator/ASSET-VALIDATION-PROTOCOL.md` -- file does not exist
4. `! git grep -l "Evaluator" -- plugins/application-dev/agents/generator.md` -- exits 0 (no matches)
5. `! git grep -l "Code Quality" -- plugins/application-dev/agents/generator.md` -- exits 0 (no matches)
6. `! git grep -l "Evaluator" -- plugins/application-dev/skills/application-dev/references/evaluator/AI-PROBING-REFERENCE.md` -- exits 0 (no matches)
7. `! git grep -l "QA-REPORT\|Code Quality" -- plugins/application-dev/README.md` -- exits 0 (no matches)
8. `git grep "Perceptual Critic" -- plugins/application-dev/README.md` -- returns at least 1 match
</verification>

<success_criteria>
All stale artifacts from the evaluator-to-ensemble migration are cleaned up:
1. Broken test file deleted
2. Orphaned ASSET-VALIDATION-PROTOCOL.md deleted
3. generator.md uses v1.1 terminology throughout
4. AI-PROBING-REFERENCE.md uses v1.1 terminology throughout
5. README.md accurately describes the v1.1 4-agent ensemble architecture
The 14 orphaned Phase 7 requirements (ENSEMBLE-01..10, BARRIER-01..04) are closeable via verification review since implementation was confirmed correct by the audit.
</success_criteria>

<output>
After completion, create `.planning/phases/10-v1.1-audit-gap-closure/10-02-SUMMARY.md`
</output>
