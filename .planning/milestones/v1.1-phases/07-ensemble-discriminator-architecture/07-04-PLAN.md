---
phase: 07-ensemble-discriminator-architecture
plan: 04
type: execute
wave: 3
depends_on:
  - 07-01
  - 07-03
files_modified:
  - plugins/application-dev/skills/application-dev/SKILL.md
autonomous: false
requirements:
  - ENSEMBLE-10

must_haves:
  truths:
    - "Orchestrator evaluation phase spawns 2 critics in parallel (perceptual-critic + projection-critic)"
    - "Orchestrator performs binary file-exists checks for both summary.json files after critic spawns"
    - "Orchestrator calls compile-evaluation CLI subcommand after both critics complete"
    - "Orchestrator calls round-complete with the CLI-compiled EVALUATION.md"
    - "Critic prompts are minimal: 'This is evaluation round N.' per ENSEMBLE-10"
    - "SKILL.md no longer references evaluator agent"
  artifacts:
    - path: "plugins/application-dev/skills/application-dev/SKILL.md"
      provides: "Updated orchestrator with ensemble evaluation phase"
      contains: "perceptual-critic"
  key_links:
    - from: "SKILL.md evaluation phase"
      to: "perceptual-critic.md"
      via: "Agent(subagent_type: 'application-dev:perceptual-critic')"
      pattern: "application-dev:perceptual-critic"
    - from: "SKILL.md evaluation phase"
      to: "projection-critic.md"
      via: "Agent(subagent_type: 'application-dev:projection-critic')"
      pattern: "application-dev:projection-critic"
    - from: "SKILL.md evaluation phase"
      to: "appdev-cli compile-evaluation"
      via: "Bash(node ... appdev-cli.mjs compile-evaluation --round N)"
      pattern: "compile-evaluation"
    - from: "SKILL.md evaluation phase"
      to: "appdev-cli round-complete"
      via: "Bash(node ... appdev-cli.mjs round-complete --round N --report ...)"
      pattern: "round-complete"
---

<objective>
Rewrite the orchestrator's evaluation phase in SKILL.md to spawn 2 parallel critics
instead of 1 monolithic evaluator, add binary file-exists checks for summary.json
files, and call CLI compile-evaluation before round-complete. Then do a final
human review of the complete ensemble architecture.

Purpose: This wires the ensemble architecture into the orchestrator workflow. The
evaluation phase becomes: spawn both critics -> check artifacts -> CLI compile ->
round-complete. The orchestrator remains minimal -- no qualitative assessment of
critic output.

Output: Updated SKILL.md with new evaluation phase flow. Human-verified ensemble.
</objective>

<execution_context>
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ensemble-discriminator-architecture/07-CONTEXT.md
@.planning/phases/07-ensemble-discriminator-architecture/07-01-SUMMARY.md
@.planning/phases/07-ensemble-discriminator-architecture/07-03-SUMMARY.md

<interfaces>
From plugins/application-dev/skills/application-dev/SKILL.md (current evaluation phase):
```
#### Evaluation Phase (each round N)

Update state:
Bash(node ${CLAUDE_PLUGIN_ROOT}/scripts/appdev-cli.mjs update --step evaluate --round N)

Spawn the Evaluator:
Agent(subagent_type: "application-dev:evaluator", prompt: "This is evaluation round N.")

Binary check: Read evaluation/round-N/EVALUATION.md -- verify exists and contains ## Verdict
```

New evaluation phase pattern (from 07-RESEARCH.md Pattern 5):
```
#### Evaluation Phase (each round N)

Update state:
Bash(node ${CLAUDE_PLUGIN_ROOT}/scripts/appdev-cli.mjs update --step evaluate --round N)

Spawn both critics in parallel:
Agent(subagent_type: "application-dev:perceptual-critic", prompt: "This is evaluation round N.")
Agent(subagent_type: "application-dev:projection-critic", prompt: "This is evaluation round N.")

Binary checks:
- evaluation/round-N/perceptual/summary.json exists
- evaluation/round-N/projection/summary.json exists

Compile evaluation:
Bash(node ${CLAUDE_PLUGIN_ROOT}/scripts/appdev-cli.mjs compile-evaluation --round N)

Binary check: evaluation/round-N/EVALUATION.md exists and contains ## Scores

Convergence check:
Bash(node ${CLAUDE_PLUGIN_ROOT}/scripts/appdev-cli.mjs round-complete --round N --report evaluation/round-N/EVALUATION.md)
```

From SKILL.md allowed-tools:
```
allowed-tools: Agent Read Write Bash(node *appdev-cli*) Bash(git *) Bash(npm *) Bash(ls *)
```

From SKILL.md architecture section (must update):
```
Three agents with distinct roles:
- Planner: Expands the user's prompt
- Generator: Builds the full application
- Evaluator: Critiques the running app
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite SKILL.md evaluation phase for ensemble architecture</name>
  <files>plugins/application-dev/skills/application-dev/SKILL.md</files>
  <action>
    Read the full SKILL.md file. Make targeted changes to the evaluation phase and
    related sections. Do NOT rewrite unrelated sections (Step 0, Step 0.5, Step 1,
    Generation Phase, Step 3, Error Recovery base pattern).

    CHANGES TO EVALUATION PHASE (currently in "#### Evaluation Phase"):

    Replace the single Evaluator spawn with the ensemble pattern:

    1. Keep the state update: `Bash(node ${CLAUDE_PLUGIN_ROOT}/scripts/appdev-cli.mjs
       update --step evaluate --round N)`

    2. Replace single Agent spawn with two parallel spawns:
       ```
       Agent(subagent_type: "application-dev:perceptual-critic", prompt: "This is evaluation round N.")
       Agent(subagent_type: "application-dev:projection-critic", prompt: "This is evaluation round N.")
       ```

    3. Replace single EVALUATION.md binary check with TWO summary.json checks:
       ```
       Bash(ls evaluation/round-N/perceptual/summary.json 2>/dev/null)
       Bash(ls evaluation/round-N/projection/summary.json 2>/dev/null)
       ```
       Apply error recovery pattern if either is missing -- retry the SPECIFIC
       critic that failed (not both).

    4. Add CLI compile step after both checks pass:
       ```
       Bash(node ${CLAUDE_PLUGIN_ROOT}/scripts/appdev-cli.mjs compile-evaluation --round N)
       ```

    5. Add EVALUATION.md binary check after compile:
       ```
       Read evaluation/round-N/EVALUATION.md -- verify exists and contains ## Scores
       ```

    6. Keep existing round-complete call unchanged.

    CHANGES TO ERROR RECOVERY SECTION:
    - "treat it as an Evaluator failure" -> "treat it as a compile failure --
      check which summary.json is missing or malformed and retry that critic"

    CHANGES TO AGENT PROMPT PROTOCOL:
    - Remove Evaluator line, add Perceptual Critic and Projection Critic lines

    CHANGES TO FILE-BASED COMMUNICATION:
    - Replace evaluator file reference with 3 entries: perceptual/summary.json,
      projection/summary.json, EVALUATION.md (CLI-compiled)

    CHANGES TO ARCHITECTURE SECTION:
    - Replace Evaluator description with Perceptual Critic, Projection Critic,
      and CLI Ensemble descriptions
    - Update adversarial pair description to reference "critics"

    CHANGES TO DESCRIPTION (frontmatter):
    - Replace "Evaluator" with "Perceptual Critic + Projection Critic"

    Do NOT change: Step 0, Step 0.5, Step 1, Generation Phase, Step 3 Summary,
    convergence check responses, the Rules section.
  </action>
  <verify>
    <automated>node -e "const f=require('fs').readFileSync('plugins/application-dev/skills/application-dev/SKILL.md','utf8'); const r={has_perceptual:f.includes('application-dev:perceptual-critic'), has_projection:f.includes('application-dev:projection-critic'), no_evaluator_spawn:!f.includes('application-dev:evaluator'), has_compile:f.includes('compile-evaluation'), has_summary_check:f.includes('perceptual/summary.json') && f.includes('projection/summary.json')}; console.log(JSON.stringify(r))"</automated>
  </verify>
  <done>
    SKILL.md evaluation phase spawns 2 critics in parallel, checks for both
    summary.json files, calls compile-evaluation, then round-complete. No references
    to "evaluator" agent remain. Architecture section describes the ensemble.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Final review of complete ensemble discriminator architecture</name>
  <files>plugins/application-dev/skills/application-dev/SKILL.md</files>
  <action>
    Present the complete ensemble architecture for human review. All automated work
    is complete across Plans 01-04. This checkpoint verifies the full integration.
  </action>
  <verify>
    <automated>node -e "const f=require('fs').readFileSync('plugins/application-dev/skills/application-dev/SKILL.md','utf8'); console.log(JSON.stringify({no_evaluator:!f.includes('application-dev:evaluator'), has_both_critics:f.includes('perceptual-critic') && f.includes('projection-critic')}))"</automated>
  </verify>
  <done>Human approved the complete ensemble discriminator architecture.</done>
</task>

</tasks>

<verification>
1. SKILL.md contains "application-dev:perceptual-critic" and "application-dev:projection-critic"
2. SKILL.md does NOT contain "application-dev:evaluator"
3. SKILL.md contains "compile-evaluation" CLI call
4. SKILL.md contains binary checks for both perceptual/summary.json and projection/summary.json
5. Architecture section describes ensemble (critics + CLI) not monolithic evaluator
</verification>

<success_criteria>
- Orchestrator evaluation phase follows the ensemble pattern: spawn both -> check both -> compile -> round-complete
- No references to "evaluator" agent remain in SKILL.md
- Error recovery handles per-critic retries
- Agent prompt protocol documents both critics with minimal prompts
- File-based communication section documents the new artifact flow
- Human approves the complete ensemble architecture
</success_criteria>

<output>
After completion, create `.planning/phases/07-ensemble-discriminator-architecture/07-04-SUMMARY.md`
</output>
