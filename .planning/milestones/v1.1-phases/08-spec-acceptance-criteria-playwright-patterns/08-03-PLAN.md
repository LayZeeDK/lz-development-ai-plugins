---
phase: 08-spec-acceptance-criteria-playwright-patterns
plan: 03
type: execute
wave: 2
depends_on: [08-02]
files_modified:
  - plugins/application-dev/agents/perceptual-critic.md
  - plugins/application-dev/agents/projection-critic.md
  - plugins/application-dev/agents/generator.md
autonomous: true
requirements: [PLAYWRIGHT-01, PLAYWRIGHT-02, TOKEN-02, TOKEN-03, TOKEN-04, TOKEN-05]

must_haves:
  truths:
    - "Perceptual-critic has a Read instruction pointing to PLAYWRIGHT-EVALUATION.md eval-first + resize+eval + console filtering sections"
    - "Projection-critic has a Read instruction pointing to PLAYWRIGHT-EVALUATION.md write-and-run + snapshot-as-fallback + console filtering + test healing + round 2+ reuse sections"
    - "Both critics use console error (filtered) not console (all messages)"
    - "Generator explicitly states dev tests in tests/ are independent from acceptance tests"
    - "Projection-critic round 2+ logic: copy prior tests, run, decide reuse/heal/regenerate"
  artifacts:
    - path: "plugins/application-dev/agents/perceptual-critic.md"
      provides: "Read instruction for PLAYWRIGHT-EVALUATION.md eval-first techniques"
      contains: "PLAYWRIGHT-EVALUATION.md"
    - path: "plugins/application-dev/agents/projection-critic.md"
      provides: "Read instruction for PLAYWRIGHT-EVALUATION.md write-and-run + round 2+ techniques"
      contains: "PLAYWRIGHT-EVALUATION.md"
    - path: "plugins/application-dev/agents/generator.md"
      provides: "Explicit dev test boundary statement (PLAYWRIGHT-01)"
      contains: "tests/"
  key_links:
    - from: "plugins/application-dev/agents/perceptual-critic.md"
      to: "plugins/application-dev/skills/application-dev/references/evaluator/PLAYWRIGHT-EVALUATION.md"
      via: "Read instruction in OBSERVE step"
      pattern: "Read.*PLAYWRIGHT-EVALUATION"
    - from: "plugins/application-dev/agents/projection-critic.md"
      to: "plugins/application-dev/skills/application-dev/references/evaluator/PLAYWRIGHT-EVALUATION.md"
      via: "Read instruction in TEST step"
      pattern: "Read.*PLAYWRIGHT-EVALUATION"
    - from: "plugins/application-dev/agents/projection-critic.md"
      to: "SPEC.md acceptance criteria"
      via: "write-and-run reads criteria then writes tests"
      pattern: "acceptance.*criteria"
---

<objective>
Wire both critic agent definitions to the new PLAYWRIGHT-EVALUATION.md reference and clarify the Generator's test boundary.

Purpose: Connect the shared Playwright evaluation reference to its consumers (critics), ensure both critics use token-efficient patterns (console error filtering, eval-first, write-and-run), add round 2+ test reuse logic to the projection-critic, and clarify that the Generator's dev tests (tests/) are independent from the projection-critic's acceptance tests (evaluation/round-N/).

Output: Modified perceptual-critic.md, projection-critic.md, and generator.md.
</objective>

<execution_context>
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-spec-acceptance-criteria-playwright-patterns/08-CONTEXT.md
@.planning/phases/08-spec-acceptance-criteria-playwright-patterns/08-RESEARCH.md
@.planning/phases/08-spec-acceptance-criteria-playwright-patterns/08-02-SUMMARY.md

<interfaces>
<!-- Perceptual-critic OBSERVE step (lines 37-48) -- this is where the Read instruction goes -->
From plugins/application-dev/agents/perceptual-critic.md:
```markdown
### OBSERVE

Use eval-first for structured page state -- `npx playwright-cli eval` returns DOM
state as structured JSON, consuming far fewer tokens than screenshots or accessibility
snapshots. Take screenshots only at key viewpoints (one per page per critical breakpoint),
not at every scroll position.

For responsive testing, resize the viewport and re-evaluate:
...

Filter console output for visual-relevant issues (CSS errors, font loading failures,
image decode errors). Functional console errors belong to the projection-critic.
```

<!-- Projection-critic TEST step (lines 39-57) -- this is where the Read instruction goes -->
From plugins/application-dev/agents/projection-critic.md:
```markdown
### TEST

Use the write-and-run pattern for token efficiency. This replaces 30+ interactive
browser commands with approximately 5 tool calls:

1. Read SPEC.md acceptance criteria (already done in UNDERSTAND)
2. Take one snapshot for selector discovery: `npx playwright-cli snapshot`
3. Write acceptance tests to `evaluation/round-N/projection/acceptance-tests.spec.ts`
4. Run: `npx playwright test evaluation/round-N/projection/acceptance-tests.spec.ts --reporter=json`
5. Read the JSON results file

The tests live outside your context -- you write them to disk, Playwright runs them,
and you read only the structured results. This keeps token usage low.

Each test maps to a SPEC.md feature and criteria pair. Cover:
- Core feature workflows end-to-end (create, read, update, delete)
- ...
```

<!-- Generator testing section (lines 83-87) -- test boundary clarification goes here -->
From plugins/application-dev/agents/generator.md:
```markdown
4. **Test framework:** Analyze SPEC.md app type and choose emphasis (see testing
   decision framework below). Install Vitest for unit/integration tests.
...
6. **E2e framework:** Install Playwright Test if the app has 3+ pages or complex
   user flows. Read `${CLAUDE_PLUGIN_ROOT}/skills/playwright-testing/SKILL.md`...
```

<!-- Projection-critic acceptance test independence section (lines 137-138) -->
From plugins/application-dev/agents/projection-critic.md:
```markdown
## Acceptance Test Independence

Your acceptance tests in `evaluation/round-N/projection/` are INDEPENDENT from the
Generator's dev tests in `tests/`.
```

<!-- PLAYWRIGHT-EVALUATION.md path (created by Plan 02) -->
Path: ${CLAUDE_PLUGIN_ROOT}/skills/application-dev/references/evaluator/PLAYWRIGHT-EVALUATION.md
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update perceptual-critic.md with PLAYWRIGHT-EVALUATION.md Read instruction and console filtering</name>
  <files>plugins/application-dev/agents/perceptual-critic.md</files>
  <action>
Modify perceptual-critic.md with targeted additions to the OBSERVE step:

1. Add a Read instruction at the beginning of the OBSERVE step, before the existing eval-first description. The Read instruction tells the critic which sections of PLAYWRIGHT-EVALUATION.md to focus on:

```
Read the eval-first, resize+eval, and console filtering sections of the Playwright evaluation reference for token-efficient browser interaction patterns:

Read `${CLAUDE_PLUGIN_ROOT}/skills/application-dev/references/evaluator/PLAYWRIGHT-EVALUATION.md`
```

This follows the progressive disclosure pattern established in Phase 7 -- the agent definition specifies which sections to read, the reference provides the technique details.

2. Update the console filtering instruction. The existing text says "Filter console output for visual-relevant issues (CSS errors, font loading failures, image decode errors)." Make it explicit that the command is `npx playwright-cli console error` (filtered), consistent with PLAYWRIGHT-EVALUATION.md's console filtering section. Example update:

"For console output, use `npx playwright-cli console error` (filtered to errors only) to catch visual-relevant issues (CSS errors, font loading failures, image decode errors) without filling context with informational messages."

3. Keep all existing content intact. Do not remove or restructure the OBSERVE section -- add the Read instruction before it, update the console line in place.

The perceptual-critic already has eval-first and viewport+eval patterns inline (from Phase 7). The Read instruction provides deeper technique guidance from the shared reference while the inline patterns remain as quick-reference reminders. This is consistent with how SCORING-CALIBRATION.md is referenced (Read instruction + brief inline guidance).
  </action>
  <verify>
    <automated>git grep -n "PLAYWRIGHT-EVALUATION" -- "plugins/application-dev/agents/perceptual-critic.md" && git grep -n "console error" -- "plugins/application-dev/agents/perceptual-critic.md"</automated>
  </verify>
  <done>
    perceptual-critic.md has Read instruction pointing to PLAYWRIGHT-EVALUATION.md specifying eval-first, resize+eval, and console filtering sections. Console filtering uses explicit `console error` command. Existing OBSERVE content preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update projection-critic.md with PLAYWRIGHT-EVALUATION.md Read instruction and round 2+ logic</name>
  <files>plugins/application-dev/agents/projection-critic.md</files>
  <action>
Modify projection-critic.md with targeted additions:

1. Add a Read instruction at the beginning of the TEST step, before the existing write-and-run description:

```
Read the write-and-run, snapshot-as-fallback, console filtering, test healing, and round 2+ test reuse sections of the Playwright evaluation reference:

Read `${CLAUDE_PLUGIN_ROOT}/skills/application-dev/references/evaluator/PLAYWRIGHT-EVALUATION.md`
```

2. Update the console filtering instruction. The existing text says "Filter console output for functional-relevant issues (uncaught exceptions, API errors, failed network requests)." Make explicit: use `npx playwright-cli console error` (filtered). Example:

"For console output, use `npx playwright-cli console error` (filtered to errors only) to catch functional-relevant issues (uncaught exceptions, API errors, failed network requests) without filling context with informational messages."

3. Add a "### Round 2+ Test Reuse" subsection after the existing TEST subsection content (after the "Each test maps to a SPEC.md feature..." paragraph). This embeds the round 2+ decision logic directly in the agent definition for quick reference:

```
### Round 2+ Test Reuse

In rounds 2 and later, reuse acceptance tests from the prior round before writing new ones. The orchestrator passes the round number ("This is evaluation round N.").

1. Copy `evaluation/round-{N-1}/projection/acceptance-tests.spec.ts` to `evaluation/round-N/projection/`
2. Run the copied tests: `npx playwright test evaluation/round-N/projection/acceptance-tests.spec.ts --reporter=json`
3. Read results and decide:
   - **Reuse** when all tests pass or only assertion failures (report failures as findings)
   - **Heal** when 1-2 selector timeouts (re-snapshot, update selectors, re-run)
   - **Regenerate** when multiple selector timeouts or >50% timeout (full write-and-run from SPEC.md + fresh snapshot)

Do not add new tests in rounds 2+. Consistent tests across rounds enable meaningful trajectory analysis by the CLI.

If round N-1 has no acceptance-tests.spec.ts (prior crash or skip), fall back to round 1 behavior: write fresh tests from SPEC.md + snapshot.
```

4. Update the existing UNDERSTAND section to explicitly mention extracting acceptance criteria (not just features). The current text says "Extract every feature, its acceptance criteria, AI integration points..." -- this already mentions acceptance criteria, so verify it is present. If "acceptance criteria" is already mentioned, no change needed.

Keep all existing content intact except the specific additions described above.
  </action>
  <verify>
    <automated>git grep -n "PLAYWRIGHT-EVALUATION" -- "plugins/application-dev/agents/projection-critic.md" && git grep -n "Round 2+" -- "plugins/application-dev/agents/projection-critic.md" && git grep -n "console error" -- "plugins/application-dev/agents/projection-critic.md"</automated>
  </verify>
  <done>
    projection-critic.md has Read instruction pointing to PLAYWRIGHT-EVALUATION.md specifying write-and-run, snapshot-as-fallback, console filtering, test healing, and round 2+ sections. Round 2+ Test Reuse subsection with reuse/heal/regenerate decision tree. Console filtering uses explicit `console error` command. Existing TEST content preserved.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add dev test boundary clarification to generator.md</name>
  <files>plugins/application-dev/agents/generator.md</files>
  <action>
Add a brief clarification to generator.md about the independence of dev tests from acceptance tests. This addresses PLAYWRIGHT-01 (Generator writes dev tests using playwright-testing skill in tests/ as internal CI).

1. In the "Testing Skills" section (around line 206), add a sentence after the "Testing principle" paragraph that clarifies the boundary:

"Your tests in `tests/` are dev tests -- internal CI that verifies implementation correctness. They are completely independent from the acceptance tests that the evaluation ensemble writes to `evaluation/round-N/`. Do not read, reference, or duplicate evaluation test artifacts. Why: independent test suites from different perspectives (white-box implementation vs black-box product surface) catch different classes of defects."

This reinforces BARRIER-04 (independent test suites) and PLAYWRIGHT-01 (Generator owns tests/) from the Generator's perspective. The projection-critic already has the mirror statement in its "Acceptance Test Independence" section.

2. Keep all other generator.md content unchanged. This is a 2-3 line addition, not a restructure.
  </action>
  <verify>
    <automated>git grep -n "dev tests" -- "plugins/application-dev/agents/generator.md" && git grep -n "acceptance tests" -- "plugins/application-dev/agents/generator.md"</automated>
  </verify>
  <done>
    generator.md has explicit statement that tests/ are dev tests independent from evaluation/round-N/ acceptance tests. Boundary is stated from the Generator's perspective with WHY-based rationale.
  </done>
</task>

</tasks>

<verification>
1. perceptual-critic.md: Read instruction for PLAYWRIGHT-EVALUATION.md specifying eval-first, resize+eval, console filtering. Console command is `npx playwright-cli console error`.
2. projection-critic.md: Read instruction for PLAYWRIGHT-EVALUATION.md specifying write-and-run, snapshot-as-fallback, console filtering, test healing, round 2+. Round 2+ Test Reuse subsection with decision tree. Console command is `npx playwright-cli console error`.
3. generator.md: Dev test boundary statement with WHY-based rationale. No mentions of evaluation internals beyond the boundary statement.
4. All three files use consistent style (WHY-based rationale, no ALL-CAPS emphasis, npx playwright-cli prefix).
5. No file exceeds its pre-Phase-8 line count by more than 20-30 lines (targeted additions, not restructures).
</verification>

<success_criteria>
- Both critics reference PLAYWRIGHT-EVALUATION.md with section-specific Read instructions
- Both critics use `console error` (filtered) explicitly
- Projection-critic has round 2+ test reuse logic with reuse/heal/regenerate decision tree
- Generator has explicit dev test boundary statement
- All 6 addressed requirements (PLAYWRIGHT-01..02, TOKEN-02..05) are covered
- Agent definitions remain compact (perceptual < 130 lines, projection < 175 lines)
</success_criteria>

<output>
After completion, create `.planning/phases/08-spec-acceptance-criteria-playwright-patterns/08-03-SUMMARY.md`
</output>
