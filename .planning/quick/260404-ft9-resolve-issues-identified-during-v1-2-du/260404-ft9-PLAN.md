---
phase: 260404-ft9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/application-dev/scripts/appdev-cli.mjs
  - plugins/application-dev/agents/perturbation-critic.md
  - plugins/application-dev/agents/projection-critic.md
  - plugins/application-dev/agents/perceptual-critic.md
  - plugins/application-dev/agents/generator.md
  - plugins/application-dev/skills/application-dev/SKILL.md
autonomous: true
requirements: [FT9-1, FT9-2, FT9-3, FT9-4, FT9-5, FT9-6]

must_haves:
  truths:
    - "Static-serve does not spawn duplicate servers when called concurrently by multiple critics"
    - "Critics write output files to the correct non-nested path evaluation/round-N/{critic}/"
    - "Projection critic does not hallucinate --test-dir CLI flag"
    - "Generator classifies website prompts as static HTML/CSS/JS without frameworks"
    - "Generator downloads images locally during generation instead of using external URLs"
    - "Generator produces project-appropriate favicon, not framework defaults"
    - "Orchestrator spawns critics sequentially (one at a time)"
    - "Orchestrator commits evaluation artifacts after all critics pass binary checks"
  artifacts:
    - path: "plugins/application-dev/scripts/appdev-cli.mjs"
      provides: "TOCTOU-safe static-serve with re-read after mutex acquisition"
      contains: "Re-read state after acquiring mutex"
    - path: "plugins/application-dev/agents/perturbation-critic.md"
      provides: "Path construction guardrail with negative example"
      contains: "evaluation/round-1/perturbation/evaluation/round-1/perturbation/"
    - path: "plugins/application-dev/agents/projection-critic.md"
      provides: "Negative instruction for --test-dir flag + path guardrail"
      contains: "Do NOT pass --test-dir"
    - path: "plugins/application-dev/agents/perceptual-critic.md"
      provides: "Path construction guardrail with negative example"
      contains: "evaluation/round-1/perceptual/evaluation/round-1/perceptual/"
    - path: "plugins/application-dev/agents/generator.md"
      provides: "Project-type classification heuristic, image bundling, favicon instructions"
      contains: "Project-Type Classification"
    - path: "plugins/application-dev/skills/application-dev/SKILL.md"
      provides: "Sequential critic spawning, git commit checkpoint, platform bug context note"
      contains: "Spawn critics sequentially"
  key_links:
    - from: "plugins/application-dev/scripts/appdev-cli.mjs"
      to: ".appdev-state.json"
      via: "readState() called inside mutex, after lock acquisition"
      pattern: "readState.*after.*mutex"
    - from: "plugins/application-dev/skills/application-dev/SKILL.md"
      to: "evaluation/round-N/"
      via: "git add + git commit after binary checks pass"
      pattern: "git add evaluation/round-N"
---

<objective>
Fix 6 issues identified during the v1.2 Dutch art museum website test. Changes
span the CLI (TOCTOU race), 3 critic agents (path guardrails, --test-dir
negative instruction), Generator (project-type heuristic, image bundling,
favicon), and Orchestrator SKILL.md (sequential critics, commit checkpoint,
platform bug notes).

Purpose: Prevent duplicate servers, path nesting bugs, hallucinated CLI flags,
inappropriate framework choices for content websites, external image dependency,
and improve evaluation resilience against Claude Code platform bugs.

Output: Updated appdev-cli.mjs, 3 critic .md files, generator.md, SKILL.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260404-ft9-resolve-issues-identified-during-v1-2-du/260404-ft9-CONTEXT.md
@.planning/quick/260404-ft9-resolve-issues-identified-during-v1-2-du/260404-ft9-RESEARCH.md
@.planning/quick/260404-ft9-resolve-issues-identified-during-v1-2-du/260404-ft9-RESEARCH-claude-bugs.md
@plugins/application-dev/scripts/appdev-cli.mjs
@plugins/application-dev/agents/generator.md
@plugins/application-dev/agents/perturbation-critic.md
@plugins/application-dev/agents/projection-critic.md
@plugins/application-dev/agents/perceptual-critic.md
@plugins/application-dev/skills/application-dev/SKILL.md

<interfaces>
<!-- Key structure the executor needs. Extracted from source files. -->

From appdev-cli.mjs cmdStaticServe (lines 901-1042):
- Line 928: `var state = readState();` -- TOCTOU: this read happens BEFORE the mutex
- Lines 935-949: Idempotent check loop over `state.servers[]`
- Lines 959-985: Mutex acquisition via `mkdirSync(lockDir)` poll loop
- Line 992: `try {` block -- this is where the re-read must be inserted
- Lines 994-1017: Port finding, server spawning, state writing
- Line 1037: `rmdirSync(lockDir)` -- mutex release in finally

From SKILL.md Evaluation Phase (lines 257-298):
- Lines 267-273: Parallel critic spawning (3 Agent calls)
- Lines 279-283: Binary checks (3 ls calls)
- Lines 288-293: compile-evaluation call
- No git commit between binary checks and compile-evaluation (gap to fill)

From generator.md Tech Stack Selection (lines 42-63):
- Lines 44-48: Generic judgment criteria (insert project-type classification before this)
- Lines 50-56: Technology-specific and free-choice blocks
- Lines 57-59: Vite+ default block

From generator.md Phase 4 Step 7 (lines 149-162):
- Asset sourcing section -- image bundling instruction goes above or within this
- Lines 157-162: Asset sourcing approaches list

From critic agents Write Restriction sections:
- perturbation-critic.md line 29: "Write ONLY to `evaluation/round-N/perturbation/`"
- projection-critic.md line 29: "Write ONLY to `evaluation/round-N/projection/`"
- perceptual-critic.md line 29: "Write ONLY to `evaluation/round-N/perceptual/`"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix static-serve TOCTOU race and harden critic path construction</name>
  <files>
    plugins/application-dev/scripts/appdev-cli.mjs
    plugins/application-dev/agents/perturbation-critic.md
    plugins/application-dev/agents/projection-critic.md
    plugins/application-dev/agents/perceptual-critic.md
  </files>
  <action>
**appdev-cli.mjs -- TOCTOU fix (lines 992-993):**

Inside the `try {` block at line 992 (after mutex is acquired, before `findAvailablePort`), insert a re-read of state and a second idempotent check. This is the double-checked locking pattern.

Insert this block immediately after `try {` (line 992) and before the `// Find available port` comment (line 993):

```javascript
    // Re-read state after acquiring mutex to prevent TOCTOU race.
    // Without this, concurrent critics all pass the pre-lock idempotent check
    // with stale state, then each spawns a separate server inside the lock.
    state = readState();

    if (!state.servers) {
      state.servers = [];
    }

    for (var j = 0; j < state.servers.length; j++) {
      var dup = state.servers[j];

      if (dup.dir === dir && isPidAlive(dup.pid)) {
        try { rmdirSync(lockDir); } catch (e) {}
        output({ server: dup, reused: true });

        return;
      }

      if (dup.dir === dir) {
        state.servers.splice(j, 1);
        j--;
      }
    }
```

Use loop variable `j` (not `i`) to avoid shadowing the outer loop variable `i` from the pre-lock check at line 935. The `rmdirSync(lockDir)` releases the mutex before the early return. The `output()` call matches the existing reuse output format at line 939.

**perturbation-critic.md -- Path guardrail:**

After the existing "Write Restriction" section (after line 29), add a new section:

```markdown
## Path Construction Guardrail

Your output directory for each round is `evaluation/round-N/perturbation/`
where N is the round number from the orchestrator's prompt. Before writing ANY
file, verify the path does NOT repeat `evaluation/round-` anywhere.

Bad:  evaluation/round-1/perturbation/evaluation/round-1/perturbation/summary.json
Good: evaluation/round-1/perturbation/summary.json

This doubled-path bug occurs when you prepend the output directory to a filename
that already contains the full relative path. Always construct paths from the
base `evaluation/round-N/perturbation/` + just the filename.
```

**projection-critic.md -- Path guardrail + --test-dir negative instruction:**

After the existing "Write Restriction" section (after line 29), add the same path guardrail section as above (substituting `projection` for `perturbation`).

Additionally, in the TEST section after the existing step 4 command template
(line 71: `Run: npx playwright test evaluation/round-N/projection/acceptance-tests.spec.ts --reporter=json`),
add this negative instruction:

```markdown
Do NOT pass `--test-dir` as a CLI flag -- this flag does not exist in Playwright
Test or Vitest. Pass the test file path as a positional argument instead. Example:
`npx playwright test evaluation/round-N/projection/acceptance-tests.spec.ts`
(correct) vs `npx playwright test --test-dir=evaluation/round-N/projection`
(WRONG -- flag does not exist).
```

**perceptual-critic.md -- Path guardrail:**

After the existing "Write Restriction" section (after line 29), add the same path guardrail section (substituting `perceptual` for `perturbation`).

All three critic guardrails follow the same template with the critic name substituted. The negative examples use round-1 as a concrete illustration.
  </action>
  <verify>
    <automated>cd "D:\projects\github\LayZeeDK\lz-development-ai-plugins" && git grep -c "Re-read state after acquiring mutex" -- plugins/application-dev/scripts/appdev-cli.mjs && git grep -c "Path Construction Guardrail" -- plugins/application-dev/agents/perturbation-critic.md plugins/application-dev/agents/projection-critic.md plugins/application-dev/agents/perceptual-critic.md && git grep -c "Do NOT pass.*--test-dir" -- plugins/application-dev/agents/projection-critic.md</automated>
  </verify>
  <done>
    - appdev-cli.mjs: state re-read + idempotent re-check inserted inside mutex (after line 992), using loop var `j`, releasing lock before early return
    - All 3 critic agents: "Path Construction Guardrail" section added after Write Restriction with Bad/Good negative examples
    - projection-critic.md: negative instruction about --test-dir hallucinated flag added in TEST section
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Generator with project-type heuristic, image bundling, and favicon</name>
  <files>plugins/application-dev/agents/generator.md</files>
  <action>
**Project-Type Classification heuristic -- insert at the TOP of the Tech Stack Selection section (after line 42 `## Tech Stack Selection`, before line 44 `Choose the best technology stack`):**

```markdown
### Project-Type Classification

Before selecting a stack, classify the project from SPEC.md content:

**Website** (museum, portfolio, blog, landing page, documentation, gallery,
restaurant, event page): Content-first projects where the primary purpose is
presenting information. Indicators: mostly static content pages, no complex
user-generated data, no real-time features, no authenticated user flows.

**App** (dashboard, task manager, chat, editor, game, tool, calculator):
Interaction-first projects where the primary purpose is user manipulation of
data or state. Indicators: CRUD operations, persistent user data, rich
interactive controls, real-time updates, authentication.

**For websites:** Use static HTML, CSS, and vanilla JavaScript. No framework
runtime, no build step, no SPA router. Serve directly from the filesystem.
The output should be plain `.html`, `.css`, and `.js` files in a flat or
shallow directory structure. When recording the production build state, use
`--build-dir .` (or the directory containing index.html) and `--spa false`.

**For apps:** SPA frameworks (React/Vite, Svelte, etc.) and build tooling
(Vite+) are appropriate. Follow the stack selection guidance below.

When classification is ambiguous, prefer "website" -- content-focused projects
do not benefit from framework overhead, and framework defaults (routing,
favicon, boilerplate) actively harm the product quality for simple sites.
```

Then keep the existing paragraphs starting with "Choose the best technology stack..." as-is -- they apply to app-type projects and to cases where the spec explicitly names a technology.

**Image bundling instruction -- add to Phase 2 Step 3 (line 114, after "Build features incrementally." paragraph):**

After the existing bullet list for Step 3 (after line 119), add:

```markdown
- When a feature requires images (photos, illustrations, icons beyond simple
  SVG), download them during generation and commit to public/ (or the project's
  static asset directory). Use `curl -L -o public/images/<filename> <url>` to
  fetch. Verify the source license permits redistribution (Wikimedia Commons
  public domain, Unsplash, Pexels, etc.). Include attribution in ASSETS.md.
  The generated application MUST NOT fetch images from external URLs at runtime
  -- all images must be bundled locally. Why: external image hosts (Wikimedia,
  Flickr) rate-limit or block automated requests, producing broken images in
  the final product.
```

**Favicon instruction -- add to Phase 1 Step 1, after the `.gitignore` paragraph (after line 95) and before the "First git commit" paragraph (line 97):**

```markdown
**Favicon:** Generate a project-appropriate favicon during setup. Do not use
framework default favicons (Vite logo, React logo, etc.). For websites, create
a simple SVG favicon that reflects the project's identity (e.g., a museum icon
for a museum site, a pen icon for a blog). Write it to the project's public/
root as `favicon.svg` and reference it in the HTML `<head>` with
`<link rel="icon" type="image/svg+xml" href="/favicon.svg">`. For apps, the
same principle applies -- the favicon should match the product, not the
framework.
```
  </action>
  <verify>
    <automated>cd "D:\projects\github\LayZeeDK\lz-development-ai-plugins" && git grep -c "Project-Type Classification" -- plugins/application-dev/agents/generator.md && git grep -c "MUST NOT fetch images from external URLs" -- plugins/application-dev/agents/generator.md && git grep -c "project-appropriate favicon" -- plugins/application-dev/agents/generator.md</automated>
  </verify>
  <done>
    - generator.md: Project-Type Classification subsection added at top of Tech Stack Selection with website vs app heuristic, indicators, and stack guidance for each
    - generator.md: Image bundling instruction added to Step 3 requiring local download + commit to public/, prohibiting runtime external image fetches
    - generator.md: Favicon instruction added to Step 1 requiring project-appropriate SVG favicon, explicitly prohibiting framework default favicons
  </done>
</task>

<task type="auto">
  <name>Task 3: Update Orchestrator for sequential critics, commit checkpoint, and platform bug note</name>
  <files>plugins/application-dev/skills/application-dev/SKILL.md</files>
  <action>
**Sequential critic spawning -- replace parallel block in Evaluation Phase (lines 267-273):**

Replace the existing parallel spawn block:

```
Spawn all three critics in parallel:

\`\`\`
Agent(subagent_type: "application-dev:perceptual-critic", prompt: "This is evaluation round N.")
Agent(subagent_type: "application-dev:projection-critic", prompt: "This is evaluation round N.")
Agent(subagent_type: "application-dev:perturbation-critic", prompt: "This is evaluation round N.")
\`\`\`
```

With:

```markdown
Spawn critics sequentially (one at a time, not in parallel). Sequential
spawning reduces memory pressure and avoids agent-freeze platform bugs in
long-running multi-agent sessions. See platform bug context below.

```
Agent(subagent_type: "application-dev:perceptual-critic", prompt: "This is evaluation round N.")
```

Apply the error recovery pattern. Binary check:
```
Bash(ls evaluation/round-N/perceptual/summary.json 2>/dev/null)
```

```
Agent(subagent_type: "application-dev:projection-critic", prompt: "This is evaluation round N.")
```

Apply the error recovery pattern. Binary check:
```
Bash(ls evaluation/round-N/projection/summary.json 2>/dev/null)
```

```
Agent(subagent_type: "application-dev:perturbation-critic", prompt: "This is evaluation round N.")
```

Apply the error recovery pattern. Binary check:
```
Bash(ls evaluation/round-N/perturbation/summary.json 2>/dev/null)
```

If any summary.json is missing, retry the SPECIFIC critic that failed (not
all) with the same prompt. Counts toward the 2-retry limit per critic.
```

This replaces both the old parallel spawn block (lines 267-273) AND the old separate binary checks block (lines 279-286). Each critic now gets its own spawn + immediate binary check before the next critic is spawned.

**Git commit checkpoint -- insert between binary checks and compile-evaluation (after the new sequential spawn block, before the `**Compile evaluation:**` line at old line 288):**

```markdown
**Commit evaluation artifacts:** After all three summary.json files exist,
commit them to enable crash recovery. If the session dies during
compile-evaluation or afterward, resume-check can detect the committed
summaries and skip directly to compile-evaluation.

```
Bash(git add evaluation/round-N/)
Bash(git commit -m "eval(round-N): critic summaries")
```
```

**Apply the same sequential + commit pattern to the SAFETY_CAP wrap-up round (lines 386-392).** Replace the parallel critic spawn in the wrap-up section:

```
  - Spawn all three critics in parallel:
    \`\`\`
    Agent(subagent_type: "application-dev:perceptual-critic", prompt: "This is evaluation round {N+1}.")
    Agent(subagent_type: "application-dev:projection-critic", prompt: "This is evaluation round {N+1}.")
    Agent(subagent_type: "application-dev:perturbation-critic", prompt: "This is evaluation round {N+1}.")
    \`\`\`
  - Binary checks (all three summary.json files exist)
```

With the same sequential pattern (spawn perceptual, check, spawn projection, check, spawn perturbation, check) and the same git commit checkpoint before compile-evaluation. Use round `{N+1}` in the prompts and paths.

**Platform bug context note -- add a new section after the "Architecture" section (after line 572, at the end of the file):**

```markdown
## Platform Bug Context

The following Claude Code platform bugs affect multi-agent orchestration.
The plugin's architecture already includes workarounds (binary file-exists
checks, sequential critic spawning, commit checkpoints), but awareness of
root causes helps interpret unexpected behavior during long sessions.

- **Agent freeze** (#37521): Agents and subagents can freeze indefinitely on
  Opus 4.6. No timeout mechanism exists. Sequential critic spawning reduces
  exposure. If a critic appears stuck, the session may need to be restarted.
- **Memory leak** (#32304): Multi-agent sessions leak memory aggressively
  (18-92 GB/hour in ArrayBuffers). The commit checkpoint after critics ensures
  evaluation artifacts survive if the process crashes from memory exhaustion.
- **Subagent context exhaustion** (#14867): Subagents have no auto-compact.
  Critics have ~60K token budgets. The write-and-run pattern and summary
  reporters keep critics within budget.
- **Context dump to parent** (#14118): When a subagent exhausts context, its
  full tool log may dump into the parent. Binary file-exists checks avoid
  parsing agent output, limiting blast radius.
- **Output lost after compaction** (#23821): Context compaction in the parent
  can lose subagent return values. Binary file-exists checks re-discover
  completion state. The commit checkpoint adds a second durability layer.

These bugs are tracked upstream. No plugin-level fix is possible for most of
them -- the mitigations above are defense-in-depth.
```
  </action>
  <verify>
    <automated>cd "D:\projects\github\LayZeeDK\lz-development-ai-plugins" && git grep -c "Spawn critics sequentially" -- plugins/application-dev/skills/application-dev/SKILL.md && git grep -c "eval(round-N): critic summaries" -- plugins/application-dev/skills/application-dev/SKILL.md && git grep -c "Platform Bug Context" -- plugins/application-dev/skills/application-dev/SKILL.md</automated>
  </verify>
  <done>
    - SKILL.md: Parallel critic spawning replaced with sequential (one at a time) in both the main evaluation phase and the SAFETY_CAP wrap-up round
    - SKILL.md: Git commit checkpoint (`git add evaluation/round-N/` + `git commit`) inserted after all binary checks pass, before compile-evaluation, in both evaluation code paths
    - SKILL.md: Platform Bug Context section added at end documenting 5 known bugs with issue numbers and explaining how existing mitigations address them
  </done>
</task>

</tasks>

<verification>
After all 3 tasks complete, verify the full set of changes:

1. appdev-cli.mjs: The `cmdStaticServe` function has a `readState()` call INSIDE the `try` block after mutex acquisition (not just the one at line 928 before the mutex)
2. All 3 critic agents have "Path Construction Guardrail" sections with Bad/Good examples
3. projection-critic.md has "--test-dir" negative instruction
4. generator.md has "Project-Type Classification" subsection, image bundling prohibition, and favicon instruction
5. SKILL.md has "Spawn critics sequentially" (not "in parallel"), "git add evaluation/round-N/", and "Platform Bug Context" section
6. No syntax errors in appdev-cli.mjs (the JavaScript must remain valid)
</verification>

<success_criteria>
- Static-serve TOCTOU race eliminated: state re-read after mutex with second idempotent check
- All 3 critics have path guardrails with concrete Bad/Good examples preventing doubled paths
- Projection critic has negative instruction preventing --test-dir hallucination
- Generator classifies website vs app from SPEC.md content and applies appropriate stack guidance
- Generator required to download images locally and produce project-appropriate favicons
- Orchestrator spawns critics sequentially with per-critic binary checks
- Orchestrator commits evaluation artifacts after all critics pass before compile-evaluation
- Platform bugs documented for operator awareness
</success_criteria>

<output>
After completion, create `.planning/quick/260404-ft9-resolve-issues-identified-during-v1-2-du/260404-ft9-SUMMARY.md`
</output>
