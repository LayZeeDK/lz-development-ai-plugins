---
phase: quick-260405-33d
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/application-dev/scripts/appdev-cli.mjs
  - plugins/application-dev/scripts/test-appdev-cli.mjs
  - plugins/application-dev/agents/perceptual-critic.md
  - plugins/application-dev/agents/projection-critic.md
  - plugins/application-dev/agents/perturbation-critic.md
  - plugins/application-dev/agents/generator.md
  - plugins/application-dev/skills/vitest-browser/SKILL.md
  - plugins/application-dev/skills/browser-built-in-ai/SKILL.md
  - plugins/application-dev/skills/playwright-testing/SKILL.md
autonomous: true
requirements: [PATCH2-CONVERGENCE, PATCH2-SCORECAP, PATCH2-CALIBRATION, PATCH2-TESTIMONIAL, PATCH2-BROWSER]

must_haves:
  truths:
    - "PASS exit requires all dimensions scoring 10 (structurally unreachable with score cap)"
    - "Score cap enforced in CLI: round 1 max 8, round 2+ max 9, perfect 10 never achievable"
    - "All three critics have scoring expectations section with round 1 prior, defect quota, and embedded calibration anchor"
    - "Generator has testimonial content policy forbidding fabricated attributions"
    - "Default browser channel is msedge with fallback chain documented"
  artifacts:
    - path: "plugins/application-dev/scripts/appdev-cli.mjs"
      provides: "determineExit with all-10s PASS condition, cmdCompileEvaluation with score cap"
      contains: "scoreCap"
    - path: "plugins/application-dev/scripts/test-appdev-cli.mjs"
      provides: "Updated PASS test, new score cap tests, new determineExit tests"
      contains: "allPerfect"
    - path: "plugins/application-dev/agents/perceptual-critic.md"
      provides: "Scoring Expectations section with Visual Design calibration anchor"
      contains: "Scoring Expectations"
    - path: "plugins/application-dev/agents/projection-critic.md"
      provides: "Scoring Expectations section with Functionality calibration anchor"
      contains: "Scoring Expectations"
    - path: "plugins/application-dev/agents/perturbation-critic.md"
      provides: "Scoring Expectations section with Robustness calibration anchor"
      contains: "Scoring Expectations"
    - path: "plugins/application-dev/agents/generator.md"
      provides: "Testimonial content policy bullet"
      contains: "No fabricated testimonials"
  key_links:
    - from: "plugins/application-dev/scripts/appdev-cli.mjs (cmdCompileEvaluation)"
      to: "plugins/application-dev/scripts/appdev-cli.mjs (computeVerdict)"
      via: "score cap applied BEFORE computeVerdict call"
      pattern: "scoreCap.*computeVerdict"
    - from: "plugins/application-dev/scripts/appdev-cli.mjs (determineExit)"
      to: "DIMENSIONS constant"
      via: "iterates DIMENSIONS checking all scores === 10"
      pattern: "DIMENSIONS.*10"
    - from: "plugins/application-dev/agents/*-critic.md"
      to: "plugins/application-dev/skills/application-dev/references/evaluator/SCORING-CALIBRATION.md"
      via: "embedded calibration scenario copied from SCORING-CALIBRATION.md"
      pattern: "Below Threshold"
---

<objective>
Fix five issues identified during the Consensus.dk website test of application-dev v1.2 patch.2: (1) convergence logic allows premature PASS exit on lenient round 1 scores, (2) no score cap enforcement in CLI, (3) critics lack calibration anchors and defect quotas, (4) Generator fabricates testimonials, (5) browser channel defaults to bundled Chromium instead of Edge.

Purpose: Prevent the premature-exit + lenient-scoring pattern that produced a bland, imageless site passing after just 1 round. The convergence + score cap changes are structural (CLI-enforced), while critic calibration is prompt-level reinforcement. Testimonial and browser channel are ancillary fixes.

Output: Updated CLI with score cap and convergence logic, all 3 critic agents with calibration sections, generator with testimonial policy, browser channel defaults in skills.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260405-33d-consensus-dk-website-test-for-applicatio/260405-33d-CONTEXT.md
@.planning/quick/260405-33d-consensus-dk-website-test-for-applicatio/260405-33d-RESEARCH.md

@plugins/application-dev/scripts/appdev-cli.mjs
@plugins/application-dev/scripts/test-appdev-cli.mjs
@plugins/application-dev/agents/perceptual-critic.md
@plugins/application-dev/agents/projection-critic.md
@plugins/application-dev/agents/perturbation-critic.md
@plugins/application-dev/agents/generator.md
@plugins/application-dev/skills/application-dev/references/evaluator/SCORING-CALIBRATION.md

<interfaces>
<!-- Key constants and functions the executor needs from appdev-cli.mjs -->

From appdev-cli.mjs line 14:
```javascript
const DIMENSIONS = [
  { name: "Product Depth", key: "product_depth", threshold: 7 },
  { name: "Functionality", key: "functionality", threshold: 7 },
  { name: "Visual Design", key: "visual_design", threshold: 6 },
  { name: "Robustness", key: "robustness", threshold: 6 },
];
```

From appdev-cli.mjs line 150:
```javascript
function computeVerdict(scores) {
  for (let i = 0; i < DIMENSIONS.length; i++) {
    var dim = DIMENSIONS[i];
    if (scores[dim.key] < dim.threshold) { return "FAIL"; }
  }
  return "PASS";
}
```

From appdev-cli.mjs line 366:
```javascript
function determineExit(rounds, escalation, maxRounds) {
  const current = rounds[rounds.length - 1];
  // PASS: all criteria meet thresholds
  if (current.verdict === "PASS") {
    return { exit_condition: "PASS", should_continue: false };
  }
  // PLATEAU, REGRESSION, SAFETY_CAP...
}
```

From appdev-cli.mjs line 1318 (cmdCompileEvaluation):
- `var round = parseInt(args.round, 10);` (line 1325)
- `allScores` object built at lines 1390-1403
- `var verdict = computeVerdict(allScores);` at line 1405

From test-appdev-cli.mjs:
- `makeReport(pd, fn, vd, rb)` helper creates EVALUATION.md content (line 65)
- `setupRound(roundNum, perceptual, projection, perturbation)` creates mock critic summaries (line 311)
- `runCLI(cmd, opts)` executes CLI commands (line 24)
- Existing PASS test at line 2067 expects 7/7/6/6 -> exit_condition: "PASS" (MUST be updated)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CLI convergence logic and score cap enforcement</name>
  <files>plugins/application-dev/scripts/appdev-cli.mjs, plugins/application-dev/scripts/test-appdev-cli.mjs</files>
  <behavior>
    - Test: determineExit with all-10s scores returns exit_condition "PASS"
    - Test: determineExit with 7/7/6/6 (threshold-met, not all-10s) does NOT return exit_condition "PASS" -- should_continue is true
    - Test: determineExit with 9/9/9/9 (high but not perfect) does NOT return "PASS"
    - Test: compile-evaluation round 1 caps all scores at 8 (critic reports 10/10/10/10, output shows 8/8/8/8)
    - Test: compile-evaluation round 2 caps all scores at 9 (critic reports 10/10/10/10, output shows 9/9/9/9)
    - Test: compile-evaluation round 1 does not cap scores below ceiling (critic reports 5/6/4/3, output unchanged)
    - Test: Product Depth is also capped (critic reports PD=10 round 1, output shows PD=8)
    - Test: existing "PASS verdict still uses raw scores" test updated -- 7/7/6/6 no longer triggers PASS exit (verdict field still says "PASS" from computeVerdict, but exit_condition is null, should_continue is true)
  </behavior>
  <action>
  **Part A: Update determineExit (line 366-397 of appdev-cli.mjs)**

  Replace the PASS condition from `current.verdict === "PASS"` to require ALL dimensions scoring exactly 10. The function receives `rounds` array where each entry has `.scores` object with dimension keys. Iterate `DIMENSIONS` and check `current.scores[DIMENSIONS[i].key] === 10` for all. Only return `{ exit_condition: "PASS", should_continue: false }` if all are 10.

  Keep the existing PLATEAU, REGRESSION, SAFETY_CAP logic unchanged. The `current.verdict` field is still set by `computeVerdict` in `cmdRoundComplete` -- it still means "all thresholds met" and is useful diagnostic info. Do NOT change `computeVerdict` itself.

  **Part B: Add score cap in cmdCompileEvaluation (after line 1403, before line 1405)**

  After `allScores` is fully built (all dimensions populated from critic summaries + Product Depth from `computeProductDepth`) but BEFORE calling `computeVerdict(allScores)`:

  ```javascript
  // One-sided label smoothing: cap scores by round (Salimans et al. 2016)
  // Round 1: max 8. Round 2+: max 9. Perfect 10 never achievable.
  var scoreCap = round === 1 ? 8 : 9;
  for (var ci = 0; ci < DIMENSIONS.length; ci++) {
    var capKey = DIMENSIONS[ci].key;
    if (allScores[capKey] > scoreCap) {
      allScores[capKey] = scoreCap;
    }
  }
  ```

  The `round` variable already exists at line 1325 of `cmdCompileEvaluation`. The cap is applied BEFORE `computeVerdict` so the verdict sees capped scores (pitfall 1 from research). Product Depth flows through `allScores[dim.key]` at line 1396, so it is also capped by the same loop -- no separate pdResult capping needed since allScores already has the PD value by this point.

  **Part C: Update existing test + add new tests (test-appdev-cli.mjs)**

  1. Update the "PASS verdict still uses raw scores" test (line 2067): Change assertions so that 7/7/6/6 still produces `verdict: "PASS"` (computeVerdict is unchanged) but `exit_condition` is null and `should_continue` is true (determineExit no longer triggers PASS on threshold-met).

  2. Add new test: "PASS exit requires all dimensions scoring 10" -- use makeReport(10, 10, 10, 10), run round-complete, assert exit_condition is "PASS".

  3. Add new test: "Threshold-met but not all-10s does not trigger PASS exit" -- use makeReport(9, 9, 9, 9), run round-complete for round 1, assert exit_condition is null and should_continue is true.

  4. Add new tests in compile-evaluation describe block: "Score cap: round 1 caps at 8" -- set up critic summaries with score 10 each, run compile-evaluation --round 1, read the produced EVALUATION.md, extract scores and verify all are 8. "Score cap: round 2 caps at 9" -- same pattern with --round 2, verify capped at 9. "Score cap: below-cap scores unchanged" -- set up summaries with score 5, run compile-evaluation --round 1, verify scores are still 5.

  Use the existing `setupRound` helper with `makePerceptualSummary`, `makeProjectionSummary`, `makePerturbationSummary` for compile-evaluation tests. Use `makeReport` + `runCLI("round-complete ...")` for determineExit tests.
  </action>
  <verify>
    <automated>node plugins/application-dev/scripts/test-appdev-cli.mjs</automated>
  </verify>
  <done>
  - determineExit returns PASS only when ALL dimensions are 10
  - 7/7/6/6 and 9/9/9/9 do NOT trigger PASS exit (fall through to continue/escalation)
  - compile-evaluation caps scores at 8 for round 1, 9 for round 2+
  - Score cap applied before computeVerdict (verdict sees capped scores)
  - All existing tests still pass, plus 5+ new tests covering score cap and convergence
  </done>
</task>

<task type="auto">
  <name>Task 2: Critic calibration anchors, defect quota, and score cap expectations</name>
  <files>plugins/application-dev/agents/perceptual-critic.md, plugins/application-dev/agents/projection-critic.md, plugins/application-dev/agents/perturbation-critic.md</files>
  <action>
  Add a new `## Scoring Expectations` section to each of the three critic agent .md files. Place it AFTER the `## Path Construction Guardrail` section and BEFORE `## Step 0: Start Evaluation Server`. This groups all constraints together before the workflow begins.

  **Common block for all three critics:**

  ```markdown
  ## Scoring Expectations

  First-generation applications typically score 3-5. A round 1 score above 6
  requires explicit comparison against the calibration scenario below explaining
  why this application exceeds the threshold example.

  Minimum 3 findings before assigning any score. Round 1 applications always
  have issues -- if you cannot find 3, look harder.

  Score cap: round 1 scores cannot exceed 8. Round 2+ scores cannot exceed 9.
  Perfect 10 is never achievable. The CLI enforces this structurally, but
  apply it in your own scoring as well.
  ```

  **Per-critic calibration anchor (append after the common block):**

  For **perceptual-critic.md**, embed the "Below Threshold: 4/10" Visual Design scenario from SCORING-CALIBRATION.md (the music app with default Material UI, no design language match). Copy verbatim, indented as a blockquote:

  ```markdown
  ### Calibration Anchor: Visual Design 4/10

  > A music streaming app specified with a "dark, immersive, neon-accented"
  > design language. The app uses a white background with default Material UI
  > components. Typography is Roboto at default sizes. No custom color palette.
  > Cards have default border-radius and box-shadow. The hero section has a stock
  > gradient. No visual connection to the music/streaming domain.
  >
  > Score: 4/10 -- No design language match. AI-slop patterns dominate: default
  > framework styling, generic fonts, no domain identity.

  If the application you are evaluating looks similar to this scenario, score
  accordingly. If you score higher than 6 on round 1, explain specifically what
  distinguishes it from the anchor above.
  ```

  For **projection-critic.md**, embed the "Below Threshold: 5/10" Functionality scenario (e-commerce app with 2 Critical bugs):

  ```markdown
  ### Calibration Anchor: Functionality 5/10

  > An e-commerce app where the shopping cart loses items on page refresh
  > (Critical bug -- data loss). The checkout form submits but silently fails to
  > create an order (Critical bug -- core workflow broken). Product search returns
  > results but clicking a product sometimes loads the wrong detail page (Major
  > bug). Three other pages have JavaScript errors that block interaction.
  >
  > Score: 5/10 -- 2 Critical bugs trigger the ceiling (any Critical bug = max 5).
  > Core workflow is broken at multiple points.

  If the application you are evaluating has Critical bugs, score accordingly.
  If you score higher than 6 on round 1, explain specifically what distinguishes
  it from the anchor above.
  ```

  For **perturbation-critic.md**, embed the "Below Threshold: 5/10" Robustness scenario (recipe app that crashes on perturbation):

  ```markdown
  ### Calibration Anchor: Robustness 5/10

  > A recipe app crashes when the search field receives a 500-character input.
  > Rapid-clicking "Add to favorites" creates duplicate entries and eventually
  > freezes the UI. Navigating back while an image loads produces a blank screen
  > requiring full page reload. Console shows 3+ uncaught exceptions under
  > normal-speed usage. Undamped -- perturbations cause the system to diverge.
  >
  > Score: 5/10 -- Undamped + insufficient variety. The app cannot survive
  > perturbation without crashing.

  If the application you are evaluating crashes under perturbation, score
  accordingly. If you score higher than 6 on round 1, explain specifically what
  distinguishes it from the anchor above.
  ```
  </action>
  <verify>
    <automated>git grep -c "Scoring Expectations" -- "plugins/application-dev/agents/perceptual-critic.md" "plugins/application-dev/agents/projection-critic.md" "plugins/application-dev/agents/perturbation-critic.md" | rg ":1$" | wc -l</automated>
  </verify>
  <done>
  - All three critic agent files contain a "## Scoring Expectations" section
  - Each section includes: round 1 score prior (3-5 typical), defect quota (minimum 3 findings), score cap mention (8/9/never-10)
  - Each critic has its own calibration anchor: perceptual=Visual Design 4/10, projection=Functionality 5/10, perturbation=Robustness 5/10
  - Sections are placed between Path Construction Guardrail and Step 0
  </done>
</task>

<task type="auto">
  <name>Task 3: Generator testimonial policy and browser channel defaults</name>
  <files>plugins/application-dev/agents/generator.md, plugins/application-dev/skills/vitest-browser/SKILL.md, plugins/application-dev/skills/browser-built-in-ai/SKILL.md, plugins/application-dev/skills/playwright-testing/SKILL.md</files>
  <action>
  **Part A: Testimonial policy in generator.md**

  In the `## Quality Standards` section (around line 314, after the "No fabricated URLs" bullet), add a new bullet:

  ```markdown
  - **No fabricated testimonials.** If SPEC.md includes testimonials sourced from the real website, use those verbatim. If no real testimonials are available, use clearly-marked placeholders (e.g., "[Placeholder - Client Name]", "-- A satisfied customer") with generic roles. Never name real people or real customer companies in fabricated testimonials. Why: fabricated testimonials attributing fake quotes to real entities is a legal and reputational hazard.
  ```

  This extends the entity research protocol from planner.md (added in 260405-0az) to the Generator as a second enforcement layer.

  **Part B: Browser channel default in vitest-browser/SKILL.md**

  In section "3. Branded browser channels" (line 101), change the default recommendation. The current example at line 71 uses `channel: 'chrome'`. Update:
  - Line 71: Change `channel: 'chrome'` to `channel: 'msedge'` and update the comment to `// branded channel -- Edge ships Phi-4-mini for browser AI APIs`
  - Line 119: Change `launchOptions: { channel: 'chrome' },` to `launchOptions: { channel: 'msedge' },`
  - Line 131: Change `process.env.BROWSER_CHANNEL || 'chrome'` to `process.env.BROWSER_CHANNEL || 'msedge'`
  - Line 168: Change `channel: 'chrome'` to `channel: 'msedge'`
  - Update the table at lines 109-112: reorder so msedge is listed first with a note "(default)" after its description
  - At line 123-126, update the "Fallback when branded channel is unavailable" subsection: document the fallback chain as msedge -> chrome -> bundled chromium. When msedge is unavailable, try `BROWSER_CHANNEL=chrome`. If neither branded browser is installed, use `BROWSER_CHANNEL=bundled` (Playwright's bundled Chromium -- AI API tests will not work).

  **Part C: Browser channel in browser-built-in-ai/SKILL.md**

  After the opening paragraph (line 18, "Types: `npm install ...`"), add a note:

  ```markdown
  **Default browser channel:** `msedge` (Phi-4-mini, 3.8B params -- more capable
  than Chrome's Gemini Nano for most tasks). When configuring Playwright or Vitest
  Browser Mode for testing browser AI features, prefer `channel: 'msedge'`. See
  `vitest-browser/SKILL.md` section 3 for fallback chain.
  ```

  **Part D: Browser channel in playwright-testing/SKILL.md**

  In the example config at line 126-131 (the projects array), change the project from `name: 'chromium'` with `devices['Desktop Chrome']` to include a channel:

  ```javascript
  projects: [
    {
      name: 'edge',
      use: { ...devices['Desktop Edge'], channel: 'msedge' },
    },
  ],
  ```

  Add a note after the config example: "Default channel is `msedge` for Phi-4-mini AI API access. If Edge is unavailable, fall back to `channel: 'chrome'` (Gemini Nano), then omit `channel` for bundled Chromium (no AI API access)."
  </action>
  <verify>
    <automated>git grep -l "No fabricated testimonials" -- "plugins/application-dev/agents/generator.md" && git grep -c "msedge" -- "plugins/application-dev/skills/vitest-browser/SKILL.md" "plugins/application-dev/skills/browser-built-in-ai/SKILL.md" "plugins/application-dev/skills/playwright-testing/SKILL.md"</automated>
  </verify>
  <done>
  - generator.md contains "No fabricated testimonials" bullet in Quality Standards
  - vitest-browser/SKILL.md defaults to msedge channel (not chrome) with fallback chain documented
  - browser-built-in-ai/SKILL.md recommends msedge as default channel
  - playwright-testing/SKILL.md example config uses msedge channel with fallback note
  - All references to `channel: 'chrome'` as default are replaced with `channel: 'msedge'`
  </done>
</task>

</tasks>

<verification>
1. `node plugins/application-dev/scripts/test-appdev-cli.mjs` -- all tests pass including new convergence and score cap tests
2. `git grep "Scoring Expectations" -- "plugins/application-dev/agents/"` -- returns 3 matches (one per critic)
3. `git grep "No fabricated testimonials" -- "plugins/application-dev/agents/generator.md"` -- returns 1 match
4. `git grep "scoreCap" -- "plugins/application-dev/scripts/appdev-cli.mjs"` -- returns matches in cmdCompileEvaluation
5. `git grep "msedge" -- "plugins/application-dev/skills/"` -- returns matches in vitest-browser, browser-built-in-ai, playwright-testing
</verification>

<success_criteria>
- All existing tests pass plus 5+ new tests for convergence and score cap
- PASS exit requires all-10s (structurally unreachable with score cap making 10 never achievable)
- Score cap enforced in CLI aggregator, not just in prompts (Beer's algedonic signal principle)
- Critics have calibration anchors from SCORING-CALIBRATION.md embedded directly in their agent definitions
- Generator cannot fabricate testimonials -- SPEC-grounded or placeholders only
- Default browser channel is msedge across Generator (Vitest, Playwright) and Critics
</success_criteria>

<output>
After completion, create `.planning/quick/260405-33d-consensus-dk-website-test-for-applicatio/260405-33d-SUMMARY.md`
</output>
