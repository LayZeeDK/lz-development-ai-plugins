---
phase: quick-260405-0az
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/application-dev/agents/planner.md
  - plugins/application-dev/skills/application-dev/references/evaluator/AI-SLOP-CHECKLIST.md
autonomous: true
requirements: [HALLUCINATION-GUARD]

must_haves:
  truths:
    - "Planner agent has WebFetch in its tools array"
    - "Planner agent instructions require entity research before spec writing"
    - "Planner agent instructions forbid fabricating facts about real entities"
    - "AI Slop Checklist flags fabricated entity facts as content slop"
  artifacts:
    - path: "plugins/application-dev/agents/planner.md"
      provides: "Entity research rules and WebFetch tool access"
      contains: "WebFetch"
    - path: "plugins/application-dev/skills/application-dev/references/evaluator/AI-SLOP-CHECKLIST.md"
      provides: "Fabricated entity detection for critics"
      contains: "fabricated"
  key_links:
    - from: "plugins/application-dev/agents/planner.md"
      to: "WebFetch tool"
      via: "tools frontmatter array"
      pattern: 'tools:.*WebFetch'
    - from: "plugins/application-dev/agents/planner.md"
      to: "SPEC.md output"
      via: "Entity Research section guards spec content"
      pattern: "Entity Research"
    - from: "plugins/application-dev/skills/application-dev/references/evaluator/AI-SLOP-CHECKLIST.md"
      to: "critic evaluation"
      via: "Content Slop section includes fabricated entity check"
      pattern: "fabricated"
---

<objective>
Prevent the Planner agent from hallucinating details about real-world entities
(companies, products, websites) referenced in user prompts.

Purpose: When a prompt references a real entity (e.g., "Create a website for
Consensus that reimagines consensus.dk"), the Planner currently fabricates the
entity's details because it lacks web access and has no instruction to avoid
fabrication. This causes hallucinated specs that propagate uncorrected through
the entire generation/evaluation pipeline.

Output: Updated planner.md with WebFetch tool and entity research rules; updated
AI-SLOP-CHECKLIST.md with fabricated-entity detection for critics.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@plugins/application-dev/agents/planner.md
@plugins/application-dev/skills/application-dev/references/evaluator/AI-SLOP-CHECKLIST.md
</context>

<interfaces>
<!-- Current planner.md structure (relevant sections): -->

Line 25, tools frontmatter:
```yaml
tools: ["Read", "Write"]
```

Line 28-35, mission section:
```markdown
## Your Mission
...
**Before writing the Visual Design Language section**, read the design principles...
```

Line 37-49, file write requirements then critical rules:
```markdown
## File Write Requirements
...
## Critical Rules
1. **Focus on product context...**
2. **Do not specify a tech stack...**
3. **Be ambitious about scope...**
4. **Weave AI features throughout...**
5. **Create a distinctive visual design language...**
6. **You may only write `SPEC.md`...**
```

<!-- Current AI-SLOP-CHECKLIST.md Content Slop section (lines 26-30): -->
```markdown
## Content Slop

- Headlines that say nothing specific: "Build the future", "Your all-in-one platform", "Scale without limits"
- Lorem ipsum or "Coming soon" placeholders
- Stock photo aesthetic (glossy, over-lit, perfect) rather than intentional photography
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add WebFetch tool and Entity Research section to planner.md</name>
  <files>plugins/application-dev/agents/planner.md</files>
  <action>
Three changes to planner.md:

(A) In the YAML frontmatter, change the tools line from:
  tools: ["Read", "Write"]
to:
  tools: ["Read", "Write", "WebFetch"]

(B) Add a new section "## Entity Research" between the "## Your Mission" section
(which ends with the design principles paragraph) and "## File Write Requirements".
Content of the new section:

```markdown
## Entity Research

When the user's prompt references real-world entities -- named companies,
products, organizations, or URLs -- you MUST research them before writing the
spec. Do not rely on your training data for entity facts.

**Detection:** Look for company names, brand names, product names, domain names,
and URLs in the prompt. Any proper noun that refers to a real-world organization
or product triggers this protocol.

**Research steps:**

1. For each URL in the prompt, fetch it with WebFetch to retrieve the entity's
   actual content, services, products, and identity.
2. For named entities without a URL, attempt `https://<entity-name>.com` or a
   reasonable domain variant. If the fetch fails, do not guess -- proceed with
   step 3.
3. Use fetched content as the authoritative source for the entity's identity,
   offerings, and domain. The spec's product overview, features, and terminology
   must align with the real entity.
4. If all fetches fail: use ONLY facts explicitly stated in the user's prompt.
   Mark any assumptions with "[ASSUMED -- not verified]" in the spec so
   downstream agents can see which claims are unverified.
5. Never invent services, products, client lists, or capabilities for a real
   entity. If you do not know what they do and cannot fetch it, say so.
```

(C) Add Critical Rule #7 after the existing rule #6. Content:

```markdown
7. **Never fabricate facts about real entities.** If the prompt references a real
   company, product, or website, follow the Entity Research protocol above.
   Ground the spec in fetched content or prompt-stated facts only. If research
   fails, flag uncertainty -- never fill gaps with invented details.
```
  </action>
  <verify>
    <automated>git grep -c "WebFetch" plugins/application-dev/agents/planner.md && git grep -c "Entity Research" plugins/application-dev/agents/planner.md && git grep -c "Never fabricate" plugins/application-dev/agents/planner.md</automated>
  </verify>
  <done>
planner.md has WebFetch in tools array, an "Entity Research" section with the
5-step research protocol, and Critical Rule #7 forbidding entity fabrication.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add fabricated-entity detection to AI Slop Checklist</name>
  <files>plugins/application-dev/skills/application-dev/references/evaluator/AI-SLOP-CHECKLIST.md</files>
  <action>
Add a new bullet to the "## Content Slop" section, after the existing three
bullets. New bullet:

```markdown
- Fabricated facts about named real-world entities: invented services, hallucinated client lists, imaginary company capabilities, or made-up product offerings that do not match the entity's actual business
```

This gives critics a concrete pattern to flag if hallucinated entity content
makes it past the Planner's guardrails.
  </action>
  <verify>
    <automated>git grep -c "Fabricated facts about named" plugins/application-dev/skills/application-dev/references/evaluator/AI-SLOP-CHECKLIST.md</automated>
  </verify>
  <done>
AI-SLOP-CHECKLIST.md Content Slop section includes a bullet for fabricated
entity facts, enabling critics to catch hallucinated company details.
  </done>
</task>

</tasks>

<verification>
After both tasks complete, verify the full change set:

1. planner.md frontmatter contains `WebFetch` in tools array
2. planner.md contains "## Entity Research" section with fetch protocol
3. planner.md contains Critical Rule #7 about fabrication
4. AI-SLOP-CHECKLIST.md Content Slop section has fabricated-entity bullet
5. No other files were modified
6. Both files parse correctly (no broken markdown structure)
</verification>

<success_criteria>
- The Planner agent can fetch URLs via WebFetch when spawned by the orchestrator
- The Planner agent has explicit instructions to research real entities before writing specs
- The Planner agent has a clear fallback protocol when fetches fail (use only prompt facts, flag assumptions)
- The Planner agent has a critical rule forbidding entity fabrication
- The AI Slop Checklist gives critics a pattern to detect fabricated entity content
</success_criteria>

<output>
After completion, create `.planning/quick/260405-0az-prevent-hallucinations-for-real-entities/260405-0az-SUMMARY.md`
</output>
