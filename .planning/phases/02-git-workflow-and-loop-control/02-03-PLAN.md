---
phase: 02-git-workflow-and-loop-control
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - plugins/application-dev/skills/application-dev/SKILL.md
  - docs/ARCHITECTURE.md
autonomous: true
requirements: [GIT-01, GIT-05, LOOP-02, LOOP-03]

must_haves:
  truths:
    - "Orchestrator initializes a git workspace (Step 0.5) before spawning any agents: git init, npm init, install @playwright/cli, seed .gitignore, initial commit"
    - "Orchestrator commits SPEC.md after Planner completes and creates appdev/planning-complete tag"
    - "Orchestrator creates appdev/round-N annotated tags after each round"
    - "Orchestrator loop runs up to 10 rounds with convergence detection via appdev-cli round-complete"
    - "Orchestrator dispatches PASS, PLATEAU, REGRESSION, SAFETY_CAP exit conditions with correct behaviors"
    - "REGRESSION triggers git reset --hard to best round's tag"
    - "SAFETY_CAP triggers one extra wrap-up round (round 11) beyond the cap"
    - "Orchestrator allowed-tools includes git and npm Bash patterns plus appdev-cli"
    - "docs/ARCHITECTURE.md documents GAN architecture, cybernetics inspirations, and key design decisions"
  artifacts:
    - path: "plugins/application-dev/skills/application-dev/SKILL.md"
      provides: "Orchestrator workflow with git workspace, convergence loop, tagging, rollback"
      contains: "appdev-cli"
    - path: "docs/ARCHITECTURE.md"
      provides: "GAN architecture documentation with escalation vocabulary and design rationale"
      contains: "Escalation"
  key_links:
    - from: "plugins/application-dev/skills/application-dev/SKILL.md"
      to: "plugins/application-dev/scripts/appdev-cli.mjs"
      via: "Bash(node *appdev-cli*) calls in loop"
      pattern: "appdev-cli.*round-complete"
    - from: "plugins/application-dev/skills/application-dev/SKILL.md"
      to: "evaluation/round-N/EVALUATION.md"
      via: "report path passed to appdev-cli round-complete"
      pattern: "evaluation/round-.*EVALUATION"
    - from: "plugins/application-dev/skills/application-dev/SKILL.md"
      to: "git tags appdev/*"
      via: "git tag -a commands after each milestone"
      pattern: "git tag -a appdev/"
---

<objective>
Rewrite the orchestrator SKILL.md with git workspace setup (Step 0.5), score-based convergence loop using appdev-cli, milestone tagging, REGRESSION rollback, SAFETY_CAP wrap-up, GAN ubiquitous language, and updated allowed-tools. Also create docs/ARCHITECTURE.md documenting the GAN architecture, cybernetics inspirations, and key design decisions from Phases 1-2.

Purpose: The orchestrator is the workflow coordinator. It must set up the git workspace, delegate convergence decisions to appdev-cli, tag milestones for traceability and rollback, and handle all four exit conditions correctly. The ARCHITECTURE.md documents the conceptual foundations (GAN, cybernetics) that are implemented in the code.

Output: Updated SKILL.md and new docs/ARCHITECTURE.md
</objective>

<execution_context>
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-git-workflow-and-loop-control/02-CONTEXT.md
@.planning/phases/02-git-workflow-and-loop-control/02-RESEARCH.md
@.planning/phases/01-orchestrator-integrity/01-01-SUMMARY.md
@.planning/phases/01-orchestrator-integrity/01-02-SUMMARY.md

<interfaces>
<!-- Current SKILL.md structure (from Phase 1, 311 lines) -->
From plugins/application-dev/skills/application-dev/SKILL.md:
```yaml
# Frontmatter
allowed-tools: Agent Read Write Bash(node *appdev-state*)

# Sections:
# - Architecture (GAN overview, enforcement model)
# - Rules (6 rules)
# - Workflow: Step 0 (Resume Check), Step 1 (Plan), Step 2 (Build/QA Loop), Step 3 (Summary)
# - Error Recovery
# - Agent Prompt Protocol
# - File-Based Communication
```

<!-- Phase 2 allowed-tools (from CONTEXT.md) -->
```
Agent Read Write Bash(node *appdev-cli*) Bash(git init*) Bash(git rev-parse *) Bash(git add *) Bash(git commit *) Bash(git tag *) Bash(git reset *) Bash(npm init*) Bash(npm install*)
```

<!-- appdev-cli.mjs round-complete output (from Plan 01) -->
```json
{
  "round": 2,
  "verdict": "FAIL",
  "scores": { "product_depth": 6, "functionality": 5, "visual_design": 4, "code_quality": 7, "total": 22 },
  "escalation": "E-I",
  "escalation_label": "Decelerating",
  "exit_condition": null,
  "should_continue": true,
  "trajectory": [{ "round": 1, "total": 18, "escalation": "E-0" }, { "round": 2, "total": 22, "escalation": "E-I" }]
}
```

<!-- Orchestrator loop pseudocode (from RESEARCH.md) -->
```
for round in 1..10:
  spawn Generator("This is generation round N.")
  binary check
  spawn Evaluator("This is evaluation round N.")
  result = appdev-cli round-complete --round N --report evaluation/round-N/EVALUATION.md
  if PASS: tag, break -> summary
  if PLATEAU: tag, break -> summary
  if REGRESSION: rollback to best round, break -> summary
  tag appdev/round-N
  continue
if round 10 with no exit: SAFETY_CAP -> wrap-up round 11 -> summary
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite orchestrator SKILL.md with git workspace, convergence loop, and GAN language</name>
  <files>plugins/application-dev/skills/application-dev/SKILL.md</files>
  <action>
Rewrite SKILL.md preserving the overall structure from Phase 1 but with major changes to every section. The file will grow from ~311 lines. Use the Phase 1 version as the base and apply these modifications:

**Frontmatter changes:**
- Change `allowed-tools` from `Agent Read Write Bash(node *appdev-state*)` to `Agent Read Write Bash(node *appdev-cli*) Bash(git init*) Bash(git rev-parse *) Bash(git add *) Bash(git commit *) Bash(git tag *) Bash(git reset *) Bash(npm init*) Bash(npm install*)`
- Update `compatibility` to mention `@playwright/cli` as a project devDependency (installed automatically in Step 0.5), not a system PATH requirement
- Update `description` to mention git version control, score-based convergence detection, and escalation vocabulary

**GAN language rename throughout the entire file:**
- "QA-REPORT.md" -> "EVALUATION.md"
- "qa/" -> "evaluation/"
- "Build/QA Loop" -> "Generation/Evaluation Loop"
- "Build Phase" -> "Generation Phase"
- "Evaluate Phase" -> "Evaluation Phase"
- "build round" -> "generation round"
- "QA round" -> "evaluation round"
- "QA artifacts" -> "evaluation artifacts"
- "QA report" -> "evaluation report"
- "Bash(node *appdev-state*)" -> "Bash(node *appdev-cli*)" in all examples

**Architecture section:** Keep the GAN architecture description and enforcement model from Phase 1. Update terminology only. Add a brief note about the escalation vocabulary (E-0 through E-IV) as part of the convergence detection system.

**Rules section:** Keep all 6 rules. Update Rule 2 to mention appdev-cli: "Never diagnose agent output beyond the binary file-exists check and appdev-cli's JSON response. Do not read EVALUATION.md directly except in the Summary step."

**New Step 0.5: Git Workspace Setup** (insert between Step 0 Resume Check and Step 1 Plan):

```
### Step 0.5: Git Workspace Setup

Initialize the workspace for version-controlled development (skip if resuming past this step):

Check for existing git repo:
Bash(git rev-parse --git-dir 2>/dev/null || git init)

Initialize package.json:
Bash(npm init -y)

Install @playwright/cli as a dev dependency:
Bash(npm install --save-dev @playwright/cli)

Seed .gitignore with harness infrastructure (use Write tool):
- .appdev-state.json
- .playwright-cli/
- node_modules/

Initial commit:
Bash(git add .gitignore package.json package-lock.json)
Bash(git commit -m "chore: initialize appdev workspace")
```

Note: Run git commands as SEPARATE Bash calls, not chained with `&&`. Each `Bash(git add ...)` and `Bash(git commit ...)` is a separate tool call. Shell operators in compound commands do not match the allowed-tools patterns.

**Step 1: Plan** -- add git operations after the Planner completes:

After the binary check on SPEC.md succeeds:
```
Bash(git add SPEC.md)
Bash(git commit -m "docs(spec): product specification")
Bash(git tag -a appdev/planning-complete -m "Planning complete: SPEC.md committed")
```

Update state: change `Bash(node *appdev-state*)` to `Bash(node *appdev-cli*)` in all state management examples.

**Step 2: Generation/Evaluation Loop** -- complete rewrite of the loop structure:

Replace "Run up to 3 rounds" with "Run up to 10 rounds with score-based convergence detection."

**Generation Phase (each round N):**
- Output: `[2/3] Generating (round N)...`
- Update state: `Bash(node ${CLAUDE_PLUGIN_ROOT}/scripts/appdev-cli.mjs update --step generate --round N)`
- Spawn Generator with minimal prompt:
  - Round 1: `Agent(subagent_type: "application-dev:generator", prompt: "This is generation round 1.")`
  - Round 2+: `Agent(subagent_type: "application-dev:generator", prompt: "This is generation round N.")`
- Apply error recovery pattern
- Binary check: same as before (ls project files)
- Output: `[2/3] Generating (round N)... done`

Note: The Generator's agent definition handles reading SPEC.md (round 1) and EVALUATION.md (rounds 2+) internally. The orchestrator does NOT include file reading instructions in the prompt per the "minimal orchestrator prompts" decision.

**Evaluation Phase (each round N):**
- Output: `[2/3] Evaluating (round N)...`
- Update state: `Bash(node ${CLAUDE_PLUGIN_ROOT}/scripts/appdev-cli.mjs update --step evaluate --round N)`
- Spawn Evaluator: `Agent(subagent_type: "application-dev:evaluator", prompt: "This is evaluation round N.")`
- Apply error recovery pattern
- Binary check: Read `evaluation/round-N/EVALUATION.md` -- verify the file exists and contains `## Verdict`. Do NOT assess report quality.

**Post-Evaluation Convergence Check:**
Instead of parsing the verdict directly, delegate to appdev-cli:
```
Bash(node ${CLAUDE_PLUGIN_ROOT}/scripts/appdev-cli.mjs round-complete --round N --report evaluation/round-N/EVALUATION.md)
```

Act on the JSON response:

If `exit_condition` is `"PASS"`:
- Output: `[2/3] Evaluating (round N)... Verdict: PASS`
- Tag: `Bash(git tag -a appdev/round-N -m "Round N complete: PASS")`
- Tag: `Bash(git tag -a appdev/final -m "Final result: PASS after N rounds")`
- Break -> Step 3 (Summary)

If `exit_condition` is `"PLATEAU"`:
- Output: `[2/3] Evaluating (round N)... Verdict: FAIL (Plateau -- scores converged)`
- Tag: `Bash(git tag -a appdev/round-N -m "Round N complete: PLATEAU exit")`
- Tag: `Bash(git tag -a appdev/final -m "Final result: PLATEAU after N rounds")`
- Break -> Step 3 (Summary). No wrap-up round. Plateau means natural convergence.

If `exit_condition` is `"REGRESSION"`:
- Output: `[2/3] Evaluating (round N)... Verdict: FAIL (Regression -- rolling back to round {best_round})`
- Rollback: `Bash(git reset --hard appdev/round-{best_round})`
- Tag: `Bash(git tag -a appdev/final -m "Final result: REGRESSION rollback to round {best_round}")`
- Break -> Step 3 (Summary). Use `evaluation/round-{best_round}/EVALUATION.md` for the summary.

If `exit_condition` is `"SAFETY_CAP"`:
- Output: `[2/3] Evaluating (round N)... Verdict: FAIL (Safety cap reached)`
- Tag: `Bash(git tag -a appdev/round-N -m "Round N complete: SAFETY_CAP")`
- Run one extra wrap-up round (round N+1, not counted toward the 10-round cap):
  - Spawn Generator: `Agent(subagent_type: "application-dev:generator", prompt: "This is generation round {N+1}.")`
  - Binary check
  - Spawn Evaluator: `Agent(subagent_type: "application-dev:evaluator", prompt: "This is evaluation round {N+1}.")`
  - `Bash(node ${CLAUDE_PLUGIN_ROOT}/scripts/appdev-cli.mjs round-complete --round {N+1} --report evaluation/round-{N+1}/EVALUATION.md)`
  - Tag: `Bash(git tag -a appdev/round-{N+1} -m "Round {N+1} complete: wrap-up")`
  - Tag: `Bash(git tag -a appdev/final -m "Final result: SAFETY_CAP with wrap-up round")`
- Break -> Step 3 (Summary)

If `should_continue` is true (no exit condition):
- Output: `[2/3] Evaluating (round N)... Verdict: FAIL (Escalation: {escalation_label})`
- Tag: `Bash(git tag -a appdev/round-N -m "Round N complete: {escalation_label}")`
- Continue to next round

**Step 3: Summary** -- update to use GAN language:
- Read `evaluation/round-{final}/EVALUATION.md` and `README.md` (for REGRESSION, read from the best_round's evaluation instead)
- Present: product name, features, final scores, verdict, number of rounds, exit condition, escalation history (from appdev-cli get-trajectory), how to run
- Complete with appropriate exit condition: `Bash(node ${CLAUDE_PLUGIN_ROOT}/scripts/appdev-cli.mjs complete --exit-condition <PASS|PLATEAU|REGRESSION|SAFETY_CAP>)`
- Delete state: `Bash(node ${CLAUDE_PLUGIN_ROOT}/scripts/appdev-cli.mjs delete)`

**Agent Prompt Protocol section** -- update to minimal prompts per CONTEXT.md:

Planner: (unchanged) `<user's full prompt, verbatim>`

Generator (all rounds): `This is generation round N.`
(No round-1-specific prompt mentioning SPEC.md. No rounds-2+-specific prompt mentioning EVALUATION.md. The Generator's agent definition handles what to read based on round number.)

Evaluator (all rounds): `This is evaluation round N.`
(No mention of SPEC.md or writing to a specific file. The Evaluator's agent definition handles all of this.)

**File-Based Communication section** -- update:
- `SPEC.md` -- Planner writes, Generator and Evaluator read
- `evaluation/round-N/EVALUATION.md` -- Evaluator writes per round, Generator reads prior round's report
- `.appdev-state.json` -- managed exclusively via appdev-cli

No root-level EVALUATION.md copy. Each round's report lives in its folder.

**Error Recovery section** -- keep the same pattern. Update any "appdev-state" references to "appdev-cli". Add note: if appdev-cli round-complete returns an error JSON (malformed report), treat it as an Evaluator failure and apply the retry pattern on the Evaluator.
  </action>
  <verify>
    <automated>git grep -c "appdev-state\|QA-REPORT\|qa/" -- plugins/application-dev/skills/application-dev/SKILL.md</automated>
  </verify>
  <done>Zero matches for "appdev-state", "QA-REPORT", or "qa/" in SKILL.md. SKILL.md contains: Step 0.5 (git workspace), Step 1 with SPEC.md commit and planning-complete tag, Step 2 with 10-round convergence loop using appdev-cli round-complete, all four exit condition dispatches, REGRESSION rollback, SAFETY_CAP wrap-up round, appdev/round-N tags, minimal agent prompts, updated allowed-tools.</done>
</task>

<task type="auto">
  <name>Task 2: Create docs/ARCHITECTURE.md</name>
  <files>docs/ARCHITECTURE.md</files>
  <action>
Create `docs/ARCHITECTURE.md` at the repository root (not inside `plugins/` -- this is repo documentation, not distributed with the plugin).

Create the `docs/` directory first if it does not exist.

Structure the document with these sections:

1. **Title and Overview:** "Application-Dev Plugin Architecture" -- brief overview of the GAN-inspired three-agent system for autonomous application development.

2. **GAN Architecture:** Explain the Generator-Evaluator adversarial pair inspired by Generative Adversarial Networks. The Generator produces applications, the Evaluator critiques them, and the adversarial tension drives quality improvement across rounds. Explain why separating generation from evaluation prevents self-praise bias. Note: this is inspired by GANs, not a literal implementation -- there is no loss function or gradient, but the feedback loop serves the same purpose of adversarial improvement.

3. **Agent Roles and Separation:**
   - Planner: expands user prompt into ambitious product specification (SPEC.md)
   - Generator: builds the full application from spec, iterates on evaluation feedback
   - Evaluator: adversarial critic that tests the running application
   - Orchestrator: "dumb coordinator" that delegates all intelligence to agents and appdev-cli

4. **Two-Layer Enforcement Model:** Tool allowlists (structural, runtime-enforced) + prompt guards (behavioral, instruction-enforced). Explain why plugin hooks and disallowedTools were dropped. Reference the Phase 1 decisions.

5. **Cybernetics Inspirations:**
   - **Escalation Vocabulary (E-0 through E-IV):** Inspired by cybernetics control theory -- named operating regimes with threshold-based state transitions in a feedback loop. E-0 Progressing, E-I Decelerating, E-II Plateau, E-III Regression, E-IV Catastrophic. Explain each level, its trigger condition, and the system response.
   - **Damping Principle (LOOP-07):** In rounds 2+, the Generator fixes only what the Evaluator flagged. Unconstrained changes cause oscillation instead of convergence. This is analogous to damping in control systems.
   - **Score-Based Convergence Detection:** The system monitors score trajectory to detect convergence (plateau), divergence (regression), and catastrophic failure. This replaces a fixed iteration count with adaptive stopping.

6. **Anthropic Article Alignment and Divergences:** Reference the Anthropic article on multi-agent application development as inspiration. Note key divergences:
   - Sprints removed (Opus 4.6 handles long sessions)
   - Context rotation replaced by fresh context per agent spawn (GAN architecture)
   - Fixed round count replaced by convergence detection
   - Roles sharpened into adversarial GAN pair rather than cooperative team

7. **Key Design Decisions:** Summarize the major decisions from Phases 1-2 as a reference table:
   - Orchestrator never reads EVALUATION.md (delegates to appdev-cli)
   - Agents commit their own work, orchestrator creates milestone tags
   - Planner has no Bash (orchestrator commits SPEC.md)
   - Minimal agent prompts ("This is generation round N.")
   - appdev-cli centralizes all convergence logic
   - @playwright/cli as project devDependency (not system PATH)

8. **File-Based Communication Protocol:** SPEC.md and EVALUATION.md as the inter-agent protocol. State file (.appdev-state.json) as the orchestrator's memory. Git tags as milestone markers and rollback recovery points.

Keep the document informative but concise -- aim for 150-250 lines. This is a reference document for understanding the architecture, not an exhaustive specification.
  </action>
  <verify>
    <automated>git grep -c "Escalation\|GAN\|cybernetics" -- docs/ARCHITECTURE.md</automated>
  </verify>
  <done>docs/ARCHITECTURE.md exists at repo root, covers GAN architecture, escalation vocabulary, cybernetics inspirations, two-layer enforcement, and key design decisions from Phases 1-2</done>
</task>

</tasks>

<verification>
1. `git grep -c "appdev-state\|QA-REPORT\|qa/" -- plugins/application-dev/skills/application-dev/SKILL.md` returns 0
2. `git grep "appdev-cli" -- plugins/application-dev/skills/application-dev/SKILL.md` shows multiple references
3. `git grep "round-complete" -- plugins/application-dev/skills/application-dev/SKILL.md` shows convergence check
4. `git grep "git tag -a appdev/" -- plugins/application-dev/skills/application-dev/SKILL.md` shows milestone tags
5. `git grep "git reset --hard" -- plugins/application-dev/skills/application-dev/SKILL.md` shows REGRESSION rollback
6. `git grep "npm install.*playwright" -- plugins/application-dev/skills/application-dev/SKILL.md` shows workspace setup
7. docs/ARCHITECTURE.md exists and contains GAN, escalation, and cybernetics content
</verification>

<success_criteria>
- SKILL.md has Step 0.5 git workspace setup (git init, npm init, @playwright/cli, .gitignore, initial commit)
- SKILL.md commits SPEC.md after Planner and tags appdev/planning-complete (GIT-01)
- SKILL.md creates appdev/round-N tags after each round and appdev/final at end (GIT-05)
- SKILL.md loop runs up to 10 rounds with appdev-cli convergence detection (LOOP-02)
- SKILL.md dispatches all four exit conditions correctly (LOOP-03 for SAFETY_CAP wrap-up)
- SKILL.md uses minimal agent prompts ("This is generation round N.", "This is evaluation round N.")
- SKILL.md allowed-tools includes all git/npm/appdev-cli patterns
- SKILL.md has zero references to appdev-state, QA-REPORT.md, or qa/
- docs/ARCHITECTURE.md exists with GAN architecture, cybernetics, and design decisions
</success_criteria>

<output>
After completion, create `.planning/phases/02-git-workflow-and-loop-control/02-03-SUMMARY.md`
</output>
