---
phase: 14-enhanced-existing-critics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/phase-14-structural.test.mjs
  - plugins/application-dev/agents/perceptual-critic.md
  - plugins/application-dev/skills/application-dev/references/evaluator/SCORING-CALIBRATION.md
  - plugins/application-dev/agents/projection-critic.md
autonomous: true
requirements: [EVAL-01, EVAL-02, EVAL-03]

must_haves:
  truths:
    - "Perceptual-critic methodology includes cross-page consistency audit instructions in the OBSERVE step"
    - "Perceptual-critic tool allowlist includes Bash(npx playwright test *) for write-and-run audit execution"
    - "Projection-critic methodology includes A->B->A round-trip navigation test guidance in the TEST section"
    - "Round-trip test failures produce FN-X findings and are explicitly excluded from acceptance_tests.results[] feature mapping"
    - "SCORING-CALIBRATION.md Visual Design ceilings include shared component divergence ceiling at max 6"
    - "SCORING-CALIBRATION.md 6/10 and 8/10 calibration scenarios reference cross-page visual consistency"
  artifacts:
    - path: "tests/phase-14-structural.test.mjs"
      provides: "Structural tests covering EVAL-01, EVAL-02, EVAL-03"
      contains: "describe"
    - path: "plugins/application-dev/agents/perceptual-critic.md"
      provides: "Enhanced perceptual-critic with cross-page consistency audit"
      contains: "consistency-audit"
    - path: "plugins/application-dev/agents/projection-critic.md"
      provides: "Enhanced projection-critic with A->B->A round-trip navigation"
      contains: "Round-trip"
    - path: "plugins/application-dev/skills/application-dev/references/evaluator/SCORING-CALIBRATION.md"
      provides: "Updated Visual Design ceilings and calibration scenarios"
      contains: "shared component"
  key_links:
    - from: "plugins/application-dev/agents/perceptual-critic.md"
      to: "npx playwright test"
      via: "YAML tools array in frontmatter"
      pattern: "Bash\\(npx playwright test"
    - from: "plugins/application-dev/agents/perceptual-critic.md"
      to: "consistency-audit.spec.ts"
      via: "OBSERVE section write-and-run instructions"
      pattern: "consistency-audit"
    - from: "plugins/application-dev/skills/application-dev/references/evaluator/SCORING-CALIBRATION.md"
      to: "plugins/application-dev/agents/perceptual-critic.md"
      via: "Ceiling rule references methodology that the critic can now evaluate"
      pattern: "shared component.*differ.*max 6"
    - from: "plugins/application-dev/agents/projection-critic.md"
      to: "acceptance-tests.spec.ts"
      via: "TEST section round-trip describe block guidance"
      pattern: "Round-trip navigation"
    - from: "plugins/application-dev/agents/projection-critic.md"
      to: "summary.json acceptance_tests.results[]"
      via: "Explicit exclusion statement for round-trip tests"
      pattern: "acceptance_tests.results"
---

<objective>
Enhance the perceptual-critic with cross-page visual consistency detection, the projection-critic with A->B->A round-trip navigation testing, and update Visual Design calibration scenarios to match the expanded methodology scope.

Purpose: The perceptual-critic currently evaluates pages in isolation -- a navigation bar that changes color between pages goes undetected. The projection-critic tests features individually -- state that is lost after navigating away and returning goes uncaught. These are real defect classes observed in the Dutch art museum test that the existing critics miss.

Output: Modified perceptual-critic.md, projection-critic.md, SCORING-CALIBRATION.md, and new structural test file.
</objective>

<execution_context>
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-enhanced-existing-critics/14-CONTEXT.md
@.planning/phases/14-enhanced-existing-critics/14-RESEARCH.md

<interfaces>
<!-- Key structures the executor needs to understand from existing files -->

From plugins/application-dev/agents/perceptual-critic.md (line 18, current tools array):
```yaml
tools: ["Read", "Write", "Bash(npx playwright-cli *)", "Bash(node *appdev-cli* install-dep *)", "Bash(node *appdev-cli* check-assets *)", "Bash(node *appdev-cli* static-serve*)"]
```

From plugins/application-dev/agents/perceptual-critic.md (methodology structure):
```
## Methodology
### UNDERSTAND
### OBSERVE  <-- cross-page consistency audit goes here, after existing content
### DETECT
### SCORE
### REPORT
```

From plugins/application-dev/agents/projection-critic.md (line 18, current tools array):
```yaml
tools: ["Read", "Write", "Bash(npx playwright-cli *)", "Bash(node *appdev-cli* install-dep *)", "Bash(npx playwright test *)", "Bash(node *appdev-cli* static-serve*)"]
```

From plugins/application-dev/agents/projection-critic.md (methodology structure):
```
## Methodology
### UNDERSTAND
### TEST  <-- A->B->A guidance goes here, before "### Round 2+ Test Reuse"
### Round 2+ Test Reuse
### PROBE
### SCORE
### REPORT
```

From plugins/application-dev/skills/application-dev/references/evaluator/SCORING-CALIBRATION.md (Visual Design ceilings):
```markdown
### Visual Design
| Condition | Ceiling |
|-----------|---------|
| All images placeholder | max 3 |
| No design language match | max 5 |
| Layout broken on mobile | max 5 |
```

From tests/phase-10-structural.test.mjs (test pattern):
```javascript
import { describe, it } from 'node:test';
import assert from 'node:assert/strict';
import { readFileSync, existsSync } from 'node:fs';
import { join, dirname } from 'node:path';
import { fileURLToPath } from 'node:url';
const ROOT = join(dirname(fileURLToPath(import.meta.url)), '..');
function readImpl(relPath) { return readFileSync(join(ROOT, relPath), 'utf8'); }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create structural tests for EVAL-01, EVAL-02, EVAL-03</name>
  <files>tests/phase-14-structural.test.mjs</files>
  <behavior>
    - EVAL-01-a: perceptual-critic.md tools array contains "Bash(npx playwright test *)"
    - EVAL-01-b: perceptual-critic.md contains "consistency-audit" text
    - EVAL-01-c: perceptual-critic.md contains reference to "consistency-audit.spec.ts" or "consistency-audit.json"
    - EVAL-01-d: perceptual-critic.md consistency audit content appears within or after the OBSERVE section
    - EVAL-02-a: projection-critic.md contains "Round-trip" or "round-trip" or "A->B->A" text
    - EVAL-02-b: projection-critic.md contains reference to "goBack" (from page.goBack())
    - EVAL-02-c: projection-critic.md mentions FN- finding prefix in the round-trip context
    - EVAL-02-d: projection-critic.md states round-trip tests are excluded from acceptance_tests.results[]
    - EVAL-03-a: SCORING-CALIBRATION.md Visual Design ceiling table contains "shared component" or "nav/footer" divergence row with "max 6"
    - EVAL-03-b: SCORING-CALIBRATION.md 6/10 scenario mentions nav consistency or cross-page language
    - EVAL-03-c: SCORING-CALIBRATION.md 8/10 scenario contains "ACROSS ALL PAGES" or equivalent cross-page language
  </behavior>
  <action>
Create `tests/phase-14-structural.test.mjs` following the established pattern from `tests/phase-10-structural.test.mjs`:

- Import `node:test` (describe, it), `node:assert/strict`, `node:fs` (readFileSync)
- Use the same `ROOT` and `readImpl()` helper pattern
- Three `describe` blocks grouped by requirement ID: EVAL-01, EVAL-02, EVAL-03

EVAL-01 describe block (4 assertions):
1. Read perceptual-critic.md, assert tools array contains `Bash(npx playwright test *)`
2. Assert file contains "consistency-audit" text
3. Assert file contains "consistency-audit.spec.ts" or "consistency-audit.json"
4. Assert the consistency audit content appears after the "### OBSERVE" heading (indexOf check)

EVAL-02 describe block (4 assertions):
1. Read projection-critic.md, assert contains "round-trip" (case-insensitive via toLowerCase) or "A->B->A"
2. Assert contains "goBack" reference
3. Assert contains "FN-" in proximity to round-trip context (verify by checking both exist)
4. Assert contains "acceptance_tests.results" with exclusion language (assert includes "acceptance_tests.results" and includes a negation word like "NOT" or "excluded" or "not included")

EVAL-03 describe block (3 assertions):
1. Read SCORING-CALIBRATION.md, assert Visual Design ceiling table area contains "shared component" or "nav/footer" with "max 6"
2. Assert 6/10 scenario area contains cross-page or nav consistency language
3. Assert 8/10 scenario area contains "ACROSS ALL PAGES"

All tests should FAIL initially since the implementation files have not been modified yet (RED phase of TDD).
  </action>
  <verify>
    <automated>node --test tests/phase-14-structural.test.mjs 2>&1 | head -50</automated>
  </verify>
  <done>Test file exists with 11 assertions across 3 describe blocks. All tests fail (RED) because implementation does not yet exist. Running `node --test tests/phase-14-structural.test.mjs` shows 11 failing tests.</done>
</task>

<task type="auto">
  <name>Task 2: Enhance perceptual-critic with cross-page consistency audit and update calibration (EVAL-01 + EVAL-03)</name>
  <files>plugins/application-dev/agents/perceptual-critic.md, plugins/application-dev/skills/application-dev/references/evaluator/SCORING-CALIBRATION.md</files>
  <action>
**perceptual-critic.md changes (2 locations):**

1. **Tool allowlist update (line 18 YAML frontmatter):** Add `"Bash(npx playwright test *)"` to the tools array, inserting it after `"Bash(npx playwright-cli *)"`. The updated line should be:
```yaml
tools: ["Read", "Write", "Bash(npx playwright-cli *)", "Bash(npx playwright test *)", "Bash(node *appdev-cli* install-dep *)", "Bash(node *appdev-cli* check-assets *)", "Bash(node *appdev-cli* static-serve*)"]
```
This enables the perceptual-critic to execute write-and-run Playwright test files (Pitfall 1 prevention from RESEARCH.md).

2. **OBSERVE section expansion:** Add a "### Cross-Page Consistency Audit" subsection at the end of the existing OBSERVE section (after the console filtering paragraph, before ### DETECT). This subsection instructs the critic to:

   a. Discover internal pages from homepage navigation links via eval:
      `npx playwright-cli eval "[...document.querySelectorAll('a[href]')].map(a => new URL(a.href, location.origin)).filter(u => u.origin === location.origin).map(u => u.pathname).filter((v,i,a) => a.indexOf(v) === i).slice(0, 4)"`
      Cap at 5 total pages (homepage + up to 4 discovered).

   b. Write `evaluation/round-N/perceptual/consistency-audit.spec.ts` using the write-and-run pattern. The test file:
      - Visits all discovered pages
      - Extracts 14 computed style properties per shared element (nav, footer, h1, h2, h3, body): color, backgroundColor, fontFamily, fontSize, fontWeight, lineHeight, letterSpacing, borderRadius, borderColor, borderWidth, boxShadow, gap, paddingTop, paddingRight, paddingBottom, paddingLeft
      - Performs Tier 1 (shared component comparison), Tier 2 (palette discipline metrics: unique text colors flag >20/escalate >30, unique background colors, unique font families flag >4/escalate >6, unique font sizes flag >12/escalate >20), and Tier 3 (CSS custom property divergence -- try/catch for cross-origin stylesheets) extraction
      - Writes `consistency-audit.json` to the same round directory
      - Note: the test file must substitute the current round number in the output path (same as acceptance-tests.spec.ts substitutes PORT)

   c. Run: `npx playwright test evaluation/round-N/perceptual/consistency-audit.spec.ts --reporter=json`

   d. Read `evaluation/round-N/perceptual/consistency-audit.json`

   e. Interpret findings using severity mapping table:
      - Nav/footer style divergence: Major (>3 properties diverge: escalate)
      - Heading style divergence: Major (fontFamily or fontSize differs)
      - Heading color divergence: Minor (only color differs -- may be intentional)
      - Palette overloaded (>20 colors): Minor (>30 colors: Major)
      - Too many font families (>4): Minor (>6 families: Major)
      - Too many font sizes (>12): Minor (>20 sizes: Major)
      - CSS custom property divergence: Critical (same variable, different resolved value)
      - Body font-family divergence: Major (always)

   f. Heuristic note: compare same semantic role across pages, not same CSS class. Different page backgrounds, responsive changes, and hover/focus/active state differences are intentional variation -- do not flag.

   g. Round 2+ note: the consistency-audit.spec.ts follows the same copy/heal/regenerate pattern as acceptance tests. Copy from prior round, run, decide.

   h. Findings from the audit flow into DETECT as VD-prefixed findings and into SCORE via existing ceiling rules plus the new "shared component divergence" ceiling.

Keep the subsection lean -- methodology guidance, not a full template. The consistency-audit.spec.ts structure is at Claude's discretion per CONTEXT.md. Aim for ~30-45 lines of methodology text.

**SCORING-CALIBRATION.md changes (3 locations):**

1. **Visual Design ceiling table:** Add a new row after the existing 3 rows:
   `| Shared components (nav/footer/header) visually differ across pages | max 6 |`

2. **Visual Design 6/10 "At Threshold" scenario:** Add to the existing description paragraph (weave in, do not replace):
   "Navigation bar uses the same font but a slightly different accent color on the settings page. Heading sizes are consistent across pages."
   Update the "Not 7 because" line to:
   "A 7 requires visual coherence across all pages. Placeholder images plus the nav color inconsistency show incomplete design execution."

3. **Visual Design 8/10 "Above Threshold" scenario:** Strengthen the existing description to include cross-page language:
   "Color palette has 4 intentional accent colors used consistently ACROSS ALL PAGES. Navigation and footer are visually identical on every page."
   Update the "Not 9 because" line to:
   "The settings page reverts to a generic form layout -- shared components are consistent but page-level design language breaks."

These two files must be updated atomically (same task) to prevent Pitfall 2: calibration-methodology desync.

**Post-edit verification:** Search both modified files for stale "both critics", "two critics", or "four agents" references (Pitfall 6 from RESEARCH.md). These should not exist but verify to be safe.
  </action>
  <verify>
    <automated>node --test tests/phase-14-structural.test.mjs 2>&1 | grep -E "(EVAL-01|pass|fail)"</automated>
  </verify>
  <done>All EVAL-01 and EVAL-03 structural tests pass. perceptual-critic.md contains the consistency audit subsection in OBSERVE with the tool allowlist updated. SCORING-CALIBRATION.md has the new ceiling rule and updated 6/10 and 8/10 scenario boundaries. No stale "both critics" or "two critics" references in modified files.</done>
</task>

<task type="auto">
  <name>Task 3: Enhance projection-critic with A->B->A round-trip navigation testing (EVAL-02)</name>
  <files>plugins/application-dev/agents/projection-critic.md</files>
  <action>
**projection-critic.md changes (1 location):**

Add a "### Round-Trip Navigation Tests" subsection within the TEST section, after the existing test coverage list ("Each test maps to a SPEC.md feature..." paragraph ending with "One negative test per feature") and before "### Round 2+ Test Reuse". This subsection instructs the critic to:

1. At the end of `acceptance-tests.spec.ts`, add a `test.describe('Round-trip navigation')` block with SPEC-derived round-trip tests:

   - For each CRUD feature in SPEC.md: create data, navigate to a different view, return via `page.goBack()`, verify data persists
   - For each filtered/sorted view in SPEC.md: apply filters, click into a detail, return via `page.goBack()`, verify filters still active
   - One URL integrity test: navigate with query params, leave, return via back button, assert URL params intact
   - One console error test: monitor `page.on("console", ...)` during an A->B->A cycle, assert no errors

2. Return method: always use `page.goBack()` followed by a content assertion or `waitForURL()`. Never use `waitForTimeout` -- SPA routers update asynchronously.

3. Scoring dimension statement: Round-trip test failures produce FN-X findings (Functionality dimension). They are NOT included in `acceptance_tests.results[]` feature mapping -- Product Depth measures feature presence, not feature durability. A feature that works on first visit but loses state on return is functionally buggy, not missing.

4. Round 2+ behavior note: Round-trip tests are part of the single acceptance-tests.spec.ts file, so they are automatically included in the existing round 2+ test reuse/heal/regenerate workflow. No special handling needed.

Keep the subsection to ~20-30 lines. The projection-critic already has the `Bash(npx playwright test *)` tool in its allowlist, so no frontmatter change is needed.

**Post-edit verification:** Search the modified file for stale "both critics", "two critics", or "four agents" references (Pitfall 6 from RESEARCH.md).
  </action>
  <verify>
    <automated>node --test tests/phase-14-structural.test.mjs 2>&1 | grep -E "(EVAL-02|pass|fail)"</automated>
  </verify>
  <done>All EVAL-02 structural tests pass. projection-critic.md contains the Round-Trip Navigation Tests subsection in the TEST section with page.goBack() return method, FN-X finding prefix, and explicit exclusion from acceptance_tests.results[]. No stale terminology references.</done>
</task>

</tasks>

<verification>
Run full structural test suite to confirm all 3 requirements are met:

```bash
node --test tests/phase-14-structural.test.mjs
```

All 11 assertions must pass across the 3 describe blocks (EVAL-01: 4, EVAL-02: 4, EVAL-03: 3).

Additionally verify the full existing test suite still passes:

```bash
node --test tests/phase-10-structural.test.mjs
```

Manual spot-checks:
- perceptual-critic.md: YAML tools array includes both `Bash(npx playwright-cli *)` AND `Bash(npx playwright test *)`
- perceptual-critic.md: Consistency audit subsection appears within OBSERVE (not DETECT or SCORE)
- projection-critic.md: Round-trip subsection appears within TEST (not PROBE or SCORE)
- projection-critic.md: Explicit statement excluding round-trip results from `acceptance_tests.results[]`
- SCORING-CALIBRATION.md: New ceiling row in Visual Design table, not in Functionality or Robustness
- No "both critics" or "two critics" stale references in any modified file
</verification>

<success_criteria>
1. `node --test tests/phase-14-structural.test.mjs` passes all 11 assertions
2. `node --test tests/phase-10-structural.test.mjs` passes (no regressions)
3. perceptual-critic.md has `Bash(npx playwright test *)` in tools and a cross-page consistency audit subsection in OBSERVE
4. projection-critic.md has an A->B->A round-trip navigation testing subsection in TEST with FN-X findings and acceptance_tests.results[] exclusion
5. SCORING-CALIBRATION.md has shared component divergence ceiling (max 6) and updated 6/10 + 8/10 scenarios with cross-page language
</success_criteria>

<output>
After completion, create `.planning/phases/14-enhanced-existing-critics/14-01-SUMMARY.md`
</output>
