---
phase: 05-optimize-agent-definitions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/application-dev/agents/evaluator.md
  - plugins/application-dev/skills/application-dev/references/evaluator/AI-SLOP-CHECKLIST.md
  - plugins/application-dev/skills/application-dev/references/evaluator/ASSET-VALIDATION-PROTOCOL.md
autonomous: true
requirements: [OPT-01, OPT-02, OPT-03]
must_haves:
  truths:
    - "Evaluator agent definition is under 400 lines after extraction and deduplication"
    - "AI Slop Checklist exists as a standalone reference file in the evaluator references directory"
    - "Asset Validation protocol exists as a standalone reference file in the evaluator references directory"
    - "Self-Verification appears exactly once in evaluator.md (Step 14 only)"
    - "Evaluator workflow still has 15 numbered steps with the same step names"
    - "WHY-based rationale replaces ALL-CAPS MUST/NEVER/CRITICAL emphasis throughout"
  artifacts:
    - path: "plugins/application-dev/agents/evaluator.md"
      provides: "Optimized evaluator agent definition with progressive disclosure"
      contains: "Step 14: Self-Verification"
    - path: "plugins/application-dev/skills/application-dev/references/evaluator/AI-SLOP-CHECKLIST.md"
      provides: "Extracted AI slop pattern checklist for visual assessment"
      min_lines: 30
    - path: "plugins/application-dev/skills/application-dev/references/evaluator/ASSET-VALIDATION-PROTOCOL.md"
      provides: "Extracted detailed asset validation procedure"
      min_lines: 25
  key_links:
    - from: "plugins/application-dev/agents/evaluator.md"
      to: "references/evaluator/AI-SLOP-CHECKLIST.md"
      via: "Read instruction at Step 5"
      pattern: "AI-SLOP-CHECKLIST"
    - from: "plugins/application-dev/agents/evaluator.md"
      to: "references/evaluator/ASSET-VALIDATION-PROTOCOL.md"
      via: "Read instruction at Step 7"
      pattern: "ASSET-VALIDATION-PROTOCOL"
    - from: "plugins/application-dev/agents/evaluator.md"
      to: "references/evaluator/EVALUATION-TEMPLATE.md"
      via: "Read instruction at Step 13"
      pattern: "EVALUATION-TEMPLATE"
---

<objective>
Optimize the Evaluator agent definition (465 lines -> ~370 lines) through progressive disclosure extraction, Self-Verification deduplication, and instruction engineering.

Purpose: The evaluator is the heaviest per-spawn agent (spawned up to 10 times per run). Extracting protocol-heavy content to reference files reduces per-spawn token cost (~1,000 tokens saved x 10 rounds = ~10,000 tokens per run) while improving behavioral quality through a leaner instruction body. Deduplicating Self-Verification follows Anthropic's "information lives in one place only" best practice. Replacing ALL-CAPS emphasis with WHY-based rationale prevents Opus 4.6 overtriggering.

Output: Restructured evaluator.md + 2 new reference files (AI-SLOP-CHECKLIST.md, ASSET-VALIDATION-PROTOCOL.md)
</objective>

<execution_context>
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-optimize-agent-definitions/05-CONTEXT.md
@.planning/phases/05-optimize-agent-definitions/05-RESEARCH.md

@plugins/application-dev/agents/evaluator.md
@plugins/application-dev/skills/application-dev/references/evaluator/EVALUATION-TEMPLATE.md
@plugins/application-dev/skills/application-dev/references/evaluator/SCORING-CALIBRATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract AI Slop Checklist and Asset Validation Protocol to reference files</name>
  <files>
    plugins/application-dev/skills/application-dev/references/evaluator/AI-SLOP-CHECKLIST.md
    plugins/application-dev/skills/application-dev/references/evaluator/ASSET-VALIDATION-PROTOCOL.md
  </files>
  <action>
Create two new reference files by extracting content from evaluator.md:

**AI-SLOP-CHECKLIST.md:** Extract the "AI Slop Checklist" section from evaluator.md (lines 46-81). Move the entire section content -- the introductory paragraph and all 6 category blocks (Typography Slop, Color Slop, Layout Slop, Content Slop, Motion Slop, Design Identity Slop) -- into the new file. Add a title heading and brief purpose statement at the top. Preserve the exact checklist items verbatim (the evaluator has been using these since Phase 3; changing them could alter scoring behavior). The file should be self-contained: readable without the evaluator.md context.

**ASSET-VALIDATION-PROTOCOL.md:** Extract the detailed asset validation procedure from evaluator.md Step 7 (lines 261-288). This includes sub-steps 7a through 7g (URL Collection, Per-image inspection, Perceptual hashing, Link checking, Font checking, Meta assets, Alt text) and the Severity rules block. Move all of this protocol detail into the new file. Add a title heading and brief purpose statement. Preserve the exact severity rules and thresholds verbatim (these define scoring behavior). Include the `sharp`, `imghash`, `leven` tool references since the evaluator installs these in Step 3.

Both files follow the naming convention of existing evaluator references (ALL-CAPS kebab: EVALUATION-TEMPLATE.md, SCORING-CALIBRATION.md, AI-PROBING-REFERENCE.md).
  </action>
  <verify>
    <automated>ls plugins/application-dev/skills/application-dev/references/evaluator/AI-SLOP-CHECKLIST.md plugins/application-dev/skills/application-dev/references/evaluator/ASSET-VALIDATION-PROTOCOL.md && wc -l plugins/application-dev/skills/application-dev/references/evaluator/AI-SLOP-CHECKLIST.md plugins/application-dev/skills/application-dev/references/evaluator/ASSET-VALIDATION-PROTOCOL.md</automated>
  </verify>
  <done>AI-SLOP-CHECKLIST.md exists with 30+ lines containing all 6 slop categories. ASSET-VALIDATION-PROTOCOL.md exists with 25+ lines containing sub-steps 7a-7g and severity rules.</done>
</task>

<task type="auto">
  <name>Task 2: Restructure evaluator.md with extraction references, deduplication, and WHY-based rationale</name>
  <files>plugins/application-dev/agents/evaluator.md</files>
  <action>
Rewrite evaluator.md applying all three optimization levers:

**1. Replace extracted sections with Read instructions:**

- Replace the inline "AI Slop Checklist" section (currently between Critical Mindset and Workflow) with a brief inline summary (2-3 lines) + a Read instruction:
  ```
  Read `${CLAUDE_PLUGIN_ROOT}/skills/application-dev/references/evaluator/AI-SLOP-CHECKLIST.md`
  during visual inspection (Step 5) and code review (Step 10).
  ```
  Keep a one-line mention of AI slop in the Critical Mindset section since it establishes behavioral identity.

- Replace the detailed asset validation protocol in Step 7 with a lean summary (3-5 lines describing the intent) + a Read instruction:
  ```
  Read `${CLAUDE_PLUGIN_ROOT}/skills/application-dev/references/evaluator/ASSET-VALIDATION-PROTOCOL.md`
  for the full validation procedure.
  ```
  Keep the Step 7 heading and its purpose statement inline (what to validate and why), but move the how (sub-steps 7a-7g, severity rules) to the reference.

**2. Deduplicate Self-Verification:**

- Remove the standalone `## Self-Verification` section at the end of evaluator.md (lines 450-465). This is an exact copy of Step 14.
- Keep only the Step 14 instance. Step 14 is the canonical location -- last workflow step before completion, right time for the inner quality gate.
- The Step 14 Self-Verification content stays exactly as-is (all 10 checks, the "fix before completing" instruction).

**3. WHY-based rationale and emphasis cleanup:**

Scan the entire file and apply these transformations:

- Replace "NEVER" with "Do not" + a WHY explanation for output-domain safety constraints. Example: "Never modify the application's source code" becomes "Do not modify the application's source code -- the evaluator is read-only; source changes contaminate the next generation round."
- Replace other ALL-CAPS emphasis (MUST, CRITICAL, ALWAYS) with normal phrasing + rationale. Example: "You MUST retest" becomes "Retest every previously-passing behavior because regressions are behavioral, not just code-level."
- Preserve bold for terms being defined: "**Fix-only mode (rounds 2+):**"
- Preserve existing XML tags in the description field.
- Keep the "Try to break it" opening of Critical Mindset -- this is effective WHY-focused behavioral guidance per the research.
- The emphasis hierarchy (from CONTEXT.md): safety-critical > behavioral identity > procedural > guidance > informational. Only use "Do not" for safety-critical constraints (output domain boundaries).

**4. Preserve integration contracts:**

- All 15 workflow step headings must remain (Step 1 through Step 15) with the same names
- The Scores table format, criterion names, threshold values, and rubric descriptors in Step 12 stay exactly as-is (appdev-cli regex contract)
- The `evaluation/round-N/EVALUATION.md` output path stays as-is
- The `## Verdict: PASS/FAIL` format stays as-is
- All existing Read instructions for EVALUATION-TEMPLATE.md, SCORING-CALIBRATION.md, and AI-PROBING-REFERENCE.md stay as-is
- The `tools: ["Read", "Write", "Glob", "Bash"]` frontmatter stays as-is
- The description field with examples stays as-is

**5. Section ordering (matches research recommendation):**

1. YAML frontmatter (unchanged)
2. Role statement + Critical Mindset (behavioral identity first)
3. Brief AI slop awareness note with Read pointer (shortened from full inline checklist)
4. Workflow (Steps 1-15, the bulk of the file)
5. Rules (output-domain constraints, fresh in context before completion)
6. Step 14 Self-Verification is already the last workflow step (correct position)

Target: ~370 lines (down from 465).
  </action>
  <verify>
    <automated>wc -l plugins/application-dev/agents/evaluator.md && git grep -c "Self-Verification" plugins/application-dev/agents/evaluator.md && git grep -c "NEVER\|MUST\|CRITICAL\|ALWAYS" plugins/application-dev/agents/evaluator.md</automated>
  </verify>
  <done>evaluator.md is under 400 lines. "Self-Verification" appears exactly once (as "Step 14: Self-Verification" heading -- the grep count should be 1-2 for the heading plus its mention in the brief note, but the standalone section at the end is gone). ALL-CAPS emphasis count is 0 (or near-0 -- only acceptable in the YAML frontmatter description field if needed). All 15 workflow steps present. All Read instructions for existing reference files preserved. Two new Read instructions added (AI-SLOP-CHECKLIST.md, ASSET-VALIDATION-PROTOCOL.md).</done>
</task>

</tasks>

<verification>
After both tasks:
1. `wc -l plugins/application-dev/agents/evaluator.md` shows under 400 lines
2. `git grep -c "## Self-Verification" plugins/application-dev/agents/evaluator.md` returns 0 (only "### Step 14: Self-Verification" remains, no standalone `## Self-Verification`)
3. All 5 evaluator reference files exist: `ls plugins/application-dev/skills/application-dev/references/evaluator/`
4. `git grep "AI-SLOP-CHECKLIST" plugins/application-dev/agents/evaluator.md` returns at least one match (Read instruction)
5. `git grep "ASSET-VALIDATION-PROTOCOL" plugins/application-dev/agents/evaluator.md` returns at least one match (Read instruction)
6. `git grep "EVALUATION-TEMPLATE" plugins/application-dev/agents/evaluator.md` still returns matches (existing reference preserved)
7. `git grep "SCORING-CALIBRATION" plugins/application-dev/agents/evaluator.md` still returns matches (existing reference preserved)
8. `git grep "Step 15" plugins/application-dev/agents/evaluator.md` returns a match (all 15 steps preserved)
</verification>

<success_criteria>
- evaluator.md under 400 lines (down from 465)
- AI-SLOP-CHECKLIST.md exists with all 6 slop categories
- ASSET-VALIDATION-PROTOCOL.md exists with sub-steps 7a-7g and severity rules
- Self-Verification appears only as Step 14 in the workflow (standalone section removed)
- Zero or near-zero ALL-CAPS emphasis instances (MUST/NEVER/CRITICAL/ALWAYS)
- All 15 workflow steps present with same step names
- All existing reference Read instructions preserved
- Two new reference Read instructions added
</success_criteria>

<output>
After completion, create `.planning/phases/05-optimize-agent-definitions/05-01-SUMMARY.md`
</output>
