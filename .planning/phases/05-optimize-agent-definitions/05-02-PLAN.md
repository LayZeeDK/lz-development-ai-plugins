---
phase: 05-optimize-agent-definitions
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/application-dev/skills/application-dev/SKILL.md
autonomous: true
requirements: [OPT-04, OPT-05]
must_haves:
  truths:
    - "SKILL.md uses imperative/infinitive voice consistently (no second-person 'you' instructions)"
    - "SKILL.md is under 420 lines after trimming educational content"
    - "Workflow section appears before Architecture and Enforcement sections"
    - "All workflow steps (0, 0.5, 1, 2, 3) preserved with same behavior"
    - "appdev-cli integration commands unchanged"
    - "Error Recovery pattern preserved"
    - "File-based communication protocol preserved"
    - "Agent Prompt Protocol preserved"
  artifacts:
    - path: "plugins/application-dev/skills/application-dev/SKILL.md"
      provides: "Optimized orchestrator skill with imperative voice and streamlined structure"
      contains: "## Workflow"
  key_links:
    - from: "plugins/application-dev/skills/application-dev/SKILL.md"
      to: "scripts/appdev-cli.mjs"
      via: "Bash commands in workflow steps"
      pattern: "appdev-cli.mjs"
    - from: "plugins/application-dev/skills/application-dev/SKILL.md"
      to: "agents/planner.md"
      via: "Agent spawn in Step 1"
      pattern: "application-dev:planner"
    - from: "plugins/application-dev/skills/application-dev/SKILL.md"
      to: "agents/generator.md"
      via: "Agent spawn in Step 2"
      pattern: "application-dev:generator"
    - from: "plugins/application-dev/skills/application-dev/SKILL.md"
      to: "agents/evaluator.md"
      via: "Agent spawn in Step 2"
      pattern: "application-dev:evaluator"
---

<objective>
Restructure the orchestrator SKILL.md (461 lines -> ~390 lines) with consistent imperative voice, streamlined section ordering, and trimmed educational content.

Purpose: SKILL.md is the orchestrator skill loaded once per session. It currently uses mixed voice (imperative + second person), contains educational explanations Claude already knows (what GANs are, how escalation works), and places Architecture/Enforcement sections before the Workflow section. Per Anthropic's skill-development guidance, SKILL.md should use imperative voice and put actionable workflow content early. Trimming content Claude already knows reduces noise without losing behavioral guidance.

Output: Restructured SKILL.md with consistent voice, leaner structure, and preserved workflow behavior
</objective>

<execution_context>
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-optimize-agent-definitions/05-CONTEXT.md
@.planning/phases/05-optimize-agent-definitions/05-RESEARCH.md

@plugins/application-dev/skills/application-dev/SKILL.md
@plugins/application-dev/scripts/appdev-cli.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure SKILL.md section ordering and trim educational content</name>
  <files>plugins/application-dev/skills/application-dev/SKILL.md</files>
  <action>
Rewrite SKILL.md applying three optimization levers:

**1. Fix voice to imperative/infinitive consistently:**

Per Anthropic's skill-development guidance, SKILL.md should use imperative form. Scan the entire file and convert:
- "Do not ask the user" -> "Do not ask the user" (already imperative, keep)
- "Do not deviate from this sequence" -> keep (already imperative)
- Any second-person "you" instructions -> rephrase to imperative. Example: "Do NOT assess spec quality -- the Planner self-verifies" is already imperative (keep). But if any "you should" or "you must" exist, convert to direct imperatives.
- Third-person for architecture descriptions: "The Generator and Evaluator form an adversarial pair" -> keep (descriptive, not instructional)

**2. Restructure section ordering:**

Current order:
1. Frontmatter
2. Title + intro paragraph
3. Architecture (educational -- explains GAN analogy, enforcement history)
4. Enforcement Model (explains two-layer design rationale)
5. Rules
6. Workflow (the actual actionable content)
7. Error Recovery
8. Agent Prompt Protocol
9. File-Based Communication

New order:
1. Frontmatter (unchanged -- name, description, license, compatibility, metadata, allowed-tools)
2. Title + intro paragraph (keep, trim to 2-3 lines maximum)
3. Rules (hard constraints first -- fresh in context when Claude reads Workflow)
4. Workflow (the main actionable content, moved up from position 6)
5. Error Recovery (stays after Workflow since it's referenced by workflow steps)
6. Agent Prompt Protocol (reference section, stays after Workflow)
7. File-Based Communication (reference section, stays last)
8. Architecture (moved to end -- design rationale, not instructions. Trim heavily)
9. Enforcement Model (moved to end -- design rationale, not instructions. Trim heavily)

Rationale: Anthropic's prompting docs say "queries at the end can improve response quality by up to 30%." For SKILL.md, this means: put rules and workflow (what Claude acts on) in the middle/end, put reference material (architecture, enforcement) that establishes context early or at the end. Since rules are hard constraints, placing them before the workflow ensures they're loaded before action begins.

**3. Trim educational content Claude already knows:**

In the **Architecture section**, remove or heavily condense:
- The GAN analogy explanation ("just as a GAN discriminator's feedback drives the generator to produce better outputs"). Claude knows what GANs are. Keep the 3-bullet agent list (Planner, Generator, Evaluator) as a quick reference.
- The paragraph about "Separating generation from evaluation prevents the self-praise bias..." -- Claude understands this from the agent definitions themselves.
- The paragraph about convergence detection and cybernetics control theory -- this is educational context. The appdev-cli handles convergence; Claude just reads its JSON output.

In the **Enforcement Model section**, remove or heavily condense:
- The detailed explanation of Layer 1 vs Layer 2 mechanics. The two-layer enforcement is documented in agent definitions. SKILL.md only needs to state: "Role boundaries enforced by tool allowlists (agent frontmatter) and prompt guards (agent instructions). The orchestrator's output domain is .appdev-state.json and .gitignore only."
- The "What is NOT available" subsection (disallowedTools, plugin hooks). This is design rationale for developers, not execution guidance for Claude.

In the **Error Recovery section**, simplify:
- The current ~30 lines can be compressed to ~15 lines. The core pattern is: spawn, if fail retry 2x with same prompt, if still fail ask user. The Agent tool unreliability note and the appdev-cli error handling note are important -- keep those.
- Remove the numbered sub-options under AskUserQuestion ("Retry now", "Resume later", "Abort") -- the state management makes these self-evident.

**4. Preserve all integration contracts:**

These must be preserved EXACTLY (appdev-cli parses them, orchestrator depends on them):
- All `Bash(node ${CLAUDE_PLUGIN_ROOT}/scripts/appdev-cli.mjs ...)` commands
- All `Agent(subagent_type: "application-dev:...")` spawn patterns
- All `Bash(git ...)` commands
- Step numbers and names (Step 0, Step 0.5, Step 1, Step 2, Step 3)
- The convergence check dispatch table (PASS/PLATEAU/REGRESSION/SAFETY_CAP handling)
- The binary check patterns (file-exists checks after each agent)
- The Agent Prompt Protocol exact prompt templates
- The File-Based Communication protocol
- The `allowed-tools` frontmatter field
- The `Note: Run git commands as SEPARATE Bash calls` instruction
- All git tag patterns (appdev/planning-complete, appdev/round-N, appdev/final)

**5. Apply WHY-based rationale where helpful:**

- Rule 1 ("Write is ONLY for .appdev-state.json and .gitignore"): add brief WHY -- "source code, specs, and evaluation artifacts belong to their respective agents"
- Rule 2 ("Never diagnose agent output beyond binary checks"): add brief WHY -- "qualitative assessment of agent output reintroduces the self-praise bias the GAN architecture eliminates"
- Rule 3 ("Never add corrective instructions"): add brief WHY -- "corrective additions accumulate prompt drift and bypass the agent's own self-verification"
- Replace any remaining MUST/NEVER/CRITICAL/ALWAYS with "Do not" + rationale where they exist.

Target: ~390 lines (down from 461). The savings come from trimming Architecture (~20 lines), Enforcement (~20 lines), Error Recovery (~15 lines), and intro paragraph (~5 lines). Workflow content is preserved in full.
  </action>
  <verify>
    <automated>wc -l plugins/application-dev/skills/application-dev/SKILL.md && git grep -c "appdev-cli" plugins/application-dev/skills/application-dev/SKILL.md && git grep "application-dev:planner\|application-dev:generator\|application-dev:evaluator" plugins/application-dev/skills/application-dev/SKILL.md | wc -l</automated>
  </verify>
  <done>SKILL.md is under 420 lines. All appdev-cli command references present (grep count matches or exceeds current count of ~15). All 3 agent spawn patterns present. Workflow steps 0, 0.5, 1, 2, 3 all present. Voice is consistently imperative (no "you should/must" patterns). Educational content trimmed (GAN explanation condensed, enforcement model condensed, error recovery simplified).</done>
</task>

<task type="auto">
  <name>Task 2: Verify SKILL.md integration contracts with smoke test</name>
  <files>plugins/application-dev/skills/application-dev/SKILL.md</files>
  <action>
Run structural verification to confirm no integration contracts were broken during the restructure:

1. Verify appdev-cli commands are all present by grepping for each subcommand:
   - `init`, `exists`, `get`, `update`, `delete`, `complete`, `round-complete`, `get-trajectory`, `check-assets`

2. Verify all agent spawn patterns are present:
   - `application-dev:planner`
   - `application-dev:generator`
   - `application-dev:evaluator`

3. Verify all git operations are present:
   - `git init`, `git add`, `git commit`, `git tag`, `git reset`

4. Verify convergence dispatch table completeness:
   - `PASS`, `PLATEAU`, `REGRESSION`, `SAFETY_CAP` exit conditions all handled

5. Verify binary check patterns are present:
   - `SPEC.md` file-exists check after Planner
   - `package.json` / `index.html` check after Generator
   - `evaluation/round-N/EVALUATION.md` check after Evaluator

6. Run `node plugins/application-dev/scripts/appdev-cli.mjs --help 2>/dev/null; echo $?` to confirm appdev-cli is still functional (the script itself is unchanged, but this verifies the path reference pattern).

If any check fails, fix the SKILL.md to restore the missing contract.
  </action>
  <verify>
    <automated>git grep -c "round-complete" plugins/application-dev/skills/application-dev/SKILL.md && git grep -c "SAFETY_CAP" plugins/application-dev/skills/application-dev/SKILL.md && git grep -c "application-dev:" plugins/application-dev/skills/application-dev/SKILL.md</automated>
  </verify>
  <done>All appdev-cli subcommands referenced in SKILL.md. All 3 agent spawn patterns present. All 4 exit conditions handled in convergence dispatch. All binary checks present. appdev-cli script runs without error.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `wc -l plugins/application-dev/skills/application-dev/SKILL.md` shows under 420 lines
2. All appdev-cli subcommands present (init, exists, get, update, delete, complete, round-complete, get-trajectory)
3. All 3 agent types spawned (planner, generator, evaluator)
4. All 4 convergence exit conditions handled (PASS, PLATEAU, REGRESSION, SAFETY_CAP)
5. Git operations preserved (init, add, commit, tag, reset)
6. Workflow steps preserved (Step 0, Step 0.5, Step 1, Step 2, Step 3)
7. Agent Prompt Protocol section preserved with exact prompt templates
8. File-Based Communication section preserved
9. allowed-tools frontmatter unchanged
</verification>

<success_criteria>
- SKILL.md under 420 lines (down from 461)
- Imperative voice used consistently (no "you should/must" patterns)
- Rules section appears before Workflow section
- Architecture and Enforcement sections moved after Workflow (or heavily trimmed in place)
- All workflow steps preserved with identical behavior
- All integration contracts preserved (appdev-cli commands, agent spawns, git operations, convergence dispatch)
- WHY-based rationale added to Rules
- Educational content trimmed (GAN explanation, enforcement mechanics, error recovery detail)
</success_criteria>

<output>
After completion, create `.planning/phases/05-optimize-agent-definitions/05-02-SUMMARY.md`
</output>
