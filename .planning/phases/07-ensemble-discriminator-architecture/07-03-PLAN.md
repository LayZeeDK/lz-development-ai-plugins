---
phase: 07-ensemble-discriminator-architecture
plan: 03
type: execute
wave: 2
depends_on:
  - 07-01
  - 07-02
files_modified:
  - plugins/application-dev/agents/perceptual-critic.md
  - plugins/application-dev/agents/projection-critic.md
  - plugins/application-dev/agents/evaluator.md
autonomous: true
requirements:
  - ENSEMBLE-01
  - ENSEMBLE-02
  - ENSEMBLE-05
  - BARRIER-01
  - BARRIER-02
  - BARRIER-03
  - BARRIER-04

must_haves:
  truths:
    - "perceptual-critic.md exists as a compact agent definition that scores Visual Design via eval-first methodology"
    - "projection-critic.md exists as a compact agent definition that scores Functionality via write-and-run acceptance tests"
    - "Neither critic tool allowlist permits reading application source code"
    - "Neither critic tool allowlist permits writing outside evaluation/ directory"
    - "Both critics write structured summary.json to evaluation/round-N/{critic-name}/"
    - "Findings describe behavioral symptoms, not code-level diagnoses"
    - "evaluator.md is deleted"
    - "Generator dev tests and critic acceptance tests are documented as independent"
  artifacts:
    - path: "plugins/application-dev/agents/perceptual-critic.md"
      provides: "Visual Design critic agent definition"
      contains: "perceptual-critic"
      min_lines: 50
    - path: "plugins/application-dev/agents/projection-critic.md"
      provides: "Functionality critic agent definition"
      contains: "projection-critic"
      min_lines: 50
  key_links:
    - from: "perceptual-critic.md"
      to: "summary.json schema"
      via: "Writes perceptual/summary.json with universal fields"
      pattern: "summary\\.json"
    - from: "projection-critic.md"
      to: "summary.json schema"
      via: "Writes projection/summary.json with universal + acceptance_tests fields"
      pattern: "summary\\.json"
    - from: "perceptual-critic.md"
      to: "AI-SLOP-CHECKLIST.md"
      via: "Progressive disclosure reference"
      pattern: "AI-SLOP-CHECKLIST"
    - from: "projection-critic.md"
      to: "AI-PROBING-REFERENCE.md"
      via: "Progressive disclosure reference"
      pattern: "AI-PROBING-REFERENCE"
---

<objective>
Create two WGAN critic agent definitions (perceptual-critic.md and
projection-critic.md) replacing the monolithic evaluator. Each critic is compact
(target under 150 lines), scores one dimension in its own isolated context. The GAN
information barrier is enforced through tool allowlists (first layer) and prompt
guards (second layer). Delete the old evaluator.md.

Purpose: Core architectural change -- splitting the 392-line evaluator into two
focused critics within safe context limits (~60K tokens each). Prevents the context
exhaustion crash from the Dutch art museum test.

Output: Two new critic agent files, one deleted evaluator file.
</objective>

<execution_context>
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ensemble-discriminator-architecture/07-CONTEXT.md
@.planning/phases/07-ensemble-discriminator-architecture/07-RESEARCH.md
@.planning/phases/07-ensemble-discriminator-architecture/07-01-SUMMARY.md

<interfaces>
From plugins/application-dev/agents/evaluator.md (frontmatter pattern):
```yaml
---
name: evaluator
description: |
  Use this agent to evaluate a running application against its product specification.
  Spawned by the application-dev orchestrator skill. Should not be triggered directly by users.
model: inherit
color: yellow
tools: ["Read", "Write", "Glob", "Bash"]
---
```

New critic frontmatter pattern (from 07-RESEARCH.md Pattern 6):
```yaml
---
name: perceptual-critic
description: |
  Use this agent to evaluate a running application's visual design quality.
  Spawned by the application-dev orchestrator during evaluation phase.
  Should not be triggered directly by users.
model: inherit
color: yellow
tools: ["Read", "Write", "Bash(npx playwright-cli *)", "Bash(node *appdev-cli* install-dep *)", "Bash(node *appdev-cli* check-assets *)"]
---
```

summary.json universal schema (from 07-CONTEXT.md):
- critic, dimension, score, threshold, pass, findings[], ceiling_applied, justification, off_spec_features[]
- Projection-critic extension: acceptance_tests { total, passed, failed, skipped, results[] }

Perceptual-critic references:
- ${CLAUDE_PLUGIN_ROOT}/skills/application-dev/references/evaluator/AI-SLOP-CHECKLIST.md
- ${CLAUDE_PLUGIN_ROOT}/skills/application-dev/references/evaluator/SCORING-CALIBRATION.md
- ${CLAUDE_PLUGIN_ROOT}/skills/application-dev/references/evaluator/ASSET-VALIDATION-PROTOCOL.md

Projection-critic references:
- ${CLAUDE_PLUGIN_ROOT}/skills/application-dev/references/evaluator/AI-PROBING-REFERENCE.md
- ${CLAUDE_PLUGIN_ROOT}/skills/application-dev/references/evaluator/SCORING-CALIBRATION.md

Evaluator capability decomposition (from 07-CONTEXT.md):
- perceptual-critic: Visual assessment via eval-first, responsive resize+eval, AI slop, asset quality, visual regression, console error (visual)
- projection-critic: Feature testing via write-and-run, AI probing, off-spec detection, asset coverage, behavioral regression, console error (functional)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create perceptual-critic.md and projection-critic.md agent definitions</name>
  <files>plugins/application-dev/agents/perceptual-critic.md, plugins/application-dev/agents/projection-critic.md</files>
  <action>
    Create both critic agent definitions. Each under 150 lines, following established
    agent patterns (CONVENTIONS.md) and SkillsBench "detailed, not comprehensive".

    **perceptual-critic.md** (Visual Design, Perceptual discriminator Section 7.3):

    Frontmatter tools: ["Read", "Write", "Bash(npx playwright-cli *)", "Bash(node *appdev-cli* install-dep *)", "Bash(node *appdev-cli* check-assets *)"]
    No Glob, no Edit (prevents codebase exploration -- BARRIER-01 first layer).

    Body structure:
    1. Mission: Perceptual discriminator evaluating product surface visual quality.
    2. Hard boundary (BARRIER-01 second layer): "You MUST NOT read application source
       code files (.js, .ts, .tsx, .jsx, .css, .html, .json except package.json and
       summary.json). Your evaluation is product-surface only. Why: discriminator
       judges output, not process."
    3. Write restriction (BARRIER-03): "Write ONLY to evaluation/round-N/perceptual/."
    4. Methodology phases (not numbered steps):
       - UNDERSTAND: Read SPEC.md for design language, layout, typography, color, assets.
       - OBSERVE: eval-first for structured page state (TOKEN-02), screenshot at key
         viewpoints only, resize+eval for responsive (Multi-Scale 1.3), console error
         filtered (TOKEN-04).
       - DETECT: Load AI-SLOP-CHECKLIST.md. Check slop patterns, placeholder images,
         watermarks, font mismatches, asset quality via perceptual observation.
       - SCORE: Load SCORING-CALIBRATION.md (Visual Design section). Apply ceilings.
         Score against spec's design language. Cite findings by ID.
       - REPORT: Write summary.json to evaluation/round-N/perceptual/summary.json.
    5. Summary.json schema with exact fields.
    6. Finding format (BARRIER-02): behavioral symptoms only, no code-level diagnoses.
    7. Tooling: install sharp/imghash/leven via appdev-cli install-dep if needed.
    8. Token efficiency: ~60K context budget, write to file not context.
    9. CLAUDE_AUTOCOMPACT_PCT_OVERRIDE=50 recommendation (RECOVERY-04 prep).

    **projection-critic.md** (Functionality, Projection discriminator Section 3.3):

    Frontmatter tools: ["Read", "Write", "Bash(npx playwright-cli *)", "Bash(node *appdev-cli* install-dep *)", "Bash(npx playwright test *)"]
    No Glob, no Edit.

    Body structure:
    1. Mission: Projection discriminator evaluating functional coverage via write-and-run
       acceptance tests. Scores Functionality dimension.
    2. Same hard boundary and write restriction as perceptual-critic.
       Write restriction: "Write ONLY to evaluation/round-N/projection/."
    3. Methodology phases:
       - UNDERSTAND: Read SPEC.md for features, acceptance criteria, AI integration points.
       - TEST: Write-and-run pattern (TOKEN-03): read SPEC criteria, take 1 snapshot for
         selector discovery, write acceptance-tests.spec.ts to evaluation/round-N/projection/,
         run via `npx playwright test --reporter=json`, read JSON results. ~5 tool calls
         replace ~30+ interactive calls.
       - PROBE: Load AI-PROBING-REFERENCE.md. Run probe battery for AI features --
         variability test, domain test, adversarial test. Detect canned vs real AI.
       - SCORE: Load SCORING-CALIBRATION.md (Functionality + Product Depth sections).
         Apply ceilings. Include acceptance_tests results in summary.json for CLI Product
         Depth computation.
       - REPORT: Write summary.json to evaluation/round-N/projection/summary.json with
         both universal fields AND acceptance_tests extension.
    4. Summary.json schema with universal fields + acceptance_tests extension.
    5. Finding format (BARRIER-02): behavioral symptoms only.
    6. Off-spec feature detection: features in app not in SPEC.md go in off_spec_features[].
    7. Acceptance test independence (BARRIER-04): "Your acceptance tests in
       evaluation/round-N/projection/ are INDEPENDENT from the Generator's dev tests
       in tests/. Different purposes: yours verify SPEC conformance from the outside,
       theirs verify implementation from the inside."
    8. Tooling: install ajv via appdev-cli install-dep if app has API endpoints.
    9. Token efficiency: ~60K context budget, write-and-run keeps tests outside context.
    10. CLAUDE_AUTOCOMPACT_PCT_OVERRIDE=50 recommendation.

    Both agents use WHY-based rationale and progressive disclosure (reference files for
    protocol-heavy content).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const pc=fs.readFileSync('plugins/application-dev/agents/perceptual-critic.md','utf8'); const pj=fs.readFileSync('plugins/application-dev/agents/projection-critic.md','utf8'); const r={pc_lines:pc.split('\n').length, pj_lines:pj.split('\n').length, pc_barrier:pc.includes('MUST NOT read application source code'), pj_barrier:pj.includes('MUST NOT read application source code'), pc_schema:pc.includes('summary.json'), pj_schema:pj.includes('summary.json'), pj_acceptance:pj.includes('acceptance_tests'), pc_no_glob:!pc.includes('\"Glob\"'), pj_no_glob:!pj.includes('\"Glob\"')}; console.log(JSON.stringify(r))"</automated>
  </verify>
  <done>
    Both critic agent files exist with correct frontmatter (name, tools, model, color),
    GAN information barrier enforced at both layers (tool allowlist + prompt guards),
    methodology sections using eval-first (perceptual) and write-and-run (projection),
    summary.json output schema documented, finding format enforcing behavioral symptoms.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete evaluator.md (ENSEMBLE-05)</name>
  <files>plugins/application-dev/agents/evaluator.md</files>
  <action>
    Delete the monolithic evaluator.md agent definition. It is replaced by the two
    critic agents created in Task 1 (perceptual-critic.md and projection-critic.md).

    Use `git rm plugins/application-dev/agents/evaluator.md` to remove the file and
    stage the deletion. The file is preserved in git history for reference.

    After deletion, verify the agents/ directory contains exactly:
    - generator.md
    - planner.md
    - perceptual-critic.md
    - projection-critic.md

    No evaluator.md should remain.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const exists=fs.existsSync('plugins/application-dev/agents/evaluator.md'); const agents=fs.readdirSync('plugins/application-dev/agents/').sort(); console.log(JSON.stringify({evaluator_deleted:!exists, agents:agents}))"</automated>
  </verify>
  <done>
    evaluator.md is deleted from the working tree. agents/ directory contains exactly
    generator.md, perceptual-critic.md, planner.md, projection-critic.md.
  </done>
</task>

</tasks>

<verification>
1. `ls plugins/application-dev/agents/` shows 4 files: generator.md, perceptual-critic.md, planner.md, projection-critic.md
2. `ls plugins/application-dev/agents/evaluator.md` fails (file does not exist)
3. Both critic files contain "MUST NOT read application source code" (BARRIER-01)
4. Both critic files contain "summary.json" output schema
5. Neither critic file contains "Glob" in the tools array
6. projection-critic.md contains "acceptance_tests" schema extension
7. Both critic files are under 150 lines
</verification>

<success_criteria>
- perceptual-critic.md is a complete, compact agent definition for Visual Design evaluation
- projection-critic.md is a complete, compact agent definition for Functionality evaluation
- GAN information barrier enforced at both tool allowlist and prompt guard layers
- Both critics document summary.json output format with correct schema
- evaluator.md is deleted
- Finding format enforces behavioral symptoms over code diagnoses
- Acceptance test independence documented (BARRIER-04)
</success_criteria>

<output>
After completion, create `.planning/phases/07-ensemble-discriminator-architecture/07-03-SUMMARY.md`
</output>
