---
phase: 07-ensemble-discriminator-architecture
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/application-dev/skills/application-dev/references/evaluator/EVALUATION-TEMPLATE.md
  - plugins/application-dev/skills/application-dev/references/evaluator/SCORING-CALIBRATION.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - ENSEMBLE-07
  - ENSEMBLE-08

must_haves:
  truths:
    - "EVALUATION-TEMPLATE.md uses 3 scoring dimensions (Product Depth, Functionality, Visual Design) with no Code Quality"
    - "EVALUATION-TEMPLATE.md has provenance markers (Source: Perceptual Critic, Source: Projection Critic, Source: CLI Ensemble)"
    - "EVALUATION-TEMPLATE.md scores table regex matches the pattern used by extractScores() in appdev-cli.mjs"
    - "SCORING-CALIBRATION.md has ceiling rules for only 3 dimensions"
    - "SCORING-CALIBRATION.md threshold table shows PD 7, Fn 7, VD 6 with no Code Quality row"
    - "ROADMAP.md WGAN Critic Roadmap table uses perceptual-critic/projection-critic naming"
    - "REQUIREMENTS.md uses perceptual-critic/projection-critic naming with no precision-critic/recall-critic references"
  artifacts:
    - path: "plugins/application-dev/skills/application-dev/references/evaluator/EVALUATION-TEMPLATE.md"
      provides: "CLI-compiled EVALUATION.md template with 3 dimensions and provenance"
      contains: "Source: CLI Ensemble"
    - path: "plugins/application-dev/skills/application-dev/references/evaluator/SCORING-CALIBRATION.md"
      provides: "Scoring calibration for 3 dimensions"
      contains: "Product Depth"
    - path: ".planning/ROADMAP.md"
      provides: "Updated naming convention in WGAN Critic Roadmap"
      contains: "perceptual-critic"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated naming convention across all requirement descriptions"
      contains: "perceptual-critic"
  key_links:
    - from: "EVALUATION-TEMPLATE.md"
      to: "appdev-cli.mjs extractScores()"
      via: "Regex-sensitive scores table format"
      pattern: "Product Depth.*Functionality.*Visual Design"
    - from: "SCORING-CALIBRATION.md"
      to: "EVALUATION-TEMPLATE.md"
      via: "Threshold values and dimension names must match"
      pattern: "Product Depth.*7.*Functionality.*7.*Visual Design.*6"
---

<objective>
Redesign EVALUATION-TEMPLATE.md as a CLI-compiled output template with 3 scoring
dimensions and provenance sections. Update SCORING-CALIBRATION.md to remove Code
Quality and align with the new 3-dimension model. Update ROADMAP.md and
REQUIREMENTS.md to use technique-based naming convention
(perceptual-critic/projection-critic).

Purpose: The template and calibration files define the scoring contract between
critics, CLI, and Generator. They must be updated atomically with the dimension
change (Pitfall 1). The ROADMAP and REQUIREMENTS naming updates ensure all
project documentation uses the locked technique-based naming convention.

Output: Updated template, calibration reference, ROADMAP, and REQUIREMENTS with
consistent naming.
</objective>

<execution_context>
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ensemble-discriminator-architecture/07-CONTEXT.md

<interfaces>
<!-- Current EVALUATION-TEMPLATE.md regex contract (from appdev-cli.mjs line 92) -->
Current regex: /\|\s*(Product Depth|Functionality|Visual Design|Code Quality)\s*\|\s*(\d+)\/10/gi
New regex:     /\|\s*(Product Depth|Functionality|Visual Design)\s*\|\s*(\d+)\/10/gi

Current verdict regex: /##\s*Verdict:\s*(PASS|FAIL)/
New verdict: CLI-computed, but still written to EVALUATION.md in the same heading format

<!-- Current SCORING-CALIBRATION.md thresholds -->
Current: PD 7, Fn 7, VD 6, CQ 6 (total /40)
New:     PD 7, Fn 7, VD 6 (total /30)

<!-- New EVALUATION-TEMPLATE.md structure from 07-RESEARCH.md -->
Sections:
1. Verdict (CLI-computed)
2. Scores table (3 dimensions, regex-sensitive)
3. Score Justifications table
4. Product Depth Assessment (Source: CLI Ensemble)
5. Functionality Assessment (Source: Projection Critic)
6. Visual Design Assessment (Source: Perceptual Critic)
7. Priority Fixes for Next Round (Source: CLI Ensemble)

<!-- ROADMAP naming updates needed -->
v1.1: precision-critic -> perceptual-critic, recall-critic -> projection-critic
v1.2: Keep spectral-critic naming
v2.0: Keep semantic-critic, fairness-critic naming
Table columns: Critic agent | GAN discriminator type | Dimension | Method

<!-- REQUIREMENTS.md naming updates needed -->
Stale references across: ENSEMBLE-01, ENSEMBLE-02, ENSEMBLE-03, ENSEMBLE-05,
ENSEMBLE-06, ENSEMBLE-07, ENSEMBLE-09, RECOVERY-01, RECOVERY-02, TOKEN-02,
TOKEN-03, BARRIER-04, PLAYWRIGHT-02, and v1.2 section (CRITIC-02, CRITIC-03).
Also update directory names: precision/ -> perceptual/, recall/ -> projection/.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign EVALUATION-TEMPLATE.md and SCORING-CALIBRATION.md for 3 dimensions</name>
  <files>plugins/application-dev/skills/application-dev/references/evaluator/EVALUATION-TEMPLATE.md, plugins/application-dev/skills/application-dev/references/evaluator/SCORING-CALIBRATION.md</files>
  <action>
    EVALUATION-TEMPLATE.md: Replace the current monolithic-evaluator template with
    a CLI-compiled output template. The new template:

    1. Keep the regex-sensitive WARNING comment at the top, but update it:
       - Remove "verdict heading" from warning (verdict is CLI-computed, not agent-written)
       - Update the regex pattern shown in comments to show 3 dimensions
       - Add note: "This file is a TEMPLATE for CLI compile-evaluation output. The CLI
         fills in placeholders at compile time. No agent writes this file directly."

    2. Structure sections in this order (from 07-CONTEXT.md locked decision):
       - `# Evaluation Report -- {product_name}`
       - `## Generation Round: {round}`
       - `## Verdict: {verdict}` (keep heading format for extractScores compatibility,
         add comment: "Verdict is CLI-computed from per-criterion thresholds")
       - `## Scores` table with 3 rows: Product Depth (threshold 7), Functionality
         (threshold 7), Visual Design (threshold 6). Remove Code Quality row entirely.
         Keep the REGEX-SENSITIVE comment with updated pattern.
       - `## Score Justifications` table with 3 rows (remove Code Quality)
       - `## Product Depth Assessment` with provenance: `*Source: CLI Ensemble
         (computed from acceptance test results)*`
       - `## Functionality Assessment` with provenance: `*Source: Projection Critic*`
       - `## Visual Design Assessment` with provenance: `*Source: Perceptual Critic*`
       - `## Priority Fixes for Next Round` with provenance: `*Source: CLI Ensemble
         (merged from both critics, severity-ordered)*`

    3. Remove these sections entirely (no longer applicable with ensemble):
       - Code Quality Assessment section
       - Asset Validation section (decomposed across ensemble -- perceptual-critic
         handles visual assets, projection-critic handles coverage via acceptance
         tests, CLI handles URL checking via check-assets)
       - AI Feature Probing section (moved into projection-critic's methodology)
       - Console & Errors section (each critic handles its own console monitoring)
       - Off-Spec Features section (moved into critic summary.json off_spec_features)
       - Regressions section (each critic handles its own regression detection)

    4. Use {placeholder} syntax for all variable content that the CLI fills in.

    SCORING-CALIBRATION.md: Update for 3 dimensions:

    1. Remove the entire "Code Quality" section from Hard Score Ceiling Rules
    2. Remove "Code Quality" calibration scenarios section
    3. Update the "No Averaging or Trading" section threshold list: remove CQ 6
    4. Keep Functionality, Product Depth, Visual Design ceiling rules unchanged
    5. Keep "Browser AI Degradation" cross-criterion ceilings unchanged
    6. Keep all calibration scenarios for PD, Fn, VD unchanged
    7. Update the "Loaded at" note to reference the new workflow: "Loaded by:
       perceptual-critic (Visual Design ceilings), projection-critic (Functionality
       and Product Depth ceilings), CLI compile-evaluation (all ceilings for
       cross-validation)"
    8. Remove any references to "evaluator.md" or "Step 12" -- replace with
       references to the appropriate critic agent
  </action>
  <verify>
    <automated>git grep -c "Code Quality" plugins/application-dev/skills/application-dev/references/evaluator/EVALUATION-TEMPLATE.md plugins/application-dev/skills/application-dev/references/evaluator/SCORING-CALIBRATION.md 2>/dev/null | rg -v ":0$"</automated>
    The command should produce NO output (zero matches for "Code Quality" in both files).
    Also verify: `git grep "Source: CLI Ensemble" plugins/application-dev/skills/application-dev/references/evaluator/EVALUATION-TEMPLATE.md` returns at least 2 matches.
    Also verify: `git grep "Source: Perceptual Critic\|Source: Projection Critic" plugins/application-dev/skills/application-dev/references/evaluator/EVALUATION-TEMPLATE.md` returns at least 2 matches.
  </verify>
  <done>
    EVALUATION-TEMPLATE.md has 3 dimensions, provenance markers for all sections,
    no Code Quality references, and regex-sensitive scores table matching the
    3-dimension pattern. SCORING-CALIBRATION.md has ceiling rules and calibration
    scenarios for 3 dimensions only, no Code Quality section.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update ROADMAP.md WGAN Critic Roadmap with technique-based naming</name>
  <files>.planning/ROADMAP.md</files>
  <action>
    Update ROADMAP.md to use the technique-based naming convention locked in
    07-CONTEXT.md. Changes:

    1. In the "v1.1 Critics (essential -- crash-fix release)" table:
       - `precision-critic` -> `perceptual-critic`
       - `recall-critic` -> `projection-critic`
       - Update GAN discriminator type column to match: "Perceptual + Multi-Scale + Style"
         for perceptual-critic (Section 7.3), "Projection + ProjectedGAN" for
         projection-critic (Section 3.3)
       - Keep CLI compile-evaluation row as-is (it's already named correctly)

    2. In the "v1.2 Critics (Dutch art museum fixes)" table:
       - `perturbation-critic` stays (already technique-based)
       - `precision-critic (enhanced)` -> `perceptual-critic (enhanced)`
       - `recall-critic (enhanced)` -> `projection-critic (enhanced)`

    3. In the "v2.0+ Critics (future expansion)" table:
       - All already technique-based, no changes needed

    4. In Phase 7 description:
       - Update success criteria references from "precision-critic" to
         "perceptual-critic" and "recall-critic" to "projection-critic"
       - Update `precision/summary.json` -> `perceptual/summary.json`
       - Update `recall/summary.json` -> `projection/summary.json`
       - Update `perturbation/summary.json` reference to note it stays as-is

    5. In Phase 8 description:
       - Update "recall-critic" references to "projection-critic"
       - Update "precision-critic" to "perceptual-critic" where referenced

    6. In Phase 9 description:
       - Update "precision/summary.json" -> "perceptual/summary.json"
       - Update "recall/summary.json" -> "projection/summary.json"

    7. In "Scoring dimensions by milestone" table:
       - No changes needed (dimensions, not critic names)

    Use precise text replacement. Do not rewrite sections -- only change the
    specific naming references.
  </action>
  <verify>
    <automated>git grep -n "precision-critic\|recall-critic" .planning/ROADMAP.md | rg -v "^$"</automated>
    The command should produce NO output (zero remaining old-style naming in ROADMAP.md).
    Also verify: `git grep -c "perceptual-critic" .planning/ROADMAP.md` returns a count > 0.
    Also verify: `git grep -c "projection-critic" .planning/ROADMAP.md` returns a count > 0.
  </verify>
  <done>
    ROADMAP.md uses perceptual-critic/projection-critic naming consistently across
    all v1.1, v1.2, and Phase 7/8/9 references. No precision-critic or recall-critic
    references remain.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update REQUIREMENTS.md with technique-based naming</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    Replace all deprecated precision-critic/recall-critic naming in REQUIREMENTS.md
    with the locked technique-based naming from 07-CONTEXT.md. This covers the
    full document -- v1.1 requirements, v1.2 requirements, and the intro paragraph.

    Specific replacements (case-sensitive where capitalized in original):

    v1.1 intro paragraph (line 9-11 area):
    - "Precision and Recall critics" -> "Perceptual and Projection critics"

    ENSEMBLE section:
    - ENSEMBLE-01: `precision-critic` -> `perceptual-critic`, "Precision discriminator" -> "Perceptual discriminator"
    - ENSEMBLE-02: `recall-critic` -> `projection-critic`, "Recall/Coverage discriminator" -> "Projection discriminator"
    - ENSEMBLE-03: `precision/summary.json + recall/summary.json` -> `perceptual/summary.json + projection/summary.json`
    - ENSEMBLE-05: `precision-critic + recall-critic` -> `perceptual-critic + projection-critic`
    - ENSEMBLE-06: `recall-critic` -> `projection-critic`, `precision-critic` -> `perceptual-critic`
    - ENSEMBLE-07: `Precision Critic, Recall Critic` -> `Perceptual Critic, Projection Critic`
    - ENSEMBLE-09: `precision/`, `recall/` -> `perceptual/`, `projection/`

    PLAYWRIGHT section:
    - PLAYWRIGHT-02: `Recall-critic` -> `Projection-critic`

    TOKEN section:
    - TOKEN-02: `Precision-critic` -> `Perceptual-critic`
    - TOKEN-03: `Recall-critic` -> `Projection-critic`

    RECOVERY section:
    - RECOVERY-01: `precision/summary.json, recall/summary.json` -> `perceptual/summary.json, projection/summary.json`
    - RECOVERY-02: `precision done, recall incomplete -> spawn recall-critic only` -> `perceptual done, projection incomplete -> spawn projection-critic only`

    BARRIER section:
    - BARRIER-04: `recall-critic's` -> `projection-critic's`

    v1.2 section:
    - CRITIC-02: `Precision-critic enhanced` -> `Perceptual-critic enhanced`
    - CRITIC-03: `Recall-critic enhanced` -> `Projection-critic enhanced`

    Do NOT change:
    - v2.0+ section (already uses semantic-critic, fairness-critic -- correct)
    - Dimension names (Product Depth, Functionality, Visual Design -- not critic names)
    - "perturbation-critic" references (already technique-based)
    - Out of Scope table (does not use precision/recall-critic naming)

    After all replacements, verify zero occurrences of "precision-critic" and
    "recall-critic" remain in the file. The terms "Precision" and "Recall" may
    still appear in GAN-theoretical contexts (e.g., "GAN Precision and Recall")
    but NOT as agent/critic names.
  </action>
  <verify>
    <automated>git grep -c "precision-critic\|recall-critic\|Precision-critic\|Recall-critic" .planning/REQUIREMENTS.md 2>/dev/null | rg -v ":0$"</automated>
    The command should produce NO output (zero matches for old-style critic naming).
    Also verify: `git grep -c "perceptual-critic\|projection-critic" .planning/REQUIREMENTS.md` returns a count > 0.
  </verify>
  <done>
    REQUIREMENTS.md uses perceptual-critic/projection-critic naming consistently
    across all v1.1 requirements (ENSEMBLE, PLAYWRIGHT, TOKEN, RECOVERY, BARRIER),
    v1.2 requirements (CRITIC-02, CRITIC-03), and the intro paragraph. No
    precision-critic or recall-critic references remain.
  </done>
</task>

</tasks>

<verification>
1. `git grep "Code Quality" plugins/application-dev/skills/application-dev/references/evaluator/EVALUATION-TEMPLATE.md` returns nothing
2. `git grep "Code Quality" plugins/application-dev/skills/application-dev/references/evaluator/SCORING-CALIBRATION.md` returns nothing
3. `git grep "precision-critic\|recall-critic" .planning/ROADMAP.md` returns nothing
4. `git grep "precision-critic\|recall-critic" .planning/REQUIREMENTS.md` returns nothing
5. `git grep "Source: CLI Ensemble" plugins/application-dev/skills/application-dev/references/evaluator/EVALUATION-TEMPLATE.md` returns 2+ matches
6. `git grep "perceptual-critic" .planning/ROADMAP.md` returns matches
7. `git grep "perceptual-critic" .planning/REQUIREMENTS.md` returns matches
8. EVALUATION-TEMPLATE.md scores table has exactly 3 dimension rows
</verification>

<success_criteria>
- EVALUATION-TEMPLATE.md is a CLI-compiled output template with 3 dimensions and provenance
- SCORING-CALIBRATION.md has no Code Quality dimension
- ROADMAP.md uses technique-based naming throughout
- REQUIREMENTS.md uses technique-based naming throughout -- no stale precision-critic/recall-critic references
- The regex-sensitive scores table format is preserved and matches 3-dimension extractScores()
</success_criteria>

<output>
After completion, create `.planning/phases/07-ensemble-discriminator-architecture/07-02-SUMMARY.md`
</output>
