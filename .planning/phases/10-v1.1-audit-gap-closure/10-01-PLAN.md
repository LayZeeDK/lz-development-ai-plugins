---
phase: 10-v1.1-audit-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/application-dev/agents/perceptual-critic.md
  - plugins/application-dev/agents/projection-critic.md
  - plugins/application-dev/skills/application-dev/SKILL.md
  - plugins/application-dev/skills/application-dev/references/evaluator/PLAYWRIGHT-EVALUATION.md
autonomous: true
requirements:
  - ENSEMBLE-04
  - RECOVERY-03
  - PLAYWRIGHT-02
  - PLAYWRIGHT-04

must_haves:
  truths:
    - "Perceptual-critic install-dep calls use --package flag matching CLI parseArgs"
    - "Projection-critic install-dep calls use --package flag matching CLI parseArgs"
    - "SAFETY_CAP wrap-up path stops static-serve before spawning wrap-up Generator"
    - "@playwright/test is installed in Step 0.5 alongside @playwright/cli"
    - "PLAYWRIGHT-EVALUATION.md skeleton test has explicit test.use({ baseURL }) configuration"
    - "Projection-critic Step 0 specifies reading port from static-serve JSON for baseURL"
  artifacts:
    - path: "plugins/application-dev/agents/perceptual-critic.md"
      provides: "Fixed install-dep calling convention"
      contains: "install-dep --package"
    - path: "plugins/application-dev/agents/projection-critic.md"
      provides: "Fixed install-dep calling convention"
      contains: "install-dep --package"
    - path: "plugins/application-dev/skills/application-dev/SKILL.md"
      provides: "SAFETY_CAP --stop fix and @playwright/test installation"
      contains: "static-serve --stop"
    - path: "plugins/application-dev/skills/application-dev/references/evaluator/PLAYWRIGHT-EVALUATION.md"
      provides: "baseURL configuration in skeleton test"
      contains: "test.use"
  key_links:
    - from: "perceptual-critic.md install-dep call"
      to: "appdev-cli.mjs cmdInstallDep --package flag"
      via: "Bash tool invocation"
      pattern: "install-dep --package"
    - from: "projection-critic.md install-dep call"
      to: "appdev-cli.mjs cmdInstallDep --package flag"
      via: "Bash tool invocation"
      pattern: "install-dep --package"
    - from: "SKILL.md SAFETY_CAP path"
      to: "static-serve --stop"
      via: "Bash tool invocation before wrap-up Generator spawn"
      pattern: "static-serve --stop.*wrap-up"
    - from: "projection-critic acceptance tests"
      to: "static-serve port"
      via: "test.use({ baseURL }) in test file"
      pattern: "baseURL.*localhost"
---

<objective>
Fix four integration bugs found by the v1.1 milestone audit that would cause runtime failures during real usage: install-dep calling convention mismatch, SAFETY_CAP stale build, missing @playwright/test dependency, and missing baseURL configuration.

Purpose: These are the only true bugs (vs. cleanup/verification debt) -- they would cause critics to fail during actual application development sessions.
Output: Four surgical fixes to agent definitions, SKILL.md, and PLAYWRIGHT-EVALUATION.md. Zero CLI code changes. All 57 existing CLI tests continue to pass.
</objective>

<execution_context>
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LarsGyrupBrinkNielse/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-v1.1-audit-gap-closure/10-RESEARCH.md

<interfaces>
<!-- Key interfaces the executor needs. Extracted from codebase. -->

From appdev-cli.mjs parseArgs (line 47-85):
- parseArgs consumes one token per flag: `--flag value`
- Special multi-token consumption only for `--prompt`
- cmdInstallDep reads `args["package"]` -- requires `--package <name>`
- npm install receives packageName verbatim: `npm install --save-dev ${packageName}`

From perceptual-critic.md line 86 (BROKEN):
```
node *appdev-cli* install-dep --dev sharp imghash leven
```

From projection-critic.md line 183 (BROKEN):
```
node *appdev-cli* install-dep --dev ajv
```

From SKILL.md SAFETY_CAP path (lines 358-397):
- Line 362-363: git tag for SAFETY_CAP
- Line 364-366: wrap-up round description
- Line 367: Generator spawn (MISSING static-serve --stop before this)
- Lines 393-396: static-serve --stop exists AFTER wrap-up evaluation (too late)

From SKILL.md should_continue path (lines 405-408) -- the correct pattern:
```
- Stop static servers before the next generation round:
  Bash(node ${CLAUDE_PLUGIN_ROOT}/scripts/appdev-cli.mjs static-serve --stop)
```

From SKILL.md Step 0.5 (lines 141-149):
```
Bash(npm install --save-dev @playwright/cli)
Bash(npm install --save-dev serve)
```
Missing: `Bash(npm install --save-dev @playwright/test)`

From projection-critic.md Step 0 (lines 39-43):
- Lines 41-43 already mention reading port and setting baseURL
- But PLAYWRIGHT-EVALUATION.md skeleton test (lines 42-72) lacks test.use({ baseURL })
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix install-dep calling convention and SAFETY_CAP stale build</name>
  <files>
    plugins/application-dev/agents/perceptual-critic.md
    plugins/application-dev/agents/projection-critic.md
    plugins/application-dev/skills/application-dev/SKILL.md
  </files>
  <action>
**Fix 1: install-dep calling convention (ENSEMBLE-04)**

In `perceptual-critic.md`, replace line 86:
```
node *appdev-cli* install-dep --dev sharp imghash leven
```
with three separate calls (one per package -- the file-based mutex handles concurrent calls safely, and this requires zero CLI code or test changes):
```
node *appdev-cli* install-dep --package sharp
node *appdev-cli* install-dep --package imghash
node *appdev-cli* install-dep --package leven
```

In `projection-critic.md`, replace line 183:
```
node *appdev-cli* install-dep --dev ajv
```
with:
```
node *appdev-cli* install-dep --package ajv
```

Using `--package` per user decision from research (simplest fix, no CLI changes, no test changes needed). Do NOT modify appdev-cli.mjs or test-appdev-cli.mjs.

**Fix 2: SAFETY_CAP static-serve --stop (RECOVERY-03)**

In `SKILL.md`, in the SAFETY_CAP section (around lines 358-367), insert a static-serve --stop call BETWEEN the git tag (line 362-363) and the "Run one extra wrap-up round" line (line 364). The insertion should match the pattern used by the `should_continue` path (lines 405-408):

```markdown
- Stop static servers before wrap-up generation:
  ```
  Bash(node ${CLAUDE_PLUGIN_ROOT}/scripts/appdev-cli.mjs static-serve --stop)
  ```
  Why: The wrap-up Generator produces a new build. Stopping the old server
  ensures critics evaluate the fresh build, not the stale one.
```

Insert this BEFORE "Run one extra wrap-up round" and AFTER the git tag line. Do NOT move or remove the existing static-serve --stop that appears after the wrap-up evaluation (lines 393-396) -- both are needed: one before Generation, one after the final evaluation.

**Fix 3: @playwright/test installation (PLAYWRIGHT-02, PLAYWRIGHT-04)**

In `SKILL.md` Step 0.5 (around lines 141-149), add a new install line for @playwright/test between the @playwright/cli and serve installs. The section should read:

```markdown
Install @playwright/cli, @playwright/test, and serve as dev dependencies:

```
Bash(npm install --save-dev @playwright/cli)
```

```
Bash(npm install --save-dev @playwright/test)
```

```
Bash(npm install --save-dev serve)
```
```

Update the heading text from "Install @playwright/cli and serve" to "Install @playwright/cli, @playwright/test, and serve" to reflect all three packages.

Also update the note at lines 16-17 in the SKILL.md header that says "Requires @playwright/cli as a project devDependency" to include @playwright/test:
```
Requires @playwright/cli and @playwright/test as project devDependencies (installed
automatically in Step 0.5 workspace setup -- no system PATH dependency).
```
  </action>
  <verify>
    <automated>node plugins/application-dev/scripts/test-appdev-cli.mjs</automated>
  </verify>
  <done>
- perceptual-critic.md uses `install-dep --package sharp`, `--package imghash`, `--package leven` (three separate calls)
- projection-critic.md uses `install-dep --package ajv`
- No `--dev` flag remains in any install-dep call across the codebase
- SKILL.md SAFETY_CAP path has static-serve --stop before wrap-up Generator spawn
- SKILL.md Step 0.5 installs @playwright/test alongside @playwright/cli and serve
- All 57 existing CLI tests pass (no CLI code was changed)
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix baseURL configuration in skeleton test and projection-critic</name>
  <files>
    plugins/application-dev/skills/application-dev/references/evaluator/PLAYWRIGHT-EVALUATION.md
    plugins/application-dev/agents/projection-critic.md
  </files>
  <action>
**Fix 4: baseURL configuration (PLAYWRIGHT-02, PLAYWRIGHT-04)**

In `PLAYWRIGHT-EVALUATION.md`, update the skeleton acceptance test (around lines 42-72) to include explicit baseURL configuration. Add `test.use({ baseURL })` after the import and before the first `test.describe`. The skeleton should become:

```typescript
// evaluation/round-N/projection/acceptance-tests.spec.ts
import { test, expect } from '@playwright/test';

// Configure baseURL from the static-serve port.
// Read the port from the static-serve JSON response and substitute below.
test.use({ baseURL: 'http://localhost:PORT' });

test.describe('Feature 1: Artwork Gallery [Core]', () => {
  test.beforeEach(async ({ page }) => {
    await page.goto('/');
  });
  // ... (rest of skeleton unchanged)
});
```

Add a note to the "Key patterns in the skeleton" list (around line 74-78) explaining the baseURL pattern:
```
- `test.use({ baseURL })` configured from static-serve port -- ensures tests
  hit the correct server regardless of which port was assigned
```

In `projection-critic.md`, verify that Step 0 (lines 39-43) already mentions reading the port from static-serve JSON and setting baseURL. It does (lines 41-43 say "The server responds with JSON containing the `port`. Use that port for all `npx playwright-cli` and `npx playwright test` commands. Set the `baseURL` in your test configuration to `http://localhost:<port>`."). No change needed to projection-critic.md Step 0.

However, also check the write-and-run section of projection-critic.md (around line 60-70 where it describes writing the acceptance test file). If it does not explicitly mention substituting the port into `test.use({ baseURL })`, add a reminder line there:
```
Substitute the static-serve port into `test.use({ baseURL: 'http://localhost:<port>' })` at the top of the test file.
```

Do NOT hardcode a specific port number (like 5173) -- the skeleton uses PORT as a placeholder that the critic substitutes at runtime from the static-serve JSON response.
  </action>
  <verify>
    <automated>git grep -n "test.use" -- "plugins/application-dev/skills/application-dev/references/evaluator/PLAYWRIGHT-EVALUATION.md"</automated>
  </verify>
  <done>
- PLAYWRIGHT-EVALUATION.md skeleton test has `test.use({ baseURL: 'http://localhost:PORT' })` before any test.describe
- Key patterns list includes baseURL explanation
- Projection-critic.md write-and-run section reminds to substitute the static-serve port into test.use({ baseURL })
- No hardcoded port numbers (5173 or similar) in any file
  </done>
</task>

</tasks>

<verification>
1. `node plugins/application-dev/scripts/test-appdev-cli.mjs` -- all 57 tests pass (no CLI code changed)
2. `git grep "install-dep --dev" -- "plugins/application-dev/"` -- returns empty (no stale --dev flags)
3. `git grep "static-serve --stop" -- "plugins/application-dev/skills/application-dev/SKILL.md"` -- returns 3 matches (SAFETY_CAP before wrap-up, SAFETY_CAP after evaluation, should_continue path)
4. `git grep "@playwright/test" -- "plugins/application-dev/skills/application-dev/SKILL.md"` -- returns at least 1 match
5. `git grep "test.use" -- "plugins/application-dev/skills/application-dev/references/evaluator/PLAYWRIGHT-EVALUATION.md"` -- returns at least 1 match
</verification>

<success_criteria>
All four integration bugs from the v1.1 milestone audit are fixed:
1. install-dep calling convention matches CLI's --package flag
2. SAFETY_CAP stops static-serve before wrap-up Generator
3. @playwright/test installed in Step 0.5
4. baseURL configuration demonstrated in skeleton test
All 57 CLI tests pass unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/10-v1.1-audit-gap-closure/10-01-SUMMARY.md`
</output>
