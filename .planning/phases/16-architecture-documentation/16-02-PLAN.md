---
phase: 16-architecture-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - plugins/application-dev/README.md
autonomous: true
requirements:
  - DOCS-01
must_haves:
  truths:
    - "README.md describes 5 agents (planner, generator, 3 critics) not 3"
    - "README.md mentions score-based convergence, not fixed round count"
    - "plugins/application-dev/README.md describes 5-agent ensemble, not 4-agent"
    - "plugins/application-dev/README.md evaluation table has 4 dimensions including Robustness"
    - "plugins/application-dev/README.md lists Perturbation Critic alongside Perceptual and Projection"
  artifacts:
    - path: "README.md"
      provides: "Accurate marketplace landing page reflecting v1.2 architecture"
      contains: "five-agent"
    - path: "plugins/application-dev/README.md"
      provides: "Accurate plugin README reflecting v1.2 ensemble architecture"
      contains: "Perturbation Critic"
---

<objective>
Update both README files to reflect the v1.2 five-agent ensemble architecture.

Purpose: Both READMEs are stale -- the marketplace README (repo root) mentions "three-agent architecture" and a singular "Evaluator", while the plugin README says "four-agent ensemble" and lists only 2 critics. The v1.2 reality is 5 agents with 3 specialized critics and 4 scoring dimensions.

Output: Updated README.md and plugins/application-dev/README.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@README.md
@plugins/application-dev/README.md
@.planning/PROJECT.md
@AGENTS.md

<interfaces>
<!-- No code interfaces -- documentation-only updates to match current architecture -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update repo-root README.md marketplace landing page</name>
  <files>README.md</files>
  <read_first>
    README.md
    .planning/PROJECT.md
  </read_first>
  <action>
Update README.md (marketplace landing page, 36 lines) to reflect v1.2 architecture. This file lives at repo root and is NOT distributed to users per AGENTS.md.

Specific edits (keep the file concise -- it is a landing page, not documentation):

1. Replace the application-dev description block (lines 22-29) with updated content:

Old:
```
Autonomous application development from a short prompt (1-4 sentences). Uses a GAN-inspired three-agent architecture:

- **Planner** -- expands your prompt into an ambitious product spec
- **Generator** -- builds the full application
- **Evaluator** -- QAs the running app via playwright-cli with adversarial skepticism

Up to 3 build/QA rounds run autonomously without user intervention.
```

New:
```
Autonomous application development from a short prompt (1-4 sentences). Uses a GAN-inspired five-agent ensemble architecture:

- **Planner** -- expands your prompt into an ambitious product spec
- **Generator** -- builds the full application
- **Perceptual Critic** -- scores visual quality via browser screenshots and interaction
- **Projection Critic** -- scores functionality via automated behavioral tests
- **Perturbation Critic** -- scores resilience via adversarial stress testing

Score-based convergence detection runs autonomously without user intervention.
```

2. Update Prerequisites section (lines 39-41):

Old:
```
- Claude Code
- `playwright-cli` on PATH (for the Evaluator agent's browser-based QA)
```

New:
```
- Claude Code
- `playwright-cli` on PATH (for the critic agents' browser-based evaluation)
```
  </action>
  <verify>
    <automated>git grep -c "five-agent" -- README.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains "five-agent ensemble architecture"
    - README.md contains "Perceptual Critic"
    - README.md contains "Projection Critic"
    - README.md contains "Perturbation Critic"
    - README.md contains "Score-based convergence"
    - README.md does NOT contain "three-agent"
    - README.md does NOT contain "Up to 3 build/QA rounds"
    - README.md does NOT contain "Evaluator" as a standalone agent name
    - README.md contains "critic agents" in Prerequisites
  </acceptance_criteria>
  <done>
    README.md marketplace landing page accurately reflects v1.2 five-agent ensemble architecture.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update plugin README.md with v1.2 ensemble architecture</name>
  <files>plugins/application-dev/README.md</files>
  <read_first>
    plugins/application-dev/README.md
    .planning/PROJECT.md
  </read_first>
  <action>
Update plugins/application-dev/README.md (distributed to users, 88 lines) to reflect v1.2 architecture. This file IS inside plugins/ and ships to users.

Specific edits:

1. Line 1 heading description -- change "four-agent" to "five-agent":
Old: "Autonomous application development plugin for Claude Code, using a GAN-inspired four-agent ensemble architecture."
New: "Autonomous application development plugin for Claude Code, using a GAN-inspired five-agent ensemble architecture."

2. Line 7 overview -- change "four specialized agents" to "five specialized agents":
Old: "This plugin orchestrates long-running application development from a short prompt (1-4 sentences). It uses four specialized agents in an adversarial feedback loop:"
New: "This plugin orchestrates long-running application development from a short prompt (1-4 sentences). It uses five specialized agents in an adversarial feedback loop:"

3. After the Projection Critic bullet (line 13), add the Perturbation Critic:
Add after the Projection Critic entry:
```
- **Perturbation Critic**: Scores Robustness by running adversarial tests -- viewport extremes, rapid navigation, input perturbation, and console monitoring under stress. Evaluates resilience beyond what the spec requires.
```

4. Line 39 (step 3 Evaluate) -- update from "Both critics" to "All three critics":
Old: "3. **Evaluate** -- Both critics evaluate the running app in parallel -- the Perceptual Critic observes visual quality, the Projection Critic runs acceptance tests. The CLI compiles their findings into a single evaluation report."
New: "3. **Evaluate** -- All three critics evaluate the running app in parallel -- the Perceptual Critic observes visual quality, the Projection Critic runs acceptance tests, and the Perturbation Critic stress-tests resilience. The CLI compiles their findings into a single evaluation report."

5. Lines 56-58 Architecture section -- update critic count:
Old: "- Two specialized critics (Perceptual + Projection) evaluate in parallel, each in its own isolated context. The CLI aggregates their findings deterministically."
New: "- Three specialized critics (Perceptual + Projection + Perturbation) evaluate in parallel, each in its own isolated context. The CLI aggregates their findings deterministically."

6. Evaluation table (lines 65-69) -- add Robustness row after Visual Design:
Add this row to the table:
```
| Robustness | 6/10 | Resilience under adversarial conditions (Perturbation Critic) |
```

The full table should be:
```
| Dimension | Threshold | What It Measures |
|-----------|-----------|------------------|
| Product Depth | 7/10 | Feature completeness vs. spec (CLI-computed from acceptance tests) |
| Functionality | 7/10 | Does it actually work when used? (Projection Critic) |
| Visual Design | 6/10 | Coherent identity, not AI-slop (Perceptual Critic) |
| Robustness | 6/10 | Resilience under adversarial conditions (Perturbation Critic) |
```
  </action>
  <verify>
    <automated>git grep -c "five-agent\|Perturbation Critic\|Robustness" -- plugins/application-dev/README.md</automated>
  </verify>
  <acceptance_criteria>
    - plugins/application-dev/README.md contains "five-agent ensemble architecture" (line 1)
    - plugins/application-dev/README.md contains "five specialized agents" (overview)
    - plugins/application-dev/README.md contains "Perturbation Critic" with description
    - plugins/application-dev/README.md contains "All three critics"
    - plugins/application-dev/README.md contains "Three specialized critics (Perceptual + Projection + Perturbation)"
    - plugins/application-dev/README.md evaluation table contains "Robustness" row with "6/10"
    - plugins/application-dev/README.md does NOT contain "four-agent"
    - plugins/application-dev/README.md does NOT contain "four specialized"
    - plugins/application-dev/README.md does NOT contain "Both critics evaluate"
    - plugins/application-dev/README.md does NOT contain "Two specialized critics"
  </acceptance_criteria>
  <done>
    plugins/application-dev/README.md accurately reflects v1.2 five-agent ensemble with 4 scoring dimensions including Robustness.
  </done>
</task>

</tasks>

<verification>
1. README.md contains "five-agent ensemble" and lists all 5 agents
2. README.md does NOT contain "three-agent" or singular "Evaluator"
3. plugins/application-dev/README.md contains "five-agent" and "five specialized"
4. plugins/application-dev/README.md lists Perturbation Critic
5. plugins/application-dev/README.md evaluation table has 4 dimensions including Robustness
6. plugins/application-dev/README.md does NOT contain "four-agent", "Both critics", or "Two specialized"
</verification>

<success_criteria>
- Both README files accurately describe the v1.2 five-agent ensemble architecture
- Plugin README evaluation table includes all 4 scoring dimensions
- All stale agent counts and critic references are updated
</success_criteria>

<output>
After completion, create `.planning/phases/16-architecture-documentation/16-02-SUMMARY.md`
</output>
